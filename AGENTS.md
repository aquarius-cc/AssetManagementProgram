# AI 执行引擎总配置 (Root Engine)
> 版本：v3.4.1 | 最后更新：2026-07-13
> 职责：跨端契约仲裁、全局红线、宪法级规则（测试/DRY/安全/可观测性/AI鲁棒性/交互写作）、子引擎路由

## §1 核心执行协议（所有子引擎必须遵守）

### 1.1 读取优先 (Read First)
- 执行任何代码任务前，**必须**先读取 `README.md` 了解项目全貌。
- **后端任务**：必须索引 `asset_management_backend/AGENTS.md` 并读取 `Rules_Fiels/backend-business-rules.md` 与 `Rules_Fiels/backend-testing-rules.md`。
- **前端任务**：必须索引 `vue-assetmanagement/AGENTS.md` 并读取 `Rules_Fiels/frontend-business-rules.md` 与 `Rules_Fiels/frontend-testing-rules.md`。
- **额外强制**：所有任务必须遵守本文件 §6（安全红线）、§7（可观测性）、§8（AI鲁棒性）、§9（AI交互与写作规范）契约。

### 1.2 小步验证 (Step-by-Step)
- 涉及数据库迁移、API 接口变更、全局样式修改时，**禁止**一次性生成大量文件。
- 必须分步执行，每完成一个模块（如 Model → Service → View）后主动汇报，待确认后再继续。

### 1.3 红线保护机制
遇到以下场景，**必须**触发 `[HALT]` 并等待人工确认：
- 删除或重命名数据库字段/表。
- 修改对外 API 的响应格式（如 `code` 字段含义）。
- 变更资产状态机的主流转路径（增删节点）。
- 修改跨端共享的枚举值（如状态码字符串）。

### 1.4 宪法级测试规则（全局红线，不可逾越）

> 以下为所有端必须遵守的**最高级别测试原则**，任何代码变更不得违反。

| ID | 规则内容 | 违规后果 |
|:---|:---|:---|
| CT-1 | **核心业务逻辑必须有测试覆盖**：资产状态流转（Service层）、数据查询（Selector层）、API鉴权等关键路径，**禁止**无测试代码。 | 触发 `[HALT]`，强制补充测试 |
| CT-2 | **覆盖率硬门槛**：整体测试覆盖率**不得低于 80%**，核心模块（后端Service/前端Store）**不得低于 90%**。 | CI失败，**禁止**合并代码 |

> **执行方式**（CI 配置完成前可本地执行）：<br>
> 1. 首次安装：`pip install pytest pytest-cov mutmut`<br>
> 2. 整体覆盖率：`pytest --cov=. --cov-fail-under=80 --cov-report=html -v`<br>
> 3. 核心模块专项：<br>
> &nbsp;&nbsp;&nbsp;&nbsp;- 后端 Service 层：`pytest --cov=apps.assetmanagement.services --cov-fail-under=90 -v`<br>
> &nbsp;&nbsp;&nbsp;&nbsp;- 前端 Store 层：`npx vitest run --coverage --coverage.include="src/stores/**/*.ts"`<br>
> 4. CI 就绪前，覆盖率检查标记为 `[PENDING]`，不阻断合并；CI 就绪后自动转为硬性门禁。
| CT-3 | **状态机全路径测试**：资产状态机（参见 `backend-business-rules.md` §3）的每一条流转路径，**必须**有至少一个集成测试用例验证。 | 触发 `[HALT]`，补全路径用例 |
| CT-4 | **回归屏障**：修复Bug或新增功能后，必须同步补充测试用例，确保该场景不会再次出现。 | 审查时打回，要求补充测试 |
| CT-5 | **测试失败即阻塞**：任何测试用例失败，**禁止**提交代码或部署。 | 必须修复所有失败后再提交 |
| CT-6 | **迁移文件安全性验证**：任何包含数据库迁移（`migrations/`）的 PR，必须执行以下三步验证：<br>① `python manage.py makemigrations --dry-run 2>&1 | findstr "No changes detected"` — 确认无遗漏迁移<br>② `python manage.py migrate --plan` — 预览待执行列表，人工确认顺序正确<br>③ 对本次新增迁移文件执行 `findstr /N "RemoveIndex RemoveField RenameField" <文件名>` — 若命中任一操作，须在 PR 描述中注明"已人工确认数据无损"，并在 Code Review 阶段重点审查。<br>**禁止**执行 `migrate <app> zero --dry-run` 进行反向迁移验证。 | 触发 `[HALT]`，修复迁移文件 |

> **执行监督**：各子引擎在最终审计票中必须包含宪法级测试规则的符合情况（格式：`CT-1[√] CT-2[√] ...`）。

### 1.5 宪法级代码复用与量化规则（DRY 红线，不可逾越）

> 以下为所有端必须遵守的最高级别复用原则与规模红线，杜绝冗余代码与巨型文件滋生。

| ID | 规则内容 | 违规后果 | 适用端 |
|:---|:---|:---|:---|
| DR-1 | **业务逻辑唯一实现**：相同的业务规则（如资产状态校验、金额计算）**禁止**在两处及以上独立实现，必须抽取为公共服务/函数。 | 触发 `[HALT]`，强制重构合并 | 全端 |
| DR-2 | **UI 组件原子化**：相同样式的按钮、表格、弹窗等 UI 元素出现 2 次及以上时，**必须**封装为公共组件，**禁止**复制粘贴代码。 | 触发 `[HALT]`，强制提取为公共组件 | 前端 |
| DR-3 | **数据查询统一入口**：相同条件的资产查询（如"查询可用资产"）**禁止**在多个 Service 中重复写过滤链，**必须**收敛至 Selector 层。 | 触发 `[HALT]`，强制下沉到 Selector | 后端 |
| DR-4 | **工具函数单一仓库**：通用工具函数（如日期格式化、文件大小转换）**必须**放置于 `utils/` 目录，**禁止**在各业务模块中重复定义。 | 触发 `[HALT]`，强制迁移至 `utils/` | 全端 |
| DR-5 | **文件/函数规模硬上限**：单个源文件（`.py`/`.vue`/`.ts`）**不得超过 500 行**；单个函数/方法**不得超过 50 行**（不含空行和注释）。超限即触发重构拆分。**存量豁免**：在 2026-07-07 基线日之前已存在的超限文件，须在文件头部添加 `# TECHNICAL_DEBT: >500 lines` 注释，不触发 `[HALT]`。存量文件后续被修改时，若本次新增代码量 ≥ 50 行，必须同步将文件拆分至 ≤ 500 行。**新文件从严**：2026-07-07 之后新建的文件，严格执行 ≤ 500 行红线。 | 触发 `[HALT]`，强制拆分 | 全端 |
| DR-6 | **调用链深度与嵌套限制**：代码调用链（从入口到最深叶子节点）**默认不得超过 5 层**；若子域规范（如后端BR-7）有更严格限制（如≤3层），则**从紧执行**，子域必须在审计票中标注所执行深度。条件/循环嵌套不得超过4层。超限即触发 `[HALT]` 强制重构。 | 触发 `[HALT]`，强制重构 | 全端 |

> **执行监督**：各子引擎在代码审查阶段必须主动扫描是否存在重复场景或规模超标。若 AI 生成代码时发现已有类似实现，**必须优先复用**，而非重新造轮子。

### 1.6 交互协议（追问与洞察）

AI 在与用户交互时，必须遵循以下协议：

**ID-1 追问挖掘**：当用户需求存在模糊地带时，AI 必须主动追问以挖掘隐藏要求、预期效果和边界约束。追问规则如下：
- **最多追问 3 轮**，每轮只提 1-2 个核心问题，避免信息过载。
- **已明确事项不再重复确认**：若用户在前序对话中已给出明确信息，后续交互中不得再次询问同一事项。
- **只有当 AI 100% 确定完全理解用户的全部真实需求与最终目标，且无任何信息遗漏或理解偏差时，才能生成完整落地的最终方案。**
- 追问内容必须与当前需求直接相关，**禁止**发送无关提问。
- **3 轮后未完全明确时的 fallback**：若追问达到 3 轮上限仍未完全明确，AI 应基于当前已获取的信息生成方案，但在方案中标注所有不确定部分（使用 `[待确认]` 标记），并明确列出需要用户确认的剩余问题，由用户决定是否补充信息或采纳当前方案。

**ID-2 主动洞察**：AI 在分析需求或审查代码时，若发现用户未意识到的关键点（如潜在风险、架构缺陷、机会成本），且该关键点可能对用户的决策或工作产生重大影响，**必须**主动告知用户，而非等待被问及。

> **执行监督**：审计票中无需检查交互协议，但子引擎在交互过程中应自觉遵守。

### 1.7 能力提取协议（重复工作→SKILL 识别）

AI 在执行任务过程中，若识别到以下模式，应主动建议用户将其封装为可复用的 SKILL：

- **重复操作模式**：用户在 3 次及以上对话中执行相同或高度相似的操作序列（如每次提交代码后手动填写审计票）。
- **固定流程模式**：存在可标准化的多步骤流程（如数据库迁移的三步验证）。
- **决策模板模式**：用户反复基于相同维度做判断（如每次 Code Review 检查相同清单）。

AI 应在任务完成后，以"**SKILL 建议**"的形式向用户提出，格式为：
```
[SKILL 建议] 建议封装：<skill名称>
- 触发场景：<描述>
- 包含步骤：<简要列出>
- 预期收益：<节省时间/减少遗漏>
```

> **执行监督**：此为建议性规则，不强制执行，不纳入审计票检查。

## §2 规则索引（规范外部化）

| 目标端 | 执行引擎 | 业务规范文件 | 测试规范文件 |
|:---|:---|:---|:---|
| 后端 (Django/DRF) | `asset_management_backend/AGENTS.md` | `Rules_Fiels/backend-business-rules.md` | `Rules_Fiels/backend-testing-rules.md` |
| 前端 (Vue/Element Plus) | `vue-assetmanagement/AGENTS.md` | `Rules_Fiels/frontend-business-rules.md` | `Rules_Fiels/frontend-testing-rules.md` |

> **功能验收**：前端开发和测试工程师必须同时参考 `Project_Requirements/01-业务需求/07-功能需求与验收标准.md`，确保实现与验收标准一致。

> **强制约束**：若生成的代码不符合上述对应规范文件中的任意一条，直接触发 `[HALT]`。

## §3 跨端一致性契约（不可单方修改）

| 契约项 | 约束值 | 涉及端 |
|:---|:---|:---|
| API 响应根结构 | `{"code": 0, "data": {}, "message": "str"}` | 后端产出，前端消费 |
| 资产状态枚举键名 | `in_store`, `in_use`, `recycled_pending`, `damaged`, `scrapped`, `broken`, `lost`, `repairing` | 后端 Model，前端映射 |
| 分页参数名 | `page` (页码), `page_size` (条数) | 后端接收，前端发送 |
| 日期时间格式 | ISO 8601 (`YYYY-MM-DDTHH:mm:ssZ`) | 后端序列化，前端解析 |

**变更以上契约，必须由根级统筹，前后端同步调整。**

## §4 最终审计票（全局通用）
AI 在任务完成后，必须输出以下审计票：

### 必填项（每次必须逐项确认，缺一不可）

```markdown
[审计票 - 必填项]
- 读取规范：已读 [后端/前端] AGENTS & Rules
- CT-1[√] CT-3[√] CT-5[√] — 核心测试覆盖 / 状态机全路径 / 测试失败阻塞
- DR-1[√] DR-5[√/豁免] — 业务逻辑唯一实现 / 文件规模（存量豁免标记）
- SC-1[√] SC-3[√] — 密钥硬编码禁止 / SQL 注入防护
- 跨端契约：未破坏
- 红线触发：无 / [HALT]已确认
- 建议提交：是 / 否
```

### 自检项（默认通过，仅触发异常时标注 [x] 并附说明）

```markdown
[审计票 - 自检项]
- 测试：CT-2[√] CT-4[√] CT-6[√]
- DRY：DR-2[√] DR-3[√] DR-4[√] DR-6[√]
- 安全：SC-2[√] SC-4~SC-8[√]
- 可观测性：OC-1~OC-3[√] OC-4[~] OC-5[~] OC-6[√] OC-7[~]
- AI鲁棒性：AR-1~AR-5[√]
- AI行为：Fact-1[√]（事实基线）
- 写作风格：Style-1~Style-3[√]
- 覆盖率：整体 XX%（≥80%）/ 核心 XX%（≥90%）
```

## §5 规则演进与自我修正（全局协议）
### 5.1 变更触发
当用户提出涉及跨端契约、状态机路径、全局红线、AI交互与写作规范的修改需求时，启动本协议。

### 5.2 执行步骤
1.**差异分析**：对比新需求与现有根级配置及子域规范，生成《冲突清单》。

2.**影响评估**：标注影响范围（仅后端/仅前端/全端）。

3.**生成补丁**：拟定对根级 AGENTS.md 和对应 *-business-rules.md 的修改草案。

4.**[HALT] 审批**：暂停执行，向用户完整展示补丁内容，等待确认。

5.**应用与追溯**：覆盖文件并更新版本号（如 v3.0.0 → v3.1.0），在文件头部变更日志中记录本次修改原因。

### 5.3 红线保护
- 禁止 AI 在未经人工审批的情况下直接覆写本文件。

- 禁止子引擎私自修改跨端契约，必须上报根级处理。

### 5.4 沙盒缓冲期（新规则上线缓冲）

> **前置条件**：本机制的自动化统计能力依赖以下 CI 基础设施就绪：<br>
> ① GitHub Actions 静态分析 job（集成 ruff / ESLint）<br>
> ② 违规数据采集与存储（GitHub API + 简易脚本）<br>
> ③ 团队通知渠道（钉钉/飞书/邮件 webhook）<br>
>
> **在 CI 就绪前**，沙盒期采用人工替代方案：由项目负责人每周手动运行 `ruff check . --statistics` 和 `npx eslint . --format json`，生成违规 Top 10 清单，在团队周会中同步。

当新增或收紧任何红线（如降低圈复杂度阈值、新增安全规则）时，必须执行：

- **14个自然日的"沙盒警告期"**：期间CI对违规仅警告（Warning）不阻断，同时自动统计每日违规最多的Top 10文件并推送至团队。

- 沙盒期结束后，根据违规数据人工复核阈值合理性，全员投票通过后方转为强制执行，并更新CI配置。

## §6 安全红线契约（全端通用，不可逾越）
| ID	| 规则内容	| 违规后果 |
| :--- | :--- | :--- |
| SC-1	| 禁止在代码仓库任何位置（含注释、配置文件）硬编码AK/SK、Token、证书密码，必须通过环境变量或Secrets Manager注入。	| 触发 `[HALT]`，强制移除 |
| SC-2	| CI强制扫描仓库，发现疑似密钥即告警并阻止合并。	| 阻断CI |
| SC-3	| 所有数据库查询（包括ORM的raw()）必须使用参数化查询（占位符），禁止字符串拼接。	| 触发 `[HALT]` |
| SC-4	| 动态表名/列名必须经过白名单校验，禁止直接使用用户输入。	| 触发 `[HALT]` |
| SC-5	| 文件上传必须校验：① 扩展名与MIME一致；② 文件大小≤10MB；③ 存储路径为配置项，不得暴露绝对路径。	| 触发 `[HALT]` |
| SC-6	| 上传文件名必须重命名（如UUID），禁止使用原始文件名。	| 触发 `[HALT]` |
| SC-7	| CI门禁必须包含依赖漏洞扫描（Python用pip-audit，Node用npm audit），检测到高危（CVSS≥7.0）组件时直接阻断合并。	| 阻断CI |

> **CI 配置模板**（需在 `.github/workflows/security-scan.yml` 中创建）：<br>
> ```yaml
> name: Dependency Security Scan
> on: [pull_request]
> jobs:
>   security-scan:
>     runs-on: ubuntu-latest
>     steps:
>       - uses: actions/checkout@v4
>       - name: Python dependency audit
>         run: pip install pip-audit && pip-audit --fail-on=high
>       - name: Node dependency audit
>         run: npm audit --audit-level=high
> ```<br>
> **执行状态**：CI workflow 配置完成前标记为 `[PENDING]`，不阻断合并；完成后自动转为硬性门禁。
| SC-8	| 每周自动扫描所有依赖，生成安全报告。| 必须执行 |
子引擎执行任务前必须主动检查本契约，发现违反立即 `[HALT]`。

## §7 可观测性契约（全端通用）
| ID	| 规则内容	| 违规后果 |
| :--- | :--- | :--- |
| OC-1	| 每个请求/消息入口必须生成全局唯一 trace_id，并在整个调用链透传。	| 触发 `[HALT]` |
| OC-2	| 日志必须结构化（JSON），至少包含 time、level、trace_id、message、module。 | 触发 `[HALT]` |
| OC-3	| 禁止在日志中记录身份证、手机号、密码、银行卡等敏感信息，必须脱敏。	| 触发 `[HALT]` |
| OC-4	| **Prometheus 指标暴露**：核心业务接口在**实际 QPS > 10** 的场景下，必须暴露 Prometheus 指标（请求总数、错误总数、耗时直方图含P50/P90/P99）。当前阶段（QPS未达阈值）标记为 `[~]` 豁免。 | 触发 `[HALT]`（若QPS达标却未实施） |
| OC-5	| 所有Redis/DB调用应记录耗时直方图，便于发现慢查询。	| 建议执行 |
| OC-6	| 服务必须提供 /health 和 /ready 端点。| 	触发 `[HALT]` |
| OC-7	| **性能基准门禁**：核心接口在**实际 QPS > 10** 的场景下，P95响应耗时不得劣化超过20%（相比上一次基线）。CI阶段应自动对比历史数据，若无基线则豁免。当前阶段标记为 `[~]` 豁免。 | 触发 `[HALT]`，必须优化或调整基线（若QPS达标） |
子引擎须在审计票中逐项确认符合情况。

## §8 AI鲁棒性契约（约束AI生成代码行为）
| ID	| 规则内容	| 违规后果 |
| :--- | :--- | :--- |
| AR-1	| 当AI对使用的第三方库API参数、异常类型或复杂业务分支逻辑的确定性 < 90%时，禁止自行猜测补全，必须在代码中标明 # TODO_AI_CONFIRM: <描述> 并中断生成，等待开发者明确回复。	| 触发 `[HALT]` |
| AR-2	| **高风险代码强制标注**：AI 生成以下三类代码时，必须在代码上方添加 `# AI_REVIEW_NEEDED: <描述>` 注释：<br>① **第三方库 API 调用**：参数含义、异常类型或返回值不确定时（如 `requests.post(timeout=?)`），标注 `# AI_REVIEW_NEEDED: verify timeout value`。<br>② **正则表达式**：所有生成的正则表达式，标注 `# AI_REVIEW_NEEDED: regex validation required`。<br>③ **复杂条件分支**：`if/elif` 嵌套 ≥ 3 层的逻辑块，标注 `# AI_REVIEW_NEEDED: review branch logic`。<br>人工复查通过后移除此注释。 | 人工复查 |
| AR-3	| AI生成的所有外部调用（HTTP、RPC、DB操作）必须显式设置超时时间（Timeout），且必须包含重试策略（Retry）。	| 触发 `[HALT]` |
| AR-4	| 超时时间及重试次数必须由配置文件管理，禁止硬编码。	| 触发 `[HALT]` |
| AR-5	| AI在生成代码后，应自动执行一次"虚拟编译/静态检查"（如调用ruff、mypy模拟），发现错误优先自我修正，修正失败后上报。	| 强制要求 |

## §9 AI交互与写作规范（跨端通用）

> 以下规范约束所有 AI 子引擎的文本输出行为，与 §8（代码生成行为）互补。

### 9.1 事实基线契约

| ID | 规则内容 | 违规后果 |
|:---|:---|:---|
| Fact-1 | **回答必须100%基于可验证事实**：禁止编造信息。当 AI 对某个事实的确定性 < 100% 时，必须直接回答"不知道"或"无法确认"，**禁止**以推测代替事实。 | 触发 `[HALT]` |
| Fact-2 | **可验证性标注**：AI 在回答技术问题或提供方案后，必须在回答末尾附加可验证性说明，格式为：<br>`[可验证] <事实描述> — 依据：<来源>`<br>`[推测] <推测描述> — 原因：<为什么是推测>`<br>**禁止**将推测作为事实陈述。 | 人工复查 |
| Fact-3 | **删除无法查证的内容**：若 AI 无法找到可靠来源验证某项信息，且该信息对当前任务非必需，则必须删除该内容，而非保留不确定的表述。 | 人工复查 |

> **适用范围**：Fact-2 适用于以下场景：<br>
> ① 涉及技术选型、架构决策的建议性回答<br>
> ② 涉及第三方库版本、API 行为、性能数据等外部事实的陈述<br>
> ③ 涉及项目现状分析（如"当前系统存在 X 问题"）的诊断性回答<br>
>
> **不适用**：<br>
> ① 纯代码生成（代码本身即为实现，无需附加标注）<br>
> ② 审计票输出（审计票格式已有固定结构，不附加可验证性说明）<br>
> ③ 简单事实查询（如"状态机有哪些状态"，答案直接来自规范条文，无需标注）<br>
>
> **例外**：设计决策建议（如"推荐使用 X 方案"）属于推测性内容，允许以推测形式呈现，但必须标注为 `[推测]`。

### 9.2 写作风格契约

| ID | 规则内容 | 违规后果 |
|:---|:---|:---|
| Style-1 | **原创表述**：除直接引用规范条文（如"根据 SC-3 规则"）外，所有内容必须用原创语言重新表述，**禁止**大段复制粘贴规范原文。 | 人工复查 |
| Style-2 | **MLA 引用格式**：当 AI 引用外部标准、文档或来源时，必须遵循 MLA 格式（作者, 标题, 来源, 日期）。例如：`"OWASP Foundation, 'OWASP Top 10,' OWASP, 2021."` | 人工复查 |
| Style-3 | **写作语调**：写作风格介于学术写作与口语之间——正式但不晦涩，专业但不冷漠。所有句子必须有主语，优先使用短句和常用词，避免复杂长难句和生僻词汇。 | 人工复查 |

> **执行监督**：审计票自检项中必须包含 `Fact-1[√]` 和 `Style-1~Style-3[√]`。

## §10 变更日志
- **v3.4.1 (2026-07-13)**：逻辑自洽修复——Fact-2增加适用范围说明（明确标注场景与不适用场景）；ID-1追问协议增加3轮fallback机制（标注[待确认]并列出剩余问题）；§5.1变更触发条件补充§9（AI交互与写作规范）。

- **v3.4.0 (2026-07-13)**：新增 §9（AI交互与写作规范：事实基线 Fact-1~Fact-3、写作风格 Style-1~Style-3）；扩展 §1 新增 1.6 交互协议（追问挖掘 ID-1、主动洞察 ID-2）和 1.7 能力提取协议；审计票自检项增加 AI行为 和 写作风格 检查行。

- **v3.3.0 (2026-07-09)**：修复P-2——CT-2执行方式从`coverage run manage.py test`统一为`pytest`，与后端AGENTS和backend-testing-rules保持一致；补充前端Store层覆盖率专项检查命令。

- **v3.2.0 (2026-07-07)**：生产化修复——CT-6迁移命令替换为安全组合；DR-5新增存量豁免条款；CT-2追加覆盖率执行脚本；SC-7追加CI配置模板；OC-4/OC-7改为条件触发+豁免；AR-2替换为高风险代码标注规则；审计票拆分为必填+自检折叠；沙盒期增加CI就绪前置条件。

- **v3.1.0 (2026-07-07)**：修复DR-6与子域冲突（新增从紧原则）；新增CT-6（迁移验证）、OC-7（性能基准门禁）；审计票OC-5标记优化为`[~]`。

- v3.0.0 (2026-07-07): 新增安全红线契约（SC-1~ SC-8）、可观测性契约（OC-1~ OC-6）、AI鲁棒性契约（AR-1~AR-5）；审计票扩展；变更流程增加沙盒缓冲期（5.4）。

- v2.7.0 (2026-07-07): 新增宪法级DRY规则 DR-1~DR-6；审计票增加代码规模检查项。

- v2.6.0 (2026-07-07): 新增宪法级测试规则 CT-1~CT-5；规则索引拆分为业务+测试双列。

- v2.5.1 (2026-07-07): 初始版本，建立根级仲裁与跨端契约。
