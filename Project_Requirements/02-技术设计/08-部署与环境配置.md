# 部署与环境配置文档

**版本：V1.0**
**日期：2026-07-09**

---

## 1. 环境矩阵

| 环境 | 用途 | 数据库 | 后端地址 | 前端地址 | 域名 |
|:---|:---|:---|:---|:---|:---|
| dev | 本地开发 | PostgreSQL 本地 | localhost:8000 | localhost:5173 | - |
| test | 集成测试 | PostgreSQL 测试库 | test-api.internal | test-app.internal | - |
| staging | 预发布验证 | PostgreSQL 预发库 | staging-api.internal | staging-app.internal | - |
| prod | 生产环境 | PostgreSQL 主库 | api.example.com | app.example.com | - |

---

## 2. 环境变量清单

### 2.1 后端环境变量（.env）

```bash
# ===== Django 核心 =====
DJANGO_SETTINGS_MODULE=config.settings.production
SECRET_KEY=<从 Secrets Manager 注入>
DEBUG=False
ALLOWED_HOSTS=api.example.com

# ===== 数据库 =====
DB_ENGINE=django.db.backends.postgresql
DB_NAME=asset_management
DB_USER=asset_user
DB_PASSWORD=<从 Secrets Manager 注入>
DB_HOST=127.0.0.1
DB_PORT=5432

# ===== Redis（缓存/会话） =====
REDIS_URL=redis://127.0.0.1:6379/0

# ===== JWT =====
JWT_ACCESS_TOKEN_LIFETIME_HOURS=2
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# ===== 文件上传 =====
MEDIA_ROOT=/data/uploads
FILE_UPLOAD_MAX_MEMORY_SIZE=10485760  # 10MB

# ===== CORS =====
CORS_ALLOWED_ORIGINS=http://app.example.com

# ===== 日志 =====
LOG_LEVEL=INFO
LOG_JSON_FORMAT=True
```

### 2.2 前端环境变量（.env）

```bash
VITE_API_BASE_URL=http://api.example.com/api/v1
VITE_APP_TITLE=资产管理系统
```

---

## 3. Docker 编排

### 3.1 docker-compose.yml

```yaml
version: "3.8"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: ${DB_ROOT_PASSWORD}
      POSTGRES_DB: asset_management
      POSTGRES_USER: asset_user
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  backend:
    build:
      context: ./asset_management_backend
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    volumes:
      - ./asset_management_backend:/app
      - uploads:/data/uploads

  frontend:
    build:
      context: ./vue-assetmanagement
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  postgres_data:
  redis_data:
  uploads:
```

### 3.2 后端 Dockerfile

```dockerfile
FROM python:3.12-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    libpq-dev gcc && \
    rm -rf /var/lib/apt/lists/*

COPY requirements/ /app/requirements/
RUN pip install --no-cache-dir -r requirements/prod.txt

COPY . /app/

EXPOSE 8000

CMD ["gunicorn", "config.wsgi:application", \
     "--bind", "0.0.0.0:8000", \
     "--workers", "4", \
     "--timeout", "120"]
```

### 3.3 前端 Dockerfile

```dockerfile
FROM node:20-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
```

---

## 4. 数据库初始化

### 4.1 首次部署

```bash
# 1. 创建数据库
psql -U postgres -c "CREATE DATABASE asset_management ENCODING 'UTF8';"

# 2. 执行迁移
python manage.py migrate

# 3. 创建超级管理员
python manage.py createsuperuser

# 4. 加载初始数据（如部门、资产类型等）
python manage.py loaddata initial_data.json
```

### 4.2 升级部署

```bash
# 1. 拉取最新代码
git pull origin main

# 2. 安装新依赖
pip install -r requirements/prod.txt

# 3. 执行迁移（无数据丢失）
python manage.py migrate

# 4. 收集静态文件
python manage.py collectstatic --noinput

# 5. 重启服务
systemctl restart asset-management
```

---

## 5. 修订历史

| 版本 | 日期 | 修订内容 |
|:---|:---|:---|
| V1.0 | 2026-07-09 | 初始版本 |
