# 前端页面与交互设计

**版本：V2.0**
**日期：2026-07-09**

---

## 1. 页面清单与路由规划

> **路由参数说明**：Vue Router 中 `:recordcode` 表示动态路由参数，`:` 前缀是 Vue Router 的语法标记，表示该段 URL 是可变的。例如 `/assets/ASSET-20260708-A3F9B2E1` 会匹配 `/assets/:recordcode`，其中 `recordcode` 的值为 `ASSET-20260708-A3F9B2E1`。使用 `recordcode` 而非 `code` 是因为系统中 recordcode 是所有模型的全局唯一主键。

| 路由 | 页面名称 | 所需角色 | 对应 API |
|:---|:---|:---|:---|
| `/login` | 登录页 | 全部 | POST /auth/login/ |
| `/` | 首页/仪表盘 | 全部 | GET /assets/simple/ |
| **资产管理** | | | |
| `/assets` | 资产列表 | 资产管理员+ | GET /assets/ |
| `/assets/:recordcode` | 资产详情 | 资产管理员+ | GET /assets/:recordcode/ |
| `/assets/create` | 新增资产 | 资产管理员+ | POST /assets/ |
| `/assets/:recordcode/edit` | 编辑资产 | 资产管理员+ | PUT /assets/:recordcode/ |
| `/assets/:recordcode/checkout` | 出库操作 | 资产管理员+ | POST /assets/:recordcode/checkout/ |
| `/assets/:recordcode/recycle` | 回收操作 | 资产管理员+ | POST /assets/:recordcode/recycle/ |
| `/assets/:recordcode/repair` | 送修操作 | 资产管理员+ | POST /assets/:recordcode/repair/ |
| `/assets/:recordcode/scrap` | 申请报废 | 资产管理员+ | POST /assets/:recordcode/apply-scrap/ |
| `/assets/:recordcode/logs` | 状态日志 | 审计员+ | GET /assets/:recordcode/logs/ |
| **业务记录** | | | |
| `/out-assets` | 出库记录 | 资产管理员+ | GET /out-assets/ |
| `/recycle-assets` | 回收记录 | 资产管理员+ | GET /recycle-assets/ |
| `/broken-assets` | 损坏记录 | 资产管理员+ | GET /broken-assets/ |
| `/lost-assets` | 遗失记录 | 资产管理员+ | GET /lost-assets/ |
| `/repair-assets` | 维修记录 | 资产管理员+ | GET /repair-assets/ |
| `/damaged-assets` | 报废审批 | 部门经理+ | GET /damaged-assets/ |
| `/unregistered-assets` | 未登记资产 | 部门经理+ | GET /unregistered-assets/ |
| **扫码查看** | | | |
| `/scan/:recordcode` | 手机扫码查看资产 | 全部（公开） | GET /public/scan/:recordcode/ |
| **组织管理** | | | |
| `/org/employees` | 员工管理 | 系统管理员 | GET /employees/ |
| `/org/departments` | 部门管理 | 系统管理员 | GET /departments/ |
| `/org/contacts` | 通讯录 | 全部（只读）/ 管理员（编辑） | GET /departments/tree/ + GET /employees/ |
| **基础数据** | | | |
| `/base/asset-types` | 资产类型管理 | 系统管理员 | GET /asset-types/ |
| `/base/storages` | 仓库管理 | 系统管理员 | GET /storages/ |
| `/base/contracts` | 合同管理 | 系统管理员 | GET /contracts/ |
| **系统管理** | | | |
| `/system/users` | 用户管理 | 系统管理员 | GET /auth/users/ |

> **角色层级说明**："资产管理员+"表示资产管理员及其上级角色（部门经理、系统管理员）；"部门经理+"表示部门经理及其上级角色（系统管理员）。具体权限矩阵见 `01-需求规格说明书.md` §2.2。

---

## 2. 核心页面交互设计

### 2.1 资产列表页

**布局**：
```
┌─────────────────────────────────────────────────────────────────────────────┐
│  资产管理                                                         [新增资产] │
├─────────────────────────────────────────────────────────────────────────────┤
│  资产编码: [________]  资产名称: [________]  规格型号: [________]           │
│  品牌:     [________]  资产类型: [▼ 全部]   状态:     [▼ 全部]            │
│  保管人:   [________]  仓库:     [▼ 全部]   使用性质: [▼ 全部]            │
│                                           [重置]  [搜索]                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  [批量删除]                                                                │
├─────┬────┬───────────┬──────────┬──────┬──────┬──────┬──────┬─────────────┤
│  □  │ 序号│ 资产编码   │ 资产名称  │ 类型  │ 状态  │ 保管人│ 仓库  │ 操作       │
├─────┼────┼───────────┼──────────┼──────┼──────┼──────┼──────┼─────────────┤
│  ☑  │ 1  │ IT-NB-..  │ ThinkPad │笔记本│ [在用]│ 张三  │ 主仓库│ 详情 编辑 删除│
│  ☐  │ 2  │ IT-SV-..  │ Dell R740│服务器│ [在库]│ 李四  │ 主仓库│ 详情 编辑 删除│
│  ☐  │ 3  │ OF-DC-..  │ 办公桌   │ 桌椅  │[已报废]│ -   │  -   │ 详情       │
│  ...│... │ ...       │ ...      │ ...  │ ...  │ ...  │ ...  │ ...        │
├─────┴────┴───────────┴──────────┴──────┴──────┴──────┴──────┴─────────────┤
│  共 150 条  < 1 2 3 ... 8 >  每页 [20▼]                                   │
└─────────────────────────────────────────────────────────────────────────────┘
```

**交互规则**：
- **多字段搜索**：顶部搜索栏支持按资产编码、名称、规格、品牌（文本输入模糊匹配）+ 资产类型、状态、仓库、使用性质（下拉框精确匹配）组合筛选，各条件之间为 AND 逻辑
- **重置**：点击"重置"清空所有搜索条件，恢复默认列表
- **序号列**：自动编号，跟随分页（第 1 页 1-20，第 2 页 21-40）
- **复选框**：支持单选和全选（表头复选框），选中后高亮行，显示"已选 N 条"
- **批量删除**：选中条目后点击"批量删除"，弹出二次确认："确认删除选中的 N 条资产？删除后不可恢复"
- **状态列**：使用 `StatusTag` 彩色标签展示，不可点击
- **操作列**：
  - "详情" → 跳转资产详情页
  - "编辑" → 跳转编辑页（仅 in_store 状态可编辑）
  - "删除" → 二次确认后软删除（仅 in_store 状态可删除）
  - 已报废资产操作列仅显示"详情"
- **列排序**：点击列头可按该列升序/降序排序，再次点击切换排序方向

### 2.2 出库操作弹窗

**触发**：资产详情页点击"出库"按钮

**弹窗内容**：
```
┌─────────────── 资产出库 ──────────────┐
│                                       │
│  资产编码: IT-NB-A3F9B2E1 (只读)       │
│  资产名称: ThinkPad X1 (只读)          │
│                                       │
│  出库类型: [领用 ▼]                    │
│  申请人:   [选择员工 ▼]                │
│  保管人:   [选择员工 ▼]                │
│  使用地点: [输入地点]                  │
│  预计归还: [日期选择器] (借用时必填)    │
│  说明:     [备注输入框]               │
│                                       │
│  [取消]                  [确认出库]    │
└───────────────────────────────────────┘
```

**交互规则**：
- 出库类型选择"借用"时，"预计归还"变为必填
- 选择员工时仅显示 active 状态员工
- 点击"确认出库"弹出二次确认："确认将资产 IT-NB-xxx 出库给 [员工名]？"
- 提交成功后自动关闭弹窗，刷新资产详情页

### 2.3 资产详情页

**布局**：
```
┌─────────────────────────────────────────────────────────┐
│  资产详情: ThinkPad X1 Carbon                   [返回]   │
│  状态: [在用]  编码: IT-NB-A3F9B2E1                     │
├─────────────────────────────────────────────────────────┤
│  [基本信息] [出库记录] [维修记录] [状态日志]              │
├─────────────────────────────────────────────────────────┤
│  资产类型:  IT > 电脑 > 笔记本                           │
│  品牌:      Lenovo                                       │
│  规格:      X1 Carbon Gen 11                             │
│  购买价格:  ¥8,999.00                                   │
│  购买日期:  2026-01-15                                   │
│  入库日期:  2026-01-20                                   │
│  当前仓库:  主仓库                                        │
│  保管人:    张三                                         │
│  使用地点:  北京办公室 3 楼                               │
│  物理成色:  良好                                          │
├─────────────────────────────────────────────────────────┤
│  [出库] [回收] [标记损坏] [标记遗失] [申请报废]           │
└─────────────────────────────────────────────────────────┘
```

**交互规则**：
- 底部操作按钮根据当前状态动态显示/隐藏
- `in_store` 状态：显示 [出库] [标记损坏] [标记遗失]
- `in_use` 状态：显示 [回收] [标记损坏] [标记遗失] [申请报废]
- `scrapped` 状态：所有操作按钮隐藏，显示"已报废（终态）"提示

### 2.4 二维码与扫码查看

**资产详情页二维码展示**：
```
┌─────────────────────────────────────────────────────────┐
│  资产详情: ThinkPad X1 Carbon                   [返回]   │
│  状态: [在用]  编码: IT-NB-A3F9B2E1                     │
├──────────────────────────────────┬──────────────────────┤
│  [基本信息] [出库记录] ...        │  ┌──────────────┐   │
│                                  │  │  ████  ████   │   │
│  资产类型: IT > 电脑 > 笔记本     │  │  █    █    █  │   │
│  品牌: Lenovo                    │  │  ████  ████   │   │
│  ...                             │  │  █  ████  █   │   │
│                                  │  │  ████  ████   │   │
│                                  │  └──────────────┘   │
│                                  │  扫码查看资产详情     │
│                                  │  [下载二维码] [打印]  │
├──────────────────────────────────┴──────────────────────┤
│  [出库] [回收] [标记损坏] [标记遗失] [申请报废]           │
└─────────────────────────────────────────────────────────┘
```

**手机扫码查看页**（路由：`/scan/:recordcode`）：
```
┌─────────────────────────────┐
│  资产详情（扫码查看）        │
├─────────────────────────────┤
│  资产编码: IT-NB-A3F9B2E1   │
│  资产名称: ThinkPad X1      │
│  资产类型: 笔记本电脑        │
│  品牌: Lenovo               │
│  规格: X1 Carbon Gen 11     │
│  当前状态: [在用]            │
│  保管人: 张三               │
│  使用地点: 北京办公室 3 楼   │
│  所在仓库: 主仓库            │
│  入库日期: 2026-01-20        │
│  物理成色: 良好              │
├─────────────────────────────┤
│  ⚠ 以下信息已脱敏            │
│  购买价格: ****             │
│  保管人联系电话: 138****5678      │
└─────────────────────────────┘
```

**交互规则**：
- 资产详情页右侧显示二维码（使用 `qrcode.vue` 或类似组件生成）
- 二维码内容为 JSON：`{"url": "https://app.example.com/scan/{recordcode}", "code": "{asset_code}"}`
- 点击"下载二维码"→ 导出 PNG 格式二维码图片
- 点击"打印"→ 调用浏览器打印对话框，仅打印二维码区域
- 手机扫码后跳转到 `/scan/:recordcode`（公开页面，无需登录）
- 扫码页面展示脱敏后的资产基本信息（价格显示"****"，联系电话前3后4脱敏）
- 扫码页面底部显示" powered by 资产管理系统"水印

---

## 3. 组织管理页面

### 3.1 员工管理页

**路由**：`/org/employees`

**布局**：
```
┌─────────────────────────────────────────────────────────────────────────────┐
│  员工管理                                                         [新增员工] │
├─────────────────────────────────────────────────────────────────────────────┤
│  工号: [________]  姓名: [________]  部门: [▼ 全部]  状态: [▼ 全部]        │
│                                           [重置]  [搜索]                   │
├─────┬────┬────────┬──────┬──────────┬──────┬──────┬──────┬─────────────────┤
│  □  │ 序号│ 工号    │ 姓名  │ 所属部门  │ 联系电话│ 办公位置│ 状态  │ 操作       │
├─────┼────┼────────┼──────┼──────────┼──────┼──────┼──────┼─────────────────┤
│  ☐  │ 1  │ EMP001 │ 张三  │ 研发一部  │ 138..│ 北京3F│ [在职]│ 详情 编辑 删除│
│  ☐  │ 2  │ EMP002 │ 李四  │ 研发二部  │ 139..│ 北京3F│ [在职]│ 详情 编辑 删除│
│  ☐  │ 3  │ EMP003 │ 王五  │ 财务部   │ 137..│ 北京5F│[离职]│ 详情 编辑    │
├─────┴────┴────────┴──────┴──────────┴──────┴──────┴──────┴─────────────────┤
│  共 86 条  < 1 2 3 ... 5 >  每页 [20▼]                                     │
└─────────────────────────────────────────────────────────────────────────────┘
```

**排序规则**：默认按 `employee_department.level`（部门层级）升序 → `employee_department.sort_order`（部门排序）升序 → `sort_order`（人员排序）升序

**交互规则**：
- 点击"详情" → 弹窗展示员工完整信息
- 点击"编辑" → 弹窗编辑员工信息
- 点击"删除" → 二次确认（若员工为资产保管人则提示"该员工为资产保管人，无法删除"）
- 已离职员工不显示"删除"按钮，仅显示"详情"和"编辑"
- 复选框 + 批量删除与资产列表页一致

### 3.2 部门管理页

**路由**：`/org/departments`

**布局**：
```
┌─────────────────────────────────────────────────────────────────────────────┐
│  部门管理                                                         [新增部门] │
├─────────────────────────────────────────────────────────────────────────────┤
│  部门编码: [________]  部门名称: [________]  层级: [▼ 全部]                 │
│                                           [重置]  [搜索]                   │
├─────┬────┬──────────┬──────────┬──────┬──────┬──────────────────────────────┤
│  □  │ 序号│ 部门编码   │ 部门名称  │ 上级部门│ 层级  │ 操作                    │
├─────┼────┼──────────┼──────────┼──────┼──────┼──────────────────────────────┤
│  ☐  │ 1  │ DEPT-000 │ 总公司    │  -   │  0   │ 详情 编辑                  │
│  ☐  │ 2  │ DEPT-001 │ 研发部    │总公司│  1   │ 详情 编辑 删除              │
│  ☐  │ 3  │ DEPT-002 │ 研发一部  │研发部│  2   │ 详情 编辑 删除              │
│  ☐  │ 4  │ DEPT-010 │ 财务部    │总公司│  1   │ 详情 编辑 删除              │
├─────┴────┴──────────┴──────────┴──────┴──────┴──────────────────────────────┤
│  共 28 条  < 1 2 >  每页 [20▼]                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

**排序规则**：默认按 `level`（层级）升序 → `sort_order`（排序）升序，同层级内按创建顺序排列

**交互规则**：
- 点击"详情" → 弹窗展示部门信息 + 下属部门列表 + 关联员工数量
- 点击"编辑" → 弹窗编辑部门信息（编码不可修改）
- 点击"删除" → 二次确认，若存在子部门或员工则提示"该部门下存在子部门/员工，无法删除"
- 根部门（level=0）不显示"删除"按钮
- 支持展开/折叠的树形视图模式切换（列表视图 ↔ 树形视图）

### 3.3 通讯录页

**路由**：`/org/contacts`

**布局**：
```
┌──────────────────┬──────────────────────────────────────────────────────────┐
│  通讯录           │  [部门信息区]                                            │
│                   │  部门名称: 研发一部        [编辑] [新增子部门]            │
│  ▼ 总公司 (86)    │  部门编码: DEPT-002                                      │
│    ▼ 研发部 (45)  │  上级部门: 研发部                                        │
│      ► 研发一部   │  层级: 2       排序: 1                                  │
│      ► 研发二部   │  部门信息员: 张三                                        │
│    ► 财务部 (12)  │                                                         │
│    ► 行政部 (8)   ├──────────────────────────────────────────────────────────┤
│    ► 人事部 (6)   │  [部门人员区]                                            │
│    ► IT部 (15)    │  状态: [▼ 全部]  搜索: [________]  [新增] [批量新增] [批量删除] │
│                   ├────┬──────┬──────┬──────┬──────┬──────┬────────────────┤
│                   │  □ │ 工号  │ 姓名  │ 联系电话│ 办公位置│ 状态  │ 操作        │
│                   ├────┼──────┼──────┼──────┼──────┼──────┼────────────────┤
│                   │  ☐ │EMP001│ 张三  │ 138..│ 北京3F│ [在职]│ 编辑         │
│                   │  ☐ │EMP005│ 赵六  │ 136..│ 北京3F│ [在职]│ 编辑         │
│                   │  ☐ │EMP012│ 钱七  │ 135..│ 北京3F│[离职]│ 编辑         │
│                   ├────┴──────┴──────┴──────┴──────┴──────┴────────────────┤
│                   │  共 18 条  < 1 >                                        │
└──────────────────┴──────────────────────────────────────────────────────────┘
```

**交互规则**：
- **左侧部门树**：
  - 默认全部展开，支持折叠/展开单个部门
  - 部门名称后括号内显示该部门下的在职员工数量（如"研发部 (45)"）
  - 点击部门名称，右侧加载该部门的信息和人员列表
  - 部门数量实时同步：右侧新增/删除/调岗员工后，左侧数量自动更新
- **右侧上部（部门信息区）**：
  - 展示当前选中部门的基本信息
  - 管理员可点击"编辑"修改部门信息
  - 管理员可点击"新增子部门"在当前部门下创建子部门
- **右侧下部（部门人员区）**：
  - 搜索框：按工号、姓名模糊搜索
  - 状态筛选：下拉框选择"全部/在职/离职/退休"
  - 操作按钮：新增（弹窗）、批量新增（导入 Excel）、批量删除
  - 列表：序号、工号、姓名、联系电话、办公位置、状态、操作（编辑）
  - 点击条目行可直接编辑该员工信息
- **数据联动**：右侧人员变更（新增/删除/调岗）后，左侧部门树的人员数量和部门列表自动刷新

---

## 4. 公共组件规范

| 组件名 | 用途 | 出现位置 |
|:---|:---|:---|
| `StatusTag` | 状态标签（彩色） | 所有列表页 |
| `AssetSelect` | 资产出库/回收时选择器 | 出库弹窗、回收弹窗 |
| `EmployeeSelect` | 员工选择器（仅 active） | 多个表单 |
| `DepartmentTree` | 部门树形选择/展示 | 通讯录、表单中的部门选择 |
| `ConfirmDialog` | 二次确认弹窗 | 所有写操作 |
| `FilterSidebar` | 多条件筛选侧边栏 | 资产列表等复杂列表页 |
| `SearchBar` | 多字段搜索栏（文本+下拉组合） | 所有列表页 |
| `BatchActionBar` | 批量操作栏（全选/批量删除） | 所有支持批量操作的列表页 |
| `Pagination` | 分页组件 | 所有列表页 |
| `EmptyState` | 空状态占位图 | 列表为空时 |
| `AuditLogTimeline` | 审计日志时间线 | 资产详情页 |

---

## 5. 状态颜色规范

| 状态 | 颜色 | 说明 |
|:---|:---|:---|
| in_store | #52C41A（绿） | 在库 |
| in_use | #1677FF（蓝） | 在用 |
| recycled_pending | #FAAD14（橙） | 已回收待发放 |
| broken | #FF4D4F（红） | 已损坏 |
| repairing | #722ED1（紫） | 维修中 |
| lost | #FF7A45（深橙） | 已遗失 |
| damaged | #F5222D（深红） | 待报废 |
| scrapped | #8C8C8C（灰） | 已报废 |
| active | #52C41A（绿） | 在职 |
| left | #8C8C8C（灰） | 离职 |
| retirement | #FAAD14（橙） | 退休 |

---

## 修订历史

| 版本 | 日期 | 修订内容 |
|:---|:---|:---|
| V2.0 | 2026-07-09 | 修复路由参数命名（:code→:recordcode）；重构资产列表页（多字段搜索+序号+复选框+批量删除+操作列）；新增组织管理模块（员工管理/部门管理/通讯录）；新增公共组件（DepartmentTree/SearchBar/BatchActionBar）；补充员工状态颜色 |
| V1.0 | 2026-07-09 | 初始版本 |
