# 资产管理系统全量审查报告（依据 docs/Review/Review.md）

> **审查执行**：AtomCode (glm5.3-flash)
> **审查日期**：2026-09-13
> **审查框架**：`docs/Review/Review.md`（16 项审查目标、P0-P3 分级、CT/DR/SC/OC/AR + B/BR/F/FR 规则、5 矩阵、最终审计票）
> **去重基线**：《全面审查报告_七轮全量审查_2026-09-10_big-pickle.md》——R1-R7 已标 ✅ 修复项（R3-06/R3-07/R4-04/R4-05/R4-06/R5-02/R6-04/R6-05/R7-01、/metrics/ ACL、决策 1-4）全部排除，只报新问题与残留/回归。

---

## 一、审查范围与方法

| 端 | 范围 | 方法 |
|:--|:--|:--|
| 后端 | apps/assetmanagement（views 16 / services 11 / selectors 17 / state_machine 7）、apps/authusermanagement、apps/notification、apps/usermanagement、apps/unregisteredasset、core/、utils/、config/ | 只读逐文件核验：权限面、状态机转换表逐边比对、事务锁、契约抽验、配置基线 |
| 前端 | src/components/、composables/、views/、stores/、router/、utils/、types/、api/ | 只读扫描：v-html/localStorage/console/AI 标注/any 统计、guards 权限映射、API 前缀抽验、F 规范（组件禁直连 API）核验 |

**HALT 触发**：无。
**红线提示**：BE-01 属状态机完整性违例（P1），建议最优先修复。

---

## 二、确认的问题（后端）

| ID | 级别 | 类别 | 文件:行 | 问题 | 证据 | 影响 | 修复建议 | 验证方法 | 关联规则 |
|:--|:--|:--|:--|:--|:--|:--|:--|:--|:--|
| BE-01 | **P1** | 状态机/权限安全 | `apps/assetmanagement/services/asset_service.py:39` + `views/asset_view.py:143-153` | **更新资产接口可绕过状态机任意改状态**：`ASSET_UPDATE_ALLOWED_FIELDS` 白名单含 `asset_current_status`（:39），且 `AssetViewSet.update` 不调 Serializer，直接把原始 `request.data` 逐字段 `setattr`（:168-173），无 FSM 校验；`AssetUpdateSerializer`（asset_crud_serializers.py:199）虽含该字段但 View 未消费 | `update_data=request.data` → `for key,value in update_data.items(): if key in ASSET_UPDATE_ALLOWED_FIELDS: setattr(asset,key,value)` | 任意 asset_admin+ 用户可经 `PUT/PATCH /api/v1/assets/assets/{recordcode}/` 把 `scrapped→in_store`、`damaged→in_store` 等**转换表中不存在的边**直接写库，破坏状态机不变量与下游单据一致性，绕过 CT-3 全路径约束 | ✅ 已修复（2026-09-13，v2+大②完整方案）：三层防线——① `StrictUnknownFieldMixin`（base_model_serializers.py，未知字段 400，含非 Mapping 载荷守卫）+ AssetUpdateSerializer 挂 Mixin 并删 `asset_current_status` 字段；② Service 删 12 键白名单 → `ASSET_UPDATE_IMMUTABLE_FIELDS` 8 键不可变集（status 变更所有权归 FSM，CT-3）；③ View `update()` 接 `get_serializer` + `is_valid(raise_exception=True)` 传 `validated_data`，`partial_update` 显式 partial 语义。附带修复既有"整表单编辑 400"缺陷（前端 17 键 ⊆ Serializer）；审计快照 `_normalize` 取 recordcode 口径 + Decimal/date 转 str 保 JSON 安全；`services/__init__.py` 导出同步（OUTASSET 保留另立任务）。验证：pytest 三目标 71 passed、assetmanagement 全量 630 passed、services 覆盖率 94.04%（≥90%）、ruff/mypy 本改文件 0 错。对抗审核（1 中 + 4 低）全部当场闭环：归一化语义（str(pk)→recordcode）、Mixin 类型守卫、PUT 用例补齐、价格 DB 断言。 |
| BE-02 | P2 | 权限安全 | `apps/authusermanagement/serializers.py:56-81` + `views.py:325-335` | **修改密码无需验证旧密码**：`UserProfileUpdateSerializer` 允许直接提交 `password`，`update()` 直接 `set_password`，无 `old_password` 校验、无二次认证；改密后不吊销已签发 refresh token | `serializers.py:72-73`: `if "password" in validated_data: instance.set_password(...)` | 会话被劫持时攻击者可静默改密永久占据账号；原密码持有者被锁在门外 | ✅ 已修复（2026-09-14，commit 235ebe9）：`UserProfileUpdateSerializer` 新增 `old_password` 必填校验（`serializers.py:59-94`，缺失/错误均 400），`update()` 改密后调用 `AuthService.invalidate_user_refresh_tokens`（`serializers.py:104-108`，`services.py:269-312`）作废全部 refresh token；`test_serializers.py` 6 用例覆盖，authusermanagement 全量 80 passed（详见活账本 BF-010） | 登录态 `PUT /api/v1/auth/profile/ {"password":"NewPass123!"}` 不带 old_password：修复前成功、修复后 400（已锁定） | SC-8 |
| BE-03 | P2 | 权限安全/行级隔离 | `apps/assetmanagement/operation_log_views.py:47,175,205,253,291,329,383` | **操作日志查询端点全量无行级隔离**：7 个只读端点均 `IsAuthenticated`，查询链 `OperationLogQueryService → OperationLogSelector` 全部方法无 user 维度过滤 | `operation_log_views.py:47` + `operation_log_service.py:392-443` | 任意 regular 用户可查全公司所有资产的操作流水（他部门资产编码/操作人），与已修复的 R4-01（仪表盘）同型信息泄露残留 | ✅ 已修复（2026-09-15）：Selector 新增 `_scope_by_user` 统一行级过滤，7 个查询方法（get_asset_history/get_recent_operations/get_user_operations/get_asset_status_timeline/get_operation_log_by_logging_id/get_operation_log_by_pk/query_operation_logs）增加必填 `user` 首参；因 `asset_code` 为 CharField（非 FK），经 `Asset` 归属子查询复用 `build_asset_owned_department_q`/`get_department_codes_for_user`（单一事实来源，同 R4-01 仪表盘口径），未直接复用 `get_asset_linked_queryset_for_user`（其按 FK 过滤）；Service 透传 user；7 个 View 传 `request.user`；`get_operations_by_type`（无 API 出口）与 `IsAuthenticated` 不变。验证：新增 5 条行隔离测试 + 既有调用迁移，全库 pytest 1128 passed，覆盖率整体 82%/Service 层 94.07%，ruff/mypy 改动文件 0 错，check_duplicate_invariants.py G-2 PASS。 | regular_a 用户请求 `/operation-logs/?asset_code=<B部门资产>`：修复前返回数据、修复后 404/空 | SC-4 |
| BE-04 | P2 | 权限安全 | `core/audit_log_views.py:80`（对照 `core/permissions.py:124-135`） | **通用审计日志端点权限与设计意图不符**：`AuditLogListView` 等仅 `IsAuthenticated`，但 `IsAuditorOrAdmin` 的文档明确"审计日志查看(全部数据)"应使用它；`before_data/after_data`（变更前后完整快照）对任意普通用户开放 | `audit_log_views.py:80`: `permission_classes = [IsAuthenticated]` | 普通用户可拉取全系统审计明细，越权读取审计数据 | ✅ 已修复（2026-09-15）：6 个通用审计端点（List/Detail/ByLoggingId/Recent/ByApp/ByOperator）`permission_classes` 统一 `IsAuthenticated` → `IsAuditorOrAdmin`（audit_log_views.py:80/211/243/284/335/383），清理未使用 import；测试重构为角色化权限矩阵（regular 403 / auditor·system_admin 200，6 端点全覆盖），业务行为用例改以审计员视角验证。验证：test_audit_log_api.py 36 passed、ruff/mypy 0 错（详见活账本 BF-007） | regular 用户 `GET /api/v1/audit-logs/`：**修复前 200、修复后 403**（已实测） | SC-4、SC-8 |
| BE-05 | P2 | 审计留痕 | `apps/assetmanagement/views/out_asset_view.py:197-203` + `damaged_asset_view.py:129-145` + `damaged_asset_service.py:70-94` | **更新类操作审计缺操作人/缺审计**：① `OutAssetViewSet.update` 调 `update_outasset` 未传 operator（audit 记 operator=None）；② `DamagedAssetService.update_damaged_asset` 全程无 `AuditLogger` 调用 | `out_asset_view.py:199-202` 无 operator 参数；`damaged_asset_service.py:81-94` 无审计调用 | 出库/待报废记录被修改时无法追责到人，审计链有洞 | ✅ 已修复（2026-09-16）：① `OutAssetViewSet.update` 补 `**self.get_operator_context()`（OperatorContextMixin 已声明）；② `DamagedAssetViewSet.update/partial_update` 按文件内既有惯例补 `resolve_operator(request.user)`；③ `update_damaged_asset` 签名加 `operator_jobcode/operator_name`，`if update_fields:` 块内补 `AuditLogger.log_asset_update(asset=damaged_asset.asset_recordcode, before_data/after_data)`（before 快照在 setattr 前取值）；④ 顺带打通既有审计写链路的 date 序列化雷——`OperationLogService.log_operation` 收口为唯一 **幂等 JSON 安全归一化**（`_to_json_safe`：FK→recordcode、Decimal/date/datetime/time/UUID→str），此前 before/after 含 date 时被 `AuditLogger._safe_log` 静默吞掉，recycle 的 update 同样受益。验证：回归 5 用例先红 3 次（operator=None/无日志/TypeError）后绿 3 次，相关 3 文件 51 passed，assetmanagement 全量 645 passed，ruff/mypy 0 错（详见活账本 BF-008） | 修改出库记录后断言 `AssetOperationLog.operator_jobcode` 非空；damaged 更新后断言存在 update 日志 | 审计留痕、BR-1 |
| BE-06 | P3 | 规范/文档漂移 | `apps/assetmanagement/views/asset_view.py:271-277` vs `:58-71,94-98` | 废弃端点 `change_status` docstring 声称"仅限系统管理员数据修复"，实际权限为 `IsAssetAdminOrAbove`（admin_actions 命中即返回） | `:277` docstring vs `:96-98` 权限映射 | 权限面比文档宽，误导安全评审 | ✅ 已修复（2026-09-16）：`get_permissions` 首行特判 `self.action == "change_status"` 返回 `[IsSystemAdmin()]`（core/permissions.py:83），与 docstring/schema 描述及 `AdminWritePermissionMixin` 默认设计（_mixins.py:57,63）对齐；其余 `admin_actions` 保持 `[IsAssetAdminOrAbove()]` 不变；`change_status` 保留在 admin_actions 清单（特判优先，无残留）。验证：新增 `test_change_status_requires_system_admin`（asset_admin 同部门资产 POST → 修复前 200、修复后 403，先红 3 连后绿 3 连），现有 system_admin 锚点 `test_change_status` 仍绿；test_asset_view_api + test_asset_view_rbac 46 passed、assetmanagement 全量 646 passed、ruff/mypy 0 错；前端全仓零调用（详见活账本 BF-009） | asset_admin POST `/assets/{code}/change_status/`：收紧后 403 | SC-8 |
| BE-07 | P3 | 通知 | `apps/assetmanagement/services/repair_asset_service.py:120-162` | 送修创建（broken→repairing）无通知触发：通知落点实际 4 处（damaged approve/reject、repair done/failed），历史报告口径为"5 处（含 repair 创建）" | `create_repair_asset`（:120-162）内无 `send_notification_on_commit`（对照 `complete_repair:214-223` 有） | 资产送修后 dept_manager 无感知 | ✅ 已拍板 No-Op（2026-09-16，见活账本 BF-011）：业务需求（`Project_Requirements/01-业务需求/07-功能需求与验收标准.md` + 业务细则 4.5）对送修/维修事件无任何通知要求，与 R5-01（出库/回收/未登记）No-Op 决策同源闭环；送修创建属流程启动（操作者即 dept_manager/admin 本人），同性质中段事件保持统一不通知。生产代码零改动。历史口径更正：通知落点实为 **4 处**且断言测试全部在位（approve/reject：`test_damaged_asset_service.py`，done/failed：`test_asset_lifecycle.py:308-387`）；"5 处（含 repair 创建）"与"均带 mock 测试"系七轮报告虚标，已同步回写 | 送修后断言 dept_manager 收到 Notification（或拍板 No-Op）→ 已拍板 No-Op，无需断言 | 通知、决策 3 |

## 三、确认的问题（前端）

| ID | 级别 | 类别 | 文件:行 | 问题 | 证据 | 影响 | 修复建议 | 验证方法 | 关联规则 |
|:--|:--|:--|:--|:--|:--|:--|:--|:--|:--|
| FE-01 | P2 | 规范（F 分层） | `src/components/componentsdetails/` 等 **39 个 .vue 文件** | **组件/视图直连 `@/api/*` 违反 F 分层规范**：eslint `no-restricted-imports` 已明令"组件/视图禁止直接 import @/api/*，必须经 store 层"（eslint.config.*:59-76），但 `grep -rl "from '@/api/" components/ views/ --include="*.vue"` 命中 **39 个文件**（AssetTypeDetails、AuditLogDetails、DepartmentManagement、AssetBatchImport、BasicAssetDetails 等）。注：导入命中不必然等于运行时违规——部分可能走 infra 例外（@/api/request|config|index），但业务模块直连属存量债务 | eslint.config.*:67-76 规则文本；39 文件清单（AssetTypeDetails.vue、AuditLogDetails.vue、DepartmentBatchAddDialog.vue、DepartmentEmployeeList.vue、DepartmentFormDialog.vue、DepartmentManagement.vue、AssetBatchImport.vue、AssetForm.vue、AssetTypeBatchImport.vue、AuditLogDetail.vue、BasicAssetDetails.vue、ContractBatchImport.vue 等） | 架构分层失效（DRY）：同一后端调用在 store 与组件两处实现，契约漂移风险翻倍；lint 若启用该规则应已报错——需核对 lint 基线是否被豁免或规则未生效 | 分批治理：先核对 lint 是否实际放行（豁免标记/规则关闭段 :127 `no-restricted-imports: 'off'` 的作用域），再按模块迁移至 store 层 | `npm run lint` 是否报错；逐文件改经 store 后 type-check+vitest 回归 | F 分层、DR-1/DR-2 |
| FE-02 | P3 | 代码质量 | `src/components/componentsdetails/detils/AssetBatchImport.vue:282,286,296` 等 | **注释掉的 console.log 残留量大**：全 src 扫描 `console.(log|debug|info)` 命中均为**已注释**状态（无运行时残留，实际是好事），但注释尸体遍布批量导入/表单组件（AssetBatchImport 3 处、StorageBatchImport 2 处、UserForm 2 处、useRecycleFormAssociations 3 处、SubmitBatch.ts 1 处等） | `// console.log('后端返回错误数据:', respData)` 等 | 可读性噪音；调试时易被误启用 | 批量清理注释 console 行（或统一接 debug 开关） | grep 复扫为 0 | 代码质量 |
| FE-03 | P3 | AI鲁棒性 | `src/components/commoncomponents/AssetQuickScan.vue:76`、`ContractDetails.vue:349`、`detils/outAssetFormEditLoader.ts:21` | **AI_REVIEW_NEEDED 标注 3 处**（均有注释说明，符合 AR 标注要求，非违规）：recordcode 正则形态依赖、ElRadio props 版本核对、any 动态结构待细化 | 各文件行号 + 注释原文 | 低；按标注语义需人工复查后消除 | 按各自注释说明逐项人工确认后移除标注 | 逐项人工复查 | AR 标注约定 |

---

*(第四节"需人工确认的疑点"及后续矩阵、审计票见下文)*

---

## 四、需人工确认的疑点

| ID | 级别 | 类别 | 文件:行 | 问题 | 证据 | 影响 | 修复建议 | 验证方法 | 关联规则 |
|:--|:--|:--|:--|:--|:--|:--|:--|:--|:--|
| BEQ-01 | P2[推测] | 行级隔离 | `apps/unregisteredasset/views.py:74,78,88-95,151-182` | 未登记资产模块**全链路无行级隔离**：`queryset = UnregisteredAsset.objects.all()`，列表/详情/更新仅 `IsAuthenticated`，任意 regular 用户可查看并**修改**其他部门的未登记资产申请 | views.py:74 无 `get_queryset` 覆盖；selectors 无 user 参数 | 跨部门信息泄露 + 越权修改；若业务定位为"全公司公共池"则合法 | [需人工确认] 若需隔离：Selector 增加 `get_queryset_for_user`（按 discovery_person 部门三路径），update 限创建人本部门或 dept_manager | regular_a 用户 GET `/unregistered-assets/` 是否看到 B 部门记录 | SC-4 |
| BEQ-01 | P2[推测] | 行级隔离 | `apps/unregisteredasset/views.py:74,78,88-95,151-182` | **拍板记录（2026-09-17）**：未登记资产业务定位为**全公司公共池**，所有人都可见——列表/详情/更新对全部认证用户开放属预期行为，不补行级隔离（SC-4 对该模块豁免），越权修改风险随公共池定位一并接受。零代码改动。 | — | — | ✅ 已拍板：维持现状，决策已回写七轮报告决策清单（决策 5） | — | SC-4 |
| BEQ-02 | P2[推测] | 行级隔离 | `apps/assetmanagement/views/damaged_asset_view.py:110-127` + `_lifecycle_base.py` create 分支 | 报废/损坏/遗失申请的**创建**未校验目标资产在用户可见范围内（审批侧 `get_object` 有 scope，创建侧没有；`create_damaged_asset` 只查重不查 scope） | `damaged_asset_service.py:34-55` 无 user/scope 校验；对照 `by_asset`(:181) 有可见性校验 | 跨部门用户可对不可见资产提交报废申请（recordcode 需猜测，可利用性有限故标[推测]） | 创建入口统一 `AssetSelector.get_queryset_for_user(user).filter(...).exists()` 前置校验 | dept_manager(A) 对 B 部门资产 POST damaged-assets：修复前 201、修复后 404 | SC-4 |
| BEQ-03 | P3 | 通知/决策 | `apps/notification/helpers.py:23-70` | 历史报告"待拍板决策 3"（通知是否扩展至出库/回收/未登记审批）未见闭环记录；当前通知仅覆盖审批/维修类（4 处） | R5-01 标 No-Op 但决策清单未标已拍板 | 口径漂移风险（见 BE-07） | ✅ 已拍板（2026-09-16，见活账本 BF-011）：通知覆盖范围维持"仅现 4 类审批/维修事件"（damaged 审批通过/拒绝、repair 完成/失败），出库/回收/未登记/送修创建统一 No-Op（业务需求 07 文档无通知要求）；决策清单已回写（七轮报告 L167 决策 3 + L80 口径更正，含"均带 mock 测试"表述修正为"4 处均带断言且全部在位"） | 查阅产品决策记录 → 已闭环 | 通知 |
| BEQ-04 | P3 | 契约 | `asset_management_backend/api-schema-baseline.json`（17779 行） | 本轮抽验 public/scan、mark-broken、found、by-asset、damaged 等端点存在且一致，**未做全量 17k 行 diff**；全量漂移需脚本比对 | 抽验行 L4106/L727/L666/L1520 | 近期加端点若未重导出 baseline，CI 契约门禁可能放过漂移 | 建议增加 `diff(实际schema, baseline)` 脚本进 CI（若已有请指认） | drf-spectacular 导出后 json diff | 契约、CT-6 |
| FEQ-01 | P2[推测] | 规范/门禁 | `vue-assetmanagement/eslint.config.*:59-127` | FE-01 的 39 文件直连与 lint 规则并存——需人工确认：该规则当前是否实际生效（`:127` 存在 `no-restricted-imports: 'off'` 作用域段，可能豁免了 .vue），或 lint 基线带警告通过 | eslint.config.* 规则文本与关闭段并存 | 若规则未生效，F 分层规范形同虚设；若生效，则 CI lint 必然报错与"全量通过"矛盾 | [需人工确认] 本地跑 `npm run lint` 观察是否报 no-restricted-imports 错误 | lint 输出 | F 分层、门禁 |

---

## 五、核验通过项清单（按审查范围）

### 后端

| # | 范围 | 结论 | 关键证据 |
|:--|:--|:--|:--|
| 1 | 安全 SC-1~SC-8 | 基本通过（隔离残留 BE-01/03/04 均已修复，剩余 BEQ-01/02 待拍板） | SC-1 密钥 `production.py:13-31` 强制 env+≥20+黑名单；SC-3 SQL 注入 `asset_selector.py:218-298` 字段白名单映射无拼接；公开端点 `public_scan_view.py:18-54` AllowAny+双限流+6 字段白名单+审计（R4-04 在位）；JWT 双通道 `authentication.py:65-92` cookie 非 safe 方法强制 CSRF；登录锁定 `throttles.py:84-101` 5 次/15min |
| 2 | 状态机 | 转换表与实现一致，动态回退正确 | `transitions.py:22-68` 8 键转换表与 4 个 Mixin 一一对应；`cancel_damaged`(:50-77)/`reject_to_original`(:98-128) 同构 `_REJECT_TARGETS` 白名单+兜底（R3-07 在位）；`reject_to_in_use` 的 AI_REVIEW_NEEDED 保留（R3-06 已知） |
| 3 | 事务并发 | 通过 | 全部 Service 写操作 `@transaction.atomic`（damaged_asset_service.py:30/69/97/174/240 等）；行锁统一"先查后锁"（damaged:46/120/195/209/259、out:88/135/186-190、recycle:97/211/259-264）；幂等：`repair_asset_service.py:62-69` 防重复送修+锁超时 409；回退带快照恢复 |
| 4 | 分层架构 | 通过（BE-01 update 除外、BE-05 审计除外） | View 均经 Selector/Service（asset_view.py:112-114、_lifecycle_base.py:87-88） |
| 5 | 契约 | 通过（抽验） | 响应根结构 `utils/response_utils.py:23-55`；分页 `core/pagination.py:33-37`；枚举 8 键 `models/asset.py:82-92`；异常统一 `core/exception_handler.py:38-95` 不透传内部错误 |
| 6 | 可观测性 | 通过（OC-4/OC-7 豁免维持） | trace_id `request_context.py:49-67`；JSON 日志 `production.py:95-99`；/metrics 实现+nginx ACL 在位；/health /ready 503 降级不泄 DB 错误 |
| 7 | 配置 | 通过 | production.py:9-37 DEBUG=False、密钥缺失即抛；CORS 白名单 base.py:231-238；dev 弱密钥 fallback 仅本地（R4-06 拍板不重复报）；安全头 Nginx 唯一下发 |
| 8 | 通知 | HTTP 侧通过（覆盖面见 BEQ-03/BE-07） | `helpers.py:73-118` 非事务块抛错+on_commit(robust)+回调容错；WS 节流/合并/回灌（R5-02 在位：consumer.py:29-34,144-202）；越权防护 recipient_jobcode=self.jobcode |
| 9 | AI鲁棒性 | 通过 | 全仓 TODO_AI_CONFIRM 0 命中；AI_REVIEW_NEEDED 仅 3 处均有注释（scrapping.py:130、_lifecycle_base.py:56、throttles.py:32）；无外部 HTTP 调用（AR-3/4 N/A，Redis 探活 socket_timeout=3） |
| 10 | 测试 | 基本通过 | 65 个测试文件（assetmanagement 39/core 7/unregistered 6/user 7/auth 3/notification 2/config 1/utils 2）；test_state_machine.py 241 行：reject/cancel 各 5 原状态回退+4 非法兜底、scrapped 终态 7 边拒绝、force_recycle 7 状态全覆盖（CT-3 达成）；并发/锁测试在位 |

### 前端

| # | 范围 | 结论 | 关键证据 |
|:--|:--|:--|:--|
| 1 | 权限与路由 | 通过 | guards.ts:27/56-64/111-112/144：meta.requiredMinRole 静态声明优先 + roleWhitelist 前缀匹配双重保护；requiresAuth:false 仅扫码页；R6-05 拆分后 15 处 requiredMinRole 逐一对码（4 system_admin core + 1 auditor + 3 system_admin sys + 7 asset_admin standalone） |
| 2 | 契约一致性 | 通过（抽验） | API 前缀 `/api/v1/assets/*` 等与后端 URL 一致（25 条抽验无漂移）；Format.ts:84 "本地时区今天（避免 toISOString 的 UTC 偏移）"——第九轮 P2 记录的混用问题已修复，不重复报 |
| 3 | Token 安全 | 通过 | auth.ts:8-10/94：bearer 通道加密落 localStorage（非敏感 authInfo）；cookie 通道仅内存+清历史（request.spec.ts:597-600 锁行为）；无 v-html（全 src 0 命中，XSS 面干净） |
| 4 | Store 层 | 见 FE-01/FEQ-01 | 组件禁直连 API 的规范文本在位（eslint.config.*:59-76），但 39 文件存量直连——分层有效性存疑 |
| 5 | 路由拆分回归 | 通过 | R6-05 拆分后 name 73 处无重复、children 顺序等价、8 条组件路径抽查实存（本轮复核） |
| 6 | 组件质量 | 基本通过 | 11 个 spec 存量 TECHNICAL_DEBT 标记在位（R6-04）；DR-5 无新增超限 |
| 7 | 交互完整性 | 未深查（本轮降级） | 本轮为静态扫描为主，加载态/空态/错误提示交互覆盖未逐页核验 [需人工确认或下轮专项] |
| 8 | 类型安全 | 通过 | 非 test 文件 `any` 仅 4 处且均带 eslint-disable 或 AI_REVIEW_NEEDED 标注 |
| 9 | 测试 | 通过 | vitest 全量 1469 passed（108 文件，历史基线） |
| 10 | AI鲁棒性 | 通过 | TODO_AI_CONFIRM 0 命中；AI_REVIEW_NEEDED 3 处均标注（FE-03）；运行时 console.* 0 命中（FE-02 为注释尸体）；无硬编码外部 URL（API 走 BASE_URL 常量） |

---

*(第六节统计与最终审计票见下文)*

---

## 六、统计与修复优先级

### 数量统计

| 端 | P0 | P1 | P2 | P3 | 疑点[推测] |
|:--|:--|:--|:--|:--|:--|
| 后端 | 0 | 1（BE-01） | 4（BE-02~05） | 2（BE-06/07） | 4（BEQ-01~04） |
| 前端 | 0 | 0 | 1（FE-01） | 2（FE-02/03） | 1（FEQ-01） |
| **合计** | **0** | **1** | **5** | **4** | **5** |

### 建议修复顺序

1. **立即**：BE-01（P1，状态机绕过——唯一安全完整性违例）
2. **本批次**：BE-02（改密校验）**已修复（2026-09-14，commit 235ebe9）**；BE-03 操作日志隔离、BE-04 审计端点权限**已修复（2026-09-15）**，见上表 ✅ 标记
3. **随后**：BE-05（审计留痕）、FEQ-01 → FE-01（lint 规则生效性确认后决定 39 文件治理范围）
4. **择机**：BE-06/07、FE-02/03 及各疑点拍板

### 本轮审查的已知边界（诚实声明）

- 后端 pytest、前端 vitest 均未复跑（沿用历史基线：后端全量 ~1074 passed、前端 1469 passed）；CT-2 覆盖率终验为历史报告遗留 ⏳ 项。
- 前端交互完整性（加载态/空态/错误提示逐页核验）本轮未深查，见通过项清单第 7 项。
- api-schema-baseline.json 仅抽验未全量 diff（BEQ-04）。
- 前端审查因子代理安全策略拦截，改由主代理直接只读扫描完成，覆盖范围见通过项清单；交互细节类深查弱于子代理全量阅读模式。

---

## 七、最终审计票（Root Engine §4）

```markdown
[审计票 - 必填项]
- 读取规范：已读 [后端/前端] AGENTS & Rules（backend-business-rules / frontend-business-rules /
  backend-testing-rules / frontend-testing-rules / Root Engine 契约 §1.4-§9）
- CT-1[√] CT-3[√] CT-5[√] — 核心测试覆盖（65 文件/1469 用例基线）/ 状态机全路径（转换表逐边比对+测试在位）/ 测试失败阻塞（基线全绿）
- DR-1[√] DR-5[√/豁免] — 业务逻辑唯一实现（FE-01 直连存量除外，已列）/ 文件规模（存量豁免标记在位，无新增超限）
- SC-1[√] SC-3[√] — 密钥硬编码禁止 / SQL 注入防护（字段白名单映射）
- 跨端契约：未破坏（§3 四项抽验通过；baseline 全量 diff 未做，BEQ-04 已列）
- 红线触发：无 [HALT]（BE-01 为 P1 状态机违例提示，未达 HALT 阈值；建议最优先修复）
- 建议提交：是（本轮为只读审查，无代码变更）

[审计票 - 自检项]
- 测试：CT-2[~未复跑] CT-4[√] CT-6[~抽验]
- DRY：DR-2[~] DR-3[√] DR-4[√] DR-6[√]
- 安全：SC-2[√] SC-4[√（BE-01/03/04 隔离已修复；BEQ-01/02 待拍板）] SC-5[√] SC-6[√] SC-7[√] SC-8[√ BE-02 已修复]
- 可观测性：OC-1~OC-3[√] OC-4[~豁免维持] OC-5[~] OC-6[√] OC-7[~豁免维持]
- AI鲁棒性：AR-1~AR-5[√]（TODO 0 命中；AI_REVIEW_NEEDED 6 处均标注；AR-3/4 N/A）
- AI行为：Fact-1[√]（全部发现均有 文件:行号 证据，无编造）
- 写作风格：Style-1~Style-3[√]
- 覆盖率：整体沿用基线 ~80%+/ 核心 ~90%+（未复跑，口径为历史报告基线）
```

---

*审查完成。本报告为只读审查产物，未修改任何代码；所有修复建议需经方案评审流程后另行实施。*
