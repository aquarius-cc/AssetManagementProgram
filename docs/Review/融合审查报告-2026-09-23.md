# 融合审查报告（2026-09-23）

> 融合对象：`docs/Review/mimo-v2.6-flash-free-2026-09-23.md`（Agent：mimo-v2.6-flash-free）+ `docs/Review/AtomCode-2026-09-23.md`（Agent：AtomCode / glm5.3-flash）  
> 融合方式：双报告全文对照 → 重复项去重、分歧项二次取证 → 独有项逐条核实后合并  
> 二次取证日期：2026-09-23 | 协议：`docs/Review/Review.md` + Root Engine v3.5.1  
> 事实基线：全部结论附文件:行号；核实推翻项见 §二；推测标注 `[推测]`，存疑标注「需人工确认」  
> 本文件为本轮唯一新建审查产出，不修改任何生产代码。

---

## 一、双报告执行摘要对照

| 维度 | mimo-v2.6-flash-free | AtomCode | 融合采信 |
|:-----|:---------------------|:---------|:---------|
| 审查方式 | 7 轮分审 + 4 路并行取证 + 门禁全量实测 | 6 轮只读调研 + 守卫实测（套件受 300s 上限） | 两者互补：静态/权限深度取 mimo，AC/细则对照取 AtomCode |
| 后端测试 | **1274 passed，覆盖 84.40%**（Service 96.97%） | asset 套件超时未完；非 asset 四 app **310 passed** | **采信 mimo 全量数字**；AtomCode 环境限制如实记录 |
| 前端测试 | **1812 passed，覆盖 93.17%，Store 97.89%** | 未重跑 | **采信 mimo** |
| 三护栏 | PASS（重复/函数长度/前端规模） | PASS（同一组脚本） | 一致 PASS |
| 静态门禁 | ruff 7 / format 102 文件 / mypy 24 / C90×2 **全红** | 未系统重跑（仅 mypy 单文件假阴性分析） | **采信 mimo 实测** |
| 变异测试 | mutmut/stryker **[PENDING]** | 未执行 | 一致 [PENDING] |
| P0 | 0 | 0 | **0** |
| P1 | 4（权限×2、CI 红灯、状态机协议） | 3（未登记资产 approver/discovery/行级） | **合并 7 项**（去重：无重叠）+ 本批新增 F-P1-8（lifecycle batch_create，用户拍板 A 并入） |
| `[HALT]` | H-1：`lost→repairing` 三方冲突 | 无 HALT；3×P1 按 ID-2 上报 | **保留 H-1**；AtomCode 3×P1 升入 P1 表 |
| 建议提交 | 否 | 否 | **否** |

---

## 二、分歧项与核实结论（Fact-1）

融合前对双方不一致或 AtomCode 独有高优先级条目做了二次取证：

| # | 分歧/独有项 | AtomCode 原述 | mimo 原述 | 二次取证结果 | 融合采信 |
|:--|:------------|:--------------|:----------|:-------------|:---------|
| V-1 | 未登记资产 approver | P1-1 传入值被采纳 | 未覆盖 | `unregisteredasset/views.py:233` `approver = serializer.validated_data.get("approver") or operator_jobcode`；规则 `backend-business-rules.md:206` 要求强制覆盖 | **P1 成立** |
| V-2 | discovery_person 冒名 | P1-2 无角色守卫 | 未覆盖 | `views.py:138` 优先取 `request.data`；规则 `:205` 仅 system_admin 可代录 | **P1 成立** |
| V-3 | 未登记写路径行级 | P1-3 `get_by_code` 无 user | 未覆盖 | `selectors.py:67` 签名无 user；调用 `views.py:164/194/224`；规则 `:198-199` 要求部门过滤 | **P1 成立** |
| V-4 | 出库 409 | P2-1 未见 409 分支 | 未覆盖 | `out_asset_service.py` 仅 `select_for_update`（:152 等），无 `ResourceConflictError`；**但** `repair_asset_service.py:48-58` 已有 AC-65→409，`core/exceptions.py:106` 定义存在，`test_asset_lifecycle.py:251` 有 ASSET_LOCKED 409 锚 | **P2 成立（出库路径缺口）**；基础设施已有 |
| V-5 | usage_type | P2-2 出库后未更新 | 未覆盖 | `usage_type` 仅现于 model/migration/admin，**services 目录零命中** | **P2 成立** |
| V-6 | 密码策略 AC-3 | P2-3 全未定位 | 未覆盖 | **部分推翻**：`serializers.py:91` 原密码校验存在；`test_dual_channel_auth.py:407/414/421` 覆盖无原密码 400 / 错误 400 / **改密后吊销 refresh**。复杂度策略（`AUTH_PASSWORD_VALIDATORS`）未检出 | **P2 降格改写**：仅「密码复杂度策略未定位」；原密码+吊销已具备 |
| V-7 | 扫码限流 20/min | P2-8 未定位显式配置 | 未覆盖 | `config/settings/base.py:205` `"anon": "20/minute"` **显式存在** | **推翻，从问题清单删除** |
| V-8 | dev SECRET_KEY | P3-5 硬编码默认 | SC-1 √（.gitignore） | `development.py:27` `config("SECRET_KEY", default=get_random_secret_key())` + `_INSECURE_KEYS` 守卫（:43）——**运行时随机，非硬编码** | **推翻/已过时，删除**（B1 已落地） |
| V-9 | console.error 计数 | ~80+ | **160** | 本轮重数非测试代码 = **160** | **采信 160** |
| V-10 | `lost→repairing` | 未提及；称 FSM 与 §3 一致 | H-1 [HALT] 三方冲突 | `transitions.py:54-57` LOST 无 REPAIRING 边；03 文档仅 broken→repairing；Review.md L110-111 仍列 | **采信 mimo H-1**；AtomCode「完全一致」仅相对 03/实现，未对照 Review.md 清单 |
| V-11 | CT-3 缺口 | P2-4 四条正向缺 FSM 直接锚 | 矩阵标 ⚠ 权限测试缺 | `test_state_machine.py` 见 mark_broken/lost 多源态显式类；recycle/found/approve 正向多在 service 测试 | **并列保留**：四条 FSM 级锚（AtomCode）+ 权限反向 403 锚（mimo N-9） |
| V-12 | AI_REVIEW_NEEDED | AR-2 ✅ 有意保留 4 处 | P2 未清零 7 处（后 4+前 3） | 后端 apps/core 实测 4 处；前端 3 处（前轮）；`TODO_AI_CONFIRM`=0 | **合并**：7 处待人工闭环确认，非违规本身 |
| V-13 | SC-5/6 上传 | N/A 无上传功能 | [~] 未取证 | AtomCode 已全仓确认无上传 | **采信 N/A** |
| V-14 | card-header 双写路径 | `common-forms.scss` | — | 实际路径 `src/assets/styles/common-forms.scss`（非 `src/styles/`） | **路径修正后成立** |

---

## 三、问题清单（融合去重后）

### P0 — 阻断级（0 件）

无。

---

### P1 — 严重级（7+1 件；F-P1-8 为本批修复时新增）

| ID | 来源 | 类别 | 文件/行号 | 问题描述 | 证据 | 影响 | 修复建议 | 验证方法 | 关联规则 |
|:---|:-----|:-----|:----------|:---------|:-----|:-----|:---------|:---------|:---------|
| F-P1-1 | mimo N-1 | 权限安全 | `assetmanagement/views/asset_view.py:59-72,95-101,383,396` | `mark_broken`/`mark_lost` 角色旁路：`@action(permission_classes=[IsAssetAdminOrAbove])` 被 `get_permissions()` 覆写后失效；两 action 不在 `admin_actions`，落回 `IsAuthenticated` | admin_actions 含 repair/found 但无 mark 两项（:59-72）；:101 非 admin 仅 IsAuthenticated；矩阵 regular 对损坏/遗失登记为 ❌（`backend-business-rules.md:141`）；row_isolation :173/:196 仅断言本部门 200 | 任意登录用户可对本部门资产标记损坏/遗失 | ~~两项加入 `admin_actions`；补 regular→403（先红后绿）~~ ✅ **已修复 2026-09-23**（`admin_actions` 追加 `mark_broken`/`mark_lost`；删除两处 `@action(permission_classes=...)` 死参数；`test_asset_view_api.py` 新增 `TestMarkBrokenLostRBAC` 6 用例先红后绿） | API 测试 403/200 双向 | B11, §4.2, CT-1/4 |
| F-P1-2 | mimo N-2 | 权限安全 | `views/storage_view.py:31-42,103` + `views/_mixins.py:52-64` | 仓库 `batch_create`/`batch_delete` 不在 Mixin 默认 `admin_actions`，落回 `IsAuthenticated`；contract/asset_type 已显式纳入 batch，Storage 未对齐 | `_mixins.py:52-59` 清单；`storage_view.py:103` 无类级覆盖；`contract_view.py:57-58`、`asset_type_view.py:49-50` 对照；矩阵仓库仅 system_admin（rules:144） | 任意认证用户批量建仓，污染全局主数据 | ~~Storage 补 `admin_actions` 两项；补 RBAC 测试~~ ✅ **已修复 2026-09-23**（Storage 类级全量 `admin_actions` 含 `batch_create`/`batch_delete` 继承 Mixin `IsSystemAdmin`；新建 `test_storage_view_rbac.py` `TestStorageBatchRBAC` 5 用例） | regular 403 / admin 201 | B11, §4.2, CT-1/4 |
| F-P1-8 | 本批（用户拍板 A 并入） | 权限安全 | `views/_lifecycle_base.py`（broken/lost/found 生命周期） | `batch_create` 不在 `get_permissions` 写路径清单：`admin_actions` 原仅 `create/update/partial_update/destroy/batch_delete`，`batch_create` 落回 `IsAuthenticated`；regular 可批量创建损坏/遗失/找回记录 | 矩阵 regular 对生命周期写为 ❌（`backend-business-rules.md:141`）；contract/asset_type/recycle 均已纳入 `batch_create` 对照 | 任意登录用户批量伪造生命周期记录，污染状态机入口 | ~~类属性 `admin_actions` 补 `batch_create`；`get_permissions` 改读该属性返回 `IsAssetAdminOrAbove`（**不**继承 Mixin 的 `IsSystemAdmin`，对齐矩阵 asset_admin+）；`test_create_row_isolation.py` 改写 11 用例（含 regular→403）~~ ✅ **已修复 2026-09-23**（同左方案落地；先红后绿 4红/4绿 → 11 passed） | lifecycle API 先红后绿 | B11, §4.2, CT-1/4 |
| F-P1-3 | mimo N-3 | 代码质量/CI | `ruff format --check`（102 文件）、`ruff check`（7）、`mypy --strict`（24） | CI 静态门禁三连红，与 `ci.yml` 同口径，推送将失败 | `ci.yml` 含上述步骤；本地实测 format 102、ruff 7（5 可 fix）、mypy 24 | 合并流水线不可用 | ~~format+fix 单独提交；mypy 分批；C90 拆分~~ ✅ **已修复 2026-09-24**（四提交：`9fecb97` ruff check 归零 → `210cfa7` format 107 文件全量 → `272f8e6` mypy --strict 208 文件 0 错 + types-channels） | 三命令 exit 0 | CT 门禁, DR-5 |
| F-P1-4 | mimo N-4 | 状态机/文档 | `Review.md:110-111` vs `transitions.py:44-57` vs `03-业务规则与状态机.md` | **[HALT] H-1**：协议要求 `broken/lost→repairing`，实现与 03 文档均仅 `broken→repairing`；LOST 仅→damaged/recycled_pending | transitions :44-47 BROKEN→REPAIRING；:54-57 LOST 无 REPAIRING | 契约源冲突，误改状态机或误判 CT-3 | ~~人工裁定 A 改 Review.md / B 走 §5.2 加边~~ ✅ **已裁定 A（方案2）2026-09-24**：Review.md L110-111 删 lost 仅 broken→repairing，显式补 `lost→found_and_return→recycled_pending→（若损坏）broken→repairing` 链 + 语义约定补「送修前置必须 broken」；transitions.py/03/02/09 零改动 | 裁定后改文档或补测试+前端 | §1.3, CT-3, Fact-1 |
| F-P1-5 | AtomCode P1-1 | 权限安全 | `apps/unregisteredasset/views.py:233` | **approver 可被请求方任意指定**，违反 4.5 细则第 3 条「强制=当前审批人，防代签」 | `approver = serializer.validated_data.get("approver") or operator_jobcode`；规则 `:206`；`services.py` 仅校验存在性 | 审计链失真，代签审批 | ~~服务端 `approver = operator_jobcode` 覆盖；补「传他人被忽略」回归~~ ✅ **已修复 2026-09-24**（BF-038：approve 无条件覆盖；`TestFp15ApproverOverride` DB 断言） | 传他人 approver，断言落库=操作人 | 4.5-3, B7 |
| F-P1-6 | AtomCode P1-2 | 权限安全 | `apps/unregisteredasset/views.py:138` | **discovery_person 可冒名**，违反 4.5 第 2 条「默认=本人；仅 system_admin 可代录」 | 优先取 `request.data`，无角色白名单；规则 `:205` | 非 admin 可代他人提交发现记录 | ~~≠本人时要求 system_admin 否则 403~~ ✅ **已修复 2026-09-24**（BF-038：恒 resolve_operator + 白名单 PermissionDenied 403 + Service 审计解耦；`TestFp16DiscoveryWhitelist`） | dept_manager 传他人工号→403 | 4.5-2, B14 |
| F-P1-7 | AtomCode P1-3 | 权限安全 | `views.py:164,194,224` + `selectors.py:67` | 单条 update/destroy/approve **未接行级 scope**：`get_by_code()` 不传 user，违反 4.5「越权一律 404」 | 签名无 user；读接口已隔离、写路径未隔离；规则 `:198-199` | 跨部门/跨本人改删审批未登记记录 | ~~新增 `get_by_code_for_user`（角色+部门+本人例外，失败 404）~~ ✅ **已修复 2026-09-24**（BF-038 **范围扩大**：读 `get_queryset` + 写三路径 scoped 404 + batch_delete B14 逐条 NOT_FOUND + 门禁按 4.5 矩阵对齐，dm update 收紧 403；`test_matrix_permissions.py` 30 格） | 他人记录操作→404 | 4.5 行级, B12 |

---

### P2 — 中等级（13 件）

| ID | 来源 | 类别 | 文件/行号 | 问题描述 | 证据 | 影响 | 修复建议 | 验证方法 | 关联规则 |
|:---|:-----|:-----|:----------|:---------|:-----|:-----|:---------|:---------|:---------|
| F-P2-1 | mimo N-5 | 可观测性/测试 | `config/urls.py:150-151` | `/health/`、`/ready/` 存在但**零测试** | glob 无 test*health*/*ready* | 端点回归无护栏 | ~~补 2 个冒烟测试~~ ✅ **已修复 2026-09-24**（`core/tests/test_health_ready.py` 4 用例：health 200/503、ready 200/503+H-3 不泄露；假 redis 注入；backend `cb7d409`） | 4 passed + ruff/format 绿 | OC-6, CT-1/4 |
| F-P2-2 | **双方** | 可观测性 | `vue-assetmanagement/src/**` 非测试 | `console.error` **160 处**无结构化/trace_id（旧报告 #27 仍开放） | 重数=160→174 含 warn；样本 `api/auth.ts:50+`、`api/request.ts:117` | 前端故障难关联 trace | ~~收敛 `utils/logger`；API 层优先~~ ✅ **已修复 2026-09-24**（A 方案两批：首批 `0ca00bc` 迁 api/stores/router 36 处；后续批 `9b49888` codemod 迁 68 文件 136 处，生产路径 `console.error/warn` 残留 0，仅 logger.ts 出口；vitest 132 文件/1812 全绿） | 生产路径计数 174→0 | OC-2 前端, DR-4 |
| F-P2-3 | mimo N-7 + AtomCode AR-2 | AI鲁棒性 | 后端 4 + 前端 3 | `AI_REVIEW_NEEDED` 共 7 处待人工闭环（`TODO_AI_CONFIRM`=0） | 后端：scrapping:135、_lifecycle_base:59、throttles:32、feishu app:41；前端：AssetQuickScan:76、outAssetFormEditLoader:21、ContractDetails:349 | 标注滞留失效 | ~~逐条确认后删除或转 TODO~~ ✅ **已闭环 2026-09-24**（3 删改普通注释+feishu 转 `TODO_AI_CONFIRM`+前端 3 删/改；`rg AI_REVIEW_NEEDED`=0 残留 TODO_AI_CONFIRM=1 受控；backend `3e0f165`+frontend `0ca00bc`） | rg 归零 + 受控清单 1 | AR-2 |
| F-P2-4 | mimo N-8 | 测试/变异 | mutmut / stryker | 变异测试 **[PENDING]**：Windows/WSL 无 mutmut；stryker 配置就绪未跑 | `mutation_results` 不存在；ci.yml:214-217 有 mutmut | CT-2 变异分支未本地验证 | WSL/CI 跑 mutmut；`npm run test:mutate` | 得分≥80 归档 | CT-2, T8 |
| （更新）F-P2-4 | — | 测试/变异 | 前端 7 store + 后端 12 Service 文件 | 🟡 **双基线已归档 2026-09-24**：① stryker 聚焦 120 mutants / 3m51s：**score 60.00**（killed 72 / survived 44 / no-cov 4）<80，exit 1；② mutmut 2.5.1 原生 FS 全量 **1385 mutants / 4h15m：score 65.63%**（killed 909 / survived 476，无 timeout/untested）；两端均 <80 未达关闭条件 | `docs/Review/mutation-baseline-2026-09-24.md`（前后端得分表+复跑命令）；stryker `reports/mutation/mutation.html` | 后端 survived 头部：asset_service 67 / recycle 66 / lifecycle 59 / contract 55；repair 率最低 53.09 | 按 survived 行号补 Service 失败/边界/回滚用例；前端四 CRUD store + ignoreStatic 分批 | 复跑双端 score≥80 | CT-2, T7/T16 |
| F-P2-5 | mimo N-9 | 测试缺口 | 无 *storage*rbac*；`test_asset_view_api.py:298-314` | Storage View 无 RBAC 测试；mark_broken/lost **仅 admin 正向** | mark 测试无 403 断言 | F-P1-1/2 无红绿锚 | ~~与 P1 同 PR 补测~~ ✅ **已修复 2026-09-23**（随 F-P1-1/2/8 同批：`TestMarkBrokenLostRBAC` + `test_storage_view_rbac.py` + lifecycle 403 锚，均先红后绿） | 新测试过 CI | CT-1/4, B11 |
| F-P2-6 | mimo N-10 | 交叉基线 | 旧报告 46 项 | ~~仍开放 #27→F-P2-2；#2/#3/#4/#8 **代码已修未标 ✅**~~ ✅ **已修复 2026-09-23**（授权后旧报告 #2/#3/#4/#8 补删除线+已修复标记与证据：`out_asset_service.py:115/:322`、`asset_selector.py:100`、`recycle_asset_service.py:171`、`security-scan.yml` pip-audit+cron；#27→F-P2-2 已随后续批关闭） | 同左 | 基线状态漂移已消除 | ~~授权后补旧报告 ✅~~ | `rg "已修复 2026-09-23"` 旧报告 4 行 | Fact-1 |
| F-P2-7 | mimo N-11 | 规范/复杂度 | `authusermanagement/services.py:220`；`init_production_data.py:186` | 生产 C90 **11>10**：`update_user`、`_create_role_permissions` | ruff --select C90；另 13 处在 .trae/clear_database | CI C90 失败 | ~~BR-4 模式拆分~~ ✅ **已修复 2026-09-24**（`3377c91`：抽 `_validate_unique_constraints` + `_resolve_perms_to_add`；`.trae` ruff exclude 12 处 + `clear_database.py` per-file C901；ci.yml 改 `--config lint.mccabe.max-complexity=10`） | C90 apps=0 | BR-4, DR-6 |
| F-P2-8 | AtomCode P2-1 | 功能完整性 | `out_asset_service.py`（出库路径） | AC-30/65 并发出库要求 409，出库 Service **无** `ResourceConflictError` 分支（修复路径已有） | out_asset 无 409 抛出；repair:48-58 有；文案「资产已被其他用户操作」零命中 | 状态码/文案与验收偏差（行锁已防损坏） | ✅ **已修复 2026-09-24**（D1 拍板补实现：`core/locks.py` 新增 `lock_row_or_409`+`ASSET_LOCKED`（DR-1 收敛 repair×3 + out×3）；complete/fail_repair 新增 409 为**行为增强**；batch 捕获 ResourceConflictError→fail_items） | out_asset 3 用例先红后绿 + repair lifecycle 回归 | AC-30/65 |
| F-P2-9 | AtomCode P2-2 | 功能完整性 | 出库 Service | AC-27 出库后 `usage_type` new→used **未实现**（services 零命中） | model 有字段 default=new | 统计口径漂移 | ✅ **已修复 2026-09-24**（D2 拍板「取消不回退」：`_apply_outasset_to_asset` 写 `usage_type=Asset.UsageType.USED` 入 update_fields；无 API/schema 变更） | fresh query 断言 used + AC-29 补「不回退」 | AC-27 |
| F-P2-10 | AtomCode P2-3（修正） | 功能完整性 | auth 模块 | 密码**复杂度策略**未定位；原密码校验与改密吊销 refresh **已具备** | serializers:91；test_dual_channel :407/414/421 | 弱密码可能仅靠前端 | ✅ **已闭环 2026-09-24（零代码）**（D3 核实证据链完整：`base.py:153-159` 5 组 validators 含 `ComplexPasswordValidator`；`serializers.py:57/:129` 均挂 `validators=[validate_password]`；refresh 吊销 `test_dual_channel_auth.py:426`；`register_user:65` 无 View 引用为已知旁路，不足为计） | 证据链闭环 | AC-3, SC |
| F-P2-11 | AtomCode P2-4 + mimo CT-3 | 测试 | `test_state_machine.py` | CT-3：① 四条正向缺 FSM 级直接锚（rp→damaged、lost→found、damaged→scrapped approve、in_use→recycle）；② mark 路径缺 403 反向锚（见 F-P2-5） | 正向多靠 service 测试；FSM 文件有 mark 多源态/scrapped 阻断/reject 参数化 | 严格 CT-3 口径不满 | ✅ **已修复 2026-09-24**（`test_state_machine.py` 补 4 条 FSM 正向锚：repair_failed→damaged、found_and_return→recycled_pending、approve→scrapped、recycle→recycled_pending；mark 403 由 F-P2-5 已覆盖不重复） | 4 新用例绿（58 passed） | CT-3/4 |
| F-P2-12 | AtomCode P2-5 | 测试 | 全仓 test | AC 编号锚计数被低估（原报「后端 **1**」；实测后端生产 **8 命中 / 3 文件**）、前端 **0** | grep `AC-\d` 计数 | 回归按验收定位能力被低估 | ✅ **已闭环 2026-09-24（零代码）**（计数更正：生产唯一锚 **AC-30/32/33/61/65**（repair×4 含 AC-61/65、recycle×3 含 AC-32/33、out_asset×1 为 F-P2-9 本轮补的 AC-30）；AC-27 落 test 锚；前端 0 维持） | `rg "AC-\d+"` 实测 8/3/5 锚 | T 测试规范 |
| F-P2-13 | AtomCode P2-6 | 契约/文档 | `public_scan_view.py` vs AC-60f/g | 扫码 6 字段白名单 **严于** AC 13 字段+脱敏（良性漂移） | rules 4.6 v1.12；实现更安全 | 文档口径漂移 | ✅ **已闭环 2026-09-24（零代码）**（AC-60f/g 文案对齐 §4.6：6 字段白名单、遮罩废弃改「直接不返回」；实现 `public_scan_view.py:39-46` 不动） | AC-60f/g 措辞对齐 4.6 | AC-60, 4.6 |

> **已核实删除（不入清单）**：AtomCode P2-8 扫码限流（`base.py:205` 已有 `anon: 20/minute`）；P3-5 dev SECRET_KEY（已改为运行时随机+不安全键黑名单）。

---

### P3 — 低级/优化（6 件）

| ID | 来源 | 类别 | 文件/行号 | 问题描述 | 修复建议 | 关联规则 |
|:---|:-----|:-----|:----------|:---------|:---------|:---------|
| F-P3-1 | mimo N-13 | 代码质量 | `scripts/loadtest/*`、`test_ws_consumer.py:8` | ruff 7 中 5 可 `--fix`；测试 F401 | ~~`ruff check --fix` + 手工删 F401~~ ✅ **已修复 2026-09-24**（`9fecb97`：loadtest import json 上移+I001；test_ws_consumer 删未用 asyncio） | 代码质量 |
| F-P3-2 | mimo N-14 | 可观测性 | `core/metrics.py`、`urls.py:152-153` | OC-4 Prometheus **已超额落地**，QPS&lt;10 维持 `[~]` | 保持；达阈后加告警基线 | OC-4 |
| F-P3-3 | AtomCode P3-1/2 | 规范 | `check_duplicate_invariants.py` | **G-4 无脚本实现**（仅 G-1/2/3/5 函数）；error_code schema 观察项维持 | 补 G-4 扫描或头部注明人工执行 | §1.8, G-4 |
| F-P3-4 | AtomCode P3-3 | 代码质量 | `src/assets/styles/common-forms.scss:35-36` 等 | form-container 内嵌 `.card-header` 与 `@mixin` 双写（路径已修正） | 同值部分 `@include card-header` | DR-2 |
| F-P3-5 | AtomCode P3-4 | 规范 | `_dashboard-sections.scss:5-8` | `$section-*` 文件私有，**判定保留**（无动作） | 观察清单标注已判定 | DR-2 N/A |
| F-P3-6 | AtomCode P3-6 | 规范 | `core/constants.py:13-87` | 7 组 `*_CHOICES` 死代码 + 2 处 label map 消费（方案在案） | 按 A1-A3 既定方案执行 | BR-3, DR-1 |

---

## 四、旧报告 46 项交叉核对（沿用 mimo 实测，融合确认）

| 旧# | 结论 | 证据摘要 |
|:----|:-----|:---------|
| 1,5-7,9-26,29-31,33-38,40-41,44-46 | 已修复（维持） | 旧报告 ✅，抽查无回退 |
| **2,3,4,8** | **代码已修、旧报告未标 ✅** | original_* 快照还原；selector user 隔离；recycle :171 审计；security-scan.yml |
| **27** | **仍开放 → F-P2-2** | console.error 实测 **160** |
| 28,32,42,43 | 误报（维持） | 旧报告已论证 |
| 39 | 重复（维持） | 与 #30 同一缺陷 |

**汇总**：41 关闭（含 4 项待补 ✅ 标注）+ 1 仍开放（#27）+ 误报/重复 5 类维持。

---

## 五、五个矩阵（融合）

### 1. 功能覆盖矩阵

| 功能点 | 需求来源 | 后端实现 | 前端/Store | 测试 | 状态 |
|:-------|:---------|:---------|:-----------|:-----|:-----|
| 资产 CRUD | AC-1~9 | asset_service / asset_view | assetStore | 有 | ✅（AC-10 命名偏差） |
| 出库→在用 | AC-20~34 | out_asset_service（行锁✅；**409 缺 F-P2-8**；**usage_type 缺 F-P2-9**） | outAssetStore | 有 | ⚠️ |
| 回收→待发放 | AC-30s recycle | recycle_service :171 审计 | recycleAssetStore | 有 | ✅ |
| 损坏/遗失/找回 | AC-35~38 | broken_lost_repair + mark 端点 | assetStore | 正向有；**权限旁路 F-P1-1** | ⚠️ 权限 |
| 维修 | AC-39~41 | repair_asset_service（含 409） | repairAssetStore | 有 | ✅ |
| 报废审批 | AC-42~47 | damaged_asset_service | damagedAssetStore | 有（含终态拒绝） | ✅ |
| 未登记资产 | AC-49/50 | unregisteredasset app | — | 有（30 格矩阵+行级） | ✅ **F-P1-5/6/7 已修 2026-09-24** |
| 扫码 | AC-60d~g | public_scan 6 字段 | qr | 白名单断言 | ✅（文档漂移 F-P2-13） |
| 密码/改密 | AC-3/3B/3C | 原密码+吊销✅；复杂度？ | — | :407/:414/:421 | ⚠️ F-P2-10 |
| 仓库配置 | 系统配置 | Storage CRUD+batch | storageStore | service 有；**RBAC 缺 F-P1-2** | ⚠️ 权限 |
| health/ready | OC-6 | urls:150-151 | — | **无 F-P2-1** | ⚠️ |

> AC 84 条抽查约 35 条（≈42%，AtomCode 口径）；未抽查不声明覆盖（Fact-1）。

### 2. 接口契约矩阵（§3）

| 契约项 | 后端 | 前端 | 一致性 |
|:-------|:-----|:-----|:-------|
| `{code,data,message}` | response_utils | request.ts 拦截器 | ✅ |
| 状态 8 键名 | TextChoices | types/common.ts（G-3 PASS） | ✅ |
| page/page_size | CustomPageNumberPagination | api 统一传参 | ✅ |
| ISO 8601 +08:00 | DRF | dayjs | ✅ |
| trace_id | request_context.py:52-63 | request.ts:255-256 | ✅ |
| 409 冲突语义 | repair 有 / **出库无** | — | ⚠️ F-P2-8（非根结构破坏） |

**§3 根结构/枚举/分页/时间：未发现单方破坏，不触发 [HALT]。**

### 3. 状态机覆盖矩阵（CT-3）

| 转换 | 实现 | FSM 直接测试 | 结论 |
|:-----|:-----|:-------------|:-----|
| in_store→in_use/broken/lost | transitions:24-27 | ✅ :249 类/mark 多例 | ✅ |
| in_use→recycled_pending | :32 | ⚠️ 缺 FSM 正向锚（service 有） | F-P2-11 |
| in_use→broken/lost | :33-34 | ✅ :95/:118 | ✅ |
| recycled_pending→in_use/broken/lost | :38-40 | ✅ | ✅ |
| recycled_pending→damaged | :41 | ⚠️ 缺直接锚 | F-P2-11 |
| broken→repairing/damaged | :44-46 | ✅ :206/:174 | ✅ |
| repairing→recycled_pending/damaged | :49-51 | ✅ :284/:347 | ✅ |
| lost→damaged | :55 | ✅ :164 | ✅ |
| lost→recycled_pending (found) | :56 | ⚠️ 正向靠 service | F-P2-11 |
| damaged→scrapped (approve) | :60 | ⚠️ 正向靠 service | F-P2-11 |
| damaged→回退 5 目标 | :61-65 | ✅ 参数化全覆盖 | ✅ |
| scrapped 终态 | :68 `{}` | ✅ :181-222 | ✅ |
| **lost→repairing** | **边不存在** | — | **F-P1-4 [HALT] H-1** |
| in_use→damaged 禁止 | 无转换 | 隐含 | ✅ |
| 4 特殊回退 | out_recycle/scrapping/unregistered | ✅ | ✅ |

### 4. 权限矩阵

| 角色/模块 | 操作 | 规范 | 实现 | 测试 | 状态 |
|:----------|:-----|:-----|:-----|:-----|:-----|
| regular 资产列表 | 读 | 本部门 | Selector user 隔离 | isolation 多例 | ✅ |
| regular 资产 CRUD | 写 | ❌ | admin_actions | 403 已有 | ✅ |
| regular **mark-broken/lost** | 写 | ❌ | **IsAssetAdminOrAbove（F-P1-1 ✅ 2026-09-23）** | 403/200 双向已补 | ✅ |
| 任意认证 **storage batch-*** | 写 | 仅 system_admin | **IsSystemAdmin（F-P1-2 ✅ 2026-09-23）** | regular 403 / admin 200 已补 | ✅ |
| regular **lifecycle batch_create** | 写 | ❌ | **IsAssetAdminOrAbove（F-P1-8 ✅ 2026-09-23）** | 403 锚已补 | ✅ |
| 未登记 **approve approver** | 审批 | 强制当前人 | **服务端强制覆盖（F-P1-5 ✅ 2026-09-24）** | 传他人被忽略已锚 | ✅ |
| 未登记 **discovery 提交** | 创建 | 本人/system_admin 代录 | **白名单 403（F-P1-6 ✅ 2026-09-24）** | 代录/自提/非法工号三向已锚 | ✅ |
| 未登记 **update/destroy/approve 对象** | 写 | 越权 404 | **读写全路径 scope + 矩阵门禁（F-P1-7 ✅ 2026-09-24）** | 30 格矩阵+跨部门 404 已锚 | ✅ |
| B12 Selector | — | 10 路 get_queryset_for_user | asset_selector:116+9 | 有 | ✅ |
| B11 角色类 | — | 写路径角色类 | core/permissions | 有 | ✅ |
| B14 批量逐条 | — | 逐条事务 | batch_mixins:157-219 | 有 | ✅ |
| system change_status | 专属 | system_admin | get_permissions 分支 | 403 有 | ✅ |
| 公开扫码 | 白名单 | 6 字段 | 严格断言 | test_public_scan | ✅ |

### 5. 规则符合性矩阵

| 规则 | 符合性 | 证据/备注 |
|:-----|:------:|:----------|
| CT-1 | √ | 双端测试目录齐备；权限反向缺口 → F-P2-5 |
| CT-2 | √ 覆盖率 / [PENDING] 变异 | mimo：84.40%/96.97%/93.17%/97.89%；变异 → F-P2-4 |
| CT-3 | √ 语义 / ⚠ 锚 / **[HALT] H-1** | 与 03/实现一致；4 正向锚+权限锚 → F-P2-11；协议冲突 → F-P1-4 |
| CT-4 | √ | 护栏 PASS；新 P1 须同 PR 补测 |
| CT-5 | √ | 双端 0 failed（mimo 全量） |
| CT-6 | [~] | 无新迁移；migration-check.yml 在 |
| DR-1 | √ / [~] constants | 护栏 PASS；死枚举 → F-P3-6 |
| DR-2 | [~] | card-header → F-P3-4 |
| DR-3 | √ | 零 raw()，Selector 收敛 |
| DR-4 | √ | utils 收口；logger 收敛后更完整 |
| DR-5 | √ | 函数长度 guard 0 超限；C90 → F-P2-7 |
| DR-6 | √ | 抽查 ≤3 层 |
| SC-1 | √ | 生产 fail-fast + dev 随机键（**V-8 推翻旧疑**）；.env gitignore |
| SC-2 | [~] | 无独立密钥扫描步骤 |
| SC-3/4 | √ | 零 raw SQL；无动态表名 |
| SC-5/6 | N/A | 全仓无上传（AtomCode） |
| SC-7/8 | √ | security-scan.yml + 周 cron |
| OC-1 | √ | request_context + 前端注入 |
| OC-2 | √ 后端 / [x] 前端 | json_formatter；160 console → F-P2-2 |
| OC-3 | √ 扫码白名单 / [~] 日志脱敏 | 4.6 六字段；通用日志 mask 需人工抽查 |
| OC-4 | √ 超额 + [~] QPS | metrics 已落地 |
| OC-5 | [~] | 未专项 |
| OC-6 | √ 端点 / [x] 测试 | F-P2-1 |
| OC-7 | [~] 豁免 | QPS 未达标 |
| AR-1 | √ | TODO_AI_CONFIRM=0 |
| AR-2 | [~] | 7 处标注待闭环 → F-P2-3 |
| AR-3/4 | √ | feishu env+退避；前端 TIMEOUT 集中 |
| AR-5 | √ | 门禁先于报告 |
| G-1~G-3,G-5 | √ | guard PASS |
| G-4 | [~] | 无脚本 → F-P3-3 |
| FR-6/8 | √ | frontend invariants PASS |
| Fact/Style | √ | 双报告+本融合均执行 |

---

## 六、[HALT] 与需人工确认

### [HALT]

| ID | 触发点 | 依据 | 状态 |
|:---|:-------|:-----|:-----|
| **H-1** | F-P1-4：`lost→repairing` 在 Review.md / 03 文档 / `transitions.py` 三方不一致 | 根级 §1.3；Review.md L110-111；CT-3 | ✅ **已裁定 A（方案2）2026-09-24**：改 Review.md（删 lost 直连 repairing，显式补找回后送修链），状态机边不动 |

- §3 跨端根契约：**未破坏**。  
- SC 红线新增：**未发现**。  
- CT-5：**0 failed，不触发**。  
- AtomCode 三处未登记资产 P1：按 ID-2 上报，**非 HALT**，待确认后修复。

### 需人工确认清单

1. ~~**H-1** 状态机契约源裁定（A/B）。~~ ✅ **已闭环 2026-09-24**（用户裁定 A 方案2：改 Review.md，状态机边不动；BF-040）。  
2. ~~**F-P1-5/6/7** 是否按 4.5 细则立即修复。~~ ✅ **已闭环 2026-09-24**（用户裁定扩到读写全路径 + 4.5 矩阵对齐，BF-038）。  
3. ~~**F-P1-1/2** 权限旁路修复方案与回归测试范围。~~ ✅ **已闭环 2026-09-23**（F-P1-1/2/8 + F-P2-5 同批完成）。  
4. ~~**F-P2-8/9/13**：AC 文档 vs 实现——改文档还是改实现。~~ ✅ **已拍板 2026-09-24**（D1 补实现、D2 取消不回退 usage_type、D3 扫码头表对齐 4.6 文档；见 §六各票 ✅）。  
5. ~~**F-P2-10**：密码复杂度是否有未定位实现。~~ ✅ **已闭环 2026-09-24**（证据链完整见 F-P2-10 票，零代码）。  
6. 旧报告 #2/#3/#4/#8 是否授权补 ✅。  
7. 变异测试执行环境（WSL/CI/stryker）。

---

## 七、建议修复顺序

1. ~~**F-P1-1 + F-P1-2 + F-P2-5**：权限旁路 + 先红后绿 403 测试。~~ ✅ **已完成 2026-09-23**（并入 F-P1-8 lifecycle `batch_create`）。  
2. ~~**F-P1-5 + F-P1-6 + F-P1-7**：未登记资产三处按 4.5 细则修复 + 回归。~~ ✅ **已完成 2026-09-24**（BF-038；范围扩大为读写全路径 + 门禁矩阵对齐）。  
3. ~~**F-P1-3 + F-P2-7 + F-P3-1**：ruff/format/mypy/C90 门禁修复（解 CI）。~~ ✅ **已完成 2026-09-24**（四提交 `9fecb97`/`210cfa7`/`272f8e6`/`3377c91`；三命令 + C90 exit 0）。  
4. ~~**H-1 人工裁定** → 改 Review.md 或走状态机变更协议。~~ ✅ **已完成 2026-09-24**（裁定 A 方案2；Review.md 已改，BF-040）。  
5. ~~**F-P2-1** health/ready 冒烟；**F-P2-4** 变异基线；**F-P2-2/3** 前端 logger 与 AI 标注清零。~~ 🟡 **F-P2-1/2/3/6 完成、F-P2-4 双基线归档 2026-09-24**（backend `cb7d409`+`3e0f165`、frontend `0ca00bc`+`9b49888`；F-P2-4 前端 60.00 / 后端 65.63 均 <80 待补测，见 `mutation-baseline-2026-09-24.md`）。  
6. ~~**F-P2-8~13** 与产品确认 AC 修订方向后批量处理。~~ ✅ **已完成 2026-09-24**（D1/D2/D3 拍板 → F-P2-8 补实现+锁收敛、F-P2-9 usage_type、F-P2-11 4 条 FSM 锚、F-P2-10/12/13 零代码闭环；backend `core/locks.py`+batch 收敛）。

---

## 八、最终审计票（融合）

```markdown
[审计票 - 必填项]
- 读取规范：已读 根/后端/前端 AGENTS & Rules（双 Agent 均读；融合二次取证补 unregisteredasset 4.5、settings throttle、development SECRET_KEY、auth 改密测试）
- CT-1[√] CT-3[√/4锚缺/H-1已裁定A] CT-5[√]
- DR-1[√/constants待收敛] DR-5[√护栏/C90×2已拆]
- SC-1[√] SC-3[√]
- 跨端契约：未破坏
- 红线触发：[HALT] H-1 已裁定 A（改文档，状态机边未变）；其余无
- 建议提交：是（H-1 文档修正 + F-P1-3/5/6/7+F-P2-7+F-P3-1 均已闭环）

[审计票 - 自检项]
- 测试：CT-2[√覆盖率/PENDING变异] CT-4[√] CT-6[~ 无新迁移]
- DRY：DR-2[~card-header] DR-3[√] DR-4[√] DR-6[√]
- 安全：SC-2[~] SC-4[√] SC-5/6[N/A] SC-7[√] SC-8[√]
- 可观测性：OC-1[√] OC-2[√后端/x前端160] OC-3[√扫码/~日志] OC-4[~] OC-5[~] OC-6[√端点/x测试] OC-7[~]
- AI鲁棒性：AR-1[√] AR-2[~ 7处] AR-3[√] AR-4[√] AR-5[√]
- AI行为：Fact-1[√]（分歧项 V-1~V-14 均二次取证）
- 写作风格：Style-1[√] Style-2[√] Style-3[√]
- 覆盖率：整体 84.40%（≥80%）/ Service 96.97%（≥90%）/ 前端 93.17% / Store 97.89%（mimo 实测采信）
- 问题计数：P0=0 | P1=7+1（F-P1-8 本批新增） | P2=13 | P3=6 | 推翻并入 2 项（限流、dev key）；F-P1-1/2/8+F-P2-5 已修；F-P1-3/5/6/7+F-P2-7+F-P3-1 已修
```

---

## 修复追踪（本批 2026-09-23；活账本 BF-036）

| 条目 | 状态 | 改动面 | 验证 |
|:-----|:----:|:-------|:-----|
| **F-P1-1** mark-broken/lost 权限旁路 | ✅ 已修 | `asset_view.py`：`admin_actions` 追加两项；删两处 `@action(permission_classes=)` 死参数 | 先红后绿 `TestMarkBrokenLostRBAC`（regular 403 / asset_admin 200） |
| **F-P1-2** storage batch 权限缺口 | ✅ 已修 | `storage_view.py`：类级全量 `admin_actions`（含 batch 两项），经 Mixin 落 `IsSystemAdmin` | 新建 `test_storage_view_rbac.py` `TestStorageBatchRBAC`（regular 403 / system_admin 200） |
| **F-P1-8** lifecycle `batch_create` 旁路（用户拍板 A 并入） | ✅ 已修 | `_lifecycle_base.py`：`admin_actions` 含 `batch_create`；`get_permissions` 读该属性返回 `IsAssetAdminOrAbove`（**非** Mixin 的 `IsSystemAdmin`） | `test_create_row_isolation.py` 改写 11 用例含 403 锚 |
| **F-P2-5** RBAC 反向锚缺口 | ✅ 已修 | 上述三组测试同批补齐 | CT-1/CT-4 先红后绿 |

**门禁实测（2026-09-23）**：全量 `pytest -q` **1289 passed / 0 failed**（串行单进程）；`ruff check` 改动 6 文件 **All checks passed**；`mypy` 三 view **0 错**；`spectacular --validate` EXIT=0 且 `api-schema-baseline.json` **无漂移**（权限收紧不改端点结构，跨端契约未破坏）。

**环境备注**：`.venv` 缺 `openpyxl` 曾致 `test_export_excel` 500（`ImportError` 走 500 分支），已 `pip install openpyxl==3.1.5` 后 4 passed；`requirements/base.txt` **未声明** openpyxl——存量依赖声明缺口，建议另行补入（非本批红线）。

**相邻未修（建议另行立条，不并入本批）**：~~`export_excel` 的 `@action(permission_classes)` 同样被架空（矩阵 regular 导出 ❌）；damaged 单条 `create` 不在清单 regular 可 201。~~ ✅ **已修复 2026-09-23（BF-037）**。

---

## 修复追踪（BF-037 批 2026-09-23；活账本 BF-037）

| 条目 | 状态 | 改动面 | 验证 |
|:-----|:----:|:-------|:-----|
| **BF-036 遗留①** export_excel 权限旁路 | ✅ 已修 | `core/permissions.py:152,168` 新增 `CanExportExcel` + `resolve_viewset_permissions`；11 处 get_permissions 接线；删 `_export_mixin` 死参数 | 新建 `test_export_excel_rbac.py` 7 用例（regular 403 / 四角色 200 / anon 401 / damaged-export 403）先红后绿 |
| **BF-036 遗留②** damaged 单条 create 收紧 | ✅ 已修 | `damaged_asset_view.py:58` `admin_actions` 补 `"create"` 走 `IsDeptManagerOrAbove`；规则 `:142` 同批 v1.15 | `TestDamagedCreateRBAC` 3 用例 + `test_create_row_isolation` 改 dept_manager + 补 regular 403 锚 |
| **BF-036 遗留③** openpyxl 依赖声明缺口 | ✅ 已修 | `requirements/base.txt:66` 补 `openpyxl==3.1.5` | `import openpyxl` → 3.1.5；导出 500 消除 |
| **新发现（本批）** 导出 iterator 缺 chunk_size | ✅ 已修 | `_export_mixin.py:40,82` `_EXPORT_ITER_CHUNK_SIZE=1000` | 真实 queryset 导出 200 |
| **DR-1 收敛** 公共权限解析入口 | ✅ 已落地 | `resolve_viewset_permissions` 四分支；repair `admin_actions` 补全五项（用户修正） | `TestResolveViewsetPermissions` 5 用例 |

**门禁实测（2026-09-23 BF-037）**：全量 `pytest -q` **1312 passed / 0 failed**（串行单进程 1540s）；`ruff check` 本批 17 文件 **All checks passed**；`mypy apps core` 仅 1 存量错（`unregisteredasset:138`，非本批）；`spectacular --validate` EXIT=0 且 `api-schema-baseline.json` **SCHEMA_ZERO_DIFF=YES**；`manage.py check` no issues。

```markdown
[审计票 - 必填项]（BF-037 批）
- 读取规范：已读 根/后端 AGENTS & Rules（矩阵 :142/:148、方案 A、BF-036 遗留段）
- CT-1[√] CT-3[√/H-1 仍待裁定] CT-5[√ 1312 passed 0 failed]
- DR-1[√ resolve_viewset_permissions 唯一入口，11 处收敛] DR-5[√ 改动文件均 ≤500 行]
- SC-1[√] SC-3[√ 零 raw SQL]
- 跨端契约：未破坏（SCHEMA_ZERO_DIFF=YES；仅权限类收紧 + 依赖补录）
- 红线触发：无（未增删状态机节点/字段/枚举键名；规则 :142 属后端专属矩阵修订，已走 [PATCH-BE] 留痕）
- 建议提交：是（BF-037 闭环；与工作区其他未提交改动分开审视后提交）
```

```markdown
[审计票 - 必填项]
- 读取规范：已读 根/后端 AGENTS & Rules（backend-business-rules §4.2 权限矩阵 :141/:144 为权限收紧依据）
- CT-1[√] CT-3[√/H-1 仍待裁定] CT-5[√ 1289 passed 0 failed]
- DR-1[√ 权限清单类属性单源] DR-5[√ 改动文件均 ≤500 行]
- SC-1[√] SC-3[√ 零 raw SQL]
- 跨端契约：未破坏（schema 基线无漂移；仅权限类收紧，响应根结构/枚举/分页/时间零变更）
- 红线触发：无（仅权限收紧，未增删状态机节点/字段/枚举键名；H-1 仍待人工，本批未触碰状态机）
- 建议提交：是（F-P1-1/2/8+F-P2-5 闭环；与工作区其他未提交改动分开审视后提交）

[审计票 - 自检项]
- 测试：CT-2[√ 全量 1289；覆盖率门槛本次未单独跑 --cov-fail-under=80，以 0 failed + 既有 84.40% 基线记录] CT-4[√ 先红后绿] CT-6[~ 无迁移]
- DRY：DR-2[√] DR-3[√] DR-4[√] DR-6[√ 调用链 ≤3]
- 安全：SC-2[~] SC-4[√] SC-5/6[N/A] SC-7[√] SC-8[√]
- 可观测性：OC-1[√] OC-2[√] OC-3[√] OC-4[~] OC-5[~] OC-6[√] OC-7[~]
- AI鲁棒性：AR-1[√] AR-2[√] AR-3[√] AR-4[√] AR-5[√]
- AI行为：Fact-1[√]（openpyxl 依赖缺口、export_excel 架空为实测事实，非推测）
- 写作风格：Style-1[√] Style-2[√] Style-3[√]
- 覆盖率：沿用审查基线 整体 84.40% / Service 96.97%（本批未重跑 --cov）
```

---

## 修复追踪（BF-038 批 2026-09-24；活账本 BF-038）

| 条目 | 状态 | 改动面 | 验证 |
|:-----|:----:|:-------|:-----|
| **F-P1-5**（AtomCode P1-1）approver 可代签 | ✅ 已修 | `views.py` approve：`approver = operator_jobcode` 服务端无条件覆盖；serializer `required=True` 保留（传值被忽略，零 schema 字段变更） | `TestFp15ApproverOverride` DB 断言落库=操作人 |
| **F-P1-6**（AtomCode P1-2）discovery_person 冒名 | ✅ 已修 | `views.py` create：operator 恒 `resolve_operator`；`discovery_person` ≠本人且非 system_admin → `PermissionDenied` 403；Service `create` 增 `discovery_person_jobcode`（默认=operator，审计与代录解耦） | `TestFp16DiscoveryWhitelist`（代录 403 / 自提 200 / 系统代录 200 / 非法工号 400）+ `test_create_discovery_person_decoupled_from_operator` |
| **F-P1-7**（AtomCode P1-3）写路径无行级 scope + 门禁不符矩阵 | ✅ 已修（**范围扩大**，用户裁定） | ① 读：`Selector.get_queryset_for_user`（规则1-3+本人例外）接入 `get_queryset`；② 写：update/destroy/approve 改 `get_by_code_for_user` 越权 404；③ batch_delete：`passes_user` + Service 逐条 scoped→同构 `NOT_FOUND`（保 test_b5 契约）；④ 门禁：`_ACTION_PERMISSION_OVERRIDES` 按 4.5 矩阵对齐（list/retrieve→`IsAssetAdminOrAbove`；写→`IsSystemAdminOrAssetAdmin`；approve→`IsDeptManagerOrAbove`），**dept_manager update 由可写收紧 403**（用户拍板） | 新建 `test_matrix_permissions.py` MATRIX_GRID **30 格** + 行级隔离；`TestGetQuerysetForUser` 10 用例；跨部门 404 / 系统全量 / 下级部门可见 |

**门禁实测（2026-09-24 BF-038）**：全量 `pytest -q` **1368 passed / 0 failed**（串行单进程 1063s，较 BF-037 基线 +56；format 前执行，format 后模块复跑 **142 passed**）；`ruff check` 全仓 7 错均为存量（notification F401 + loadtest I001/E402，本批 0 新增）、本批 10 文件 `All checks passed`；`ruff format` 本批 10 文件已对齐（全仓 117 待格式化归口 F-P1-3 独立批）；`mypy .` **208 文件 0 错**（顺带消除 BF-037 记录的 `unregisteredasset:138` 存量 union-attr）；`spectacular --validate` EXIT=0，`api-schema-baseline.json` **仅 create 端点 docstring 描述 1 行漂移**（代录白名单措辞，非破坏，M-3 重导随改）；`manage.py check` no issues；`check_duplicate_invariants.py` **PASS（G-1~G-5）**；覆盖率（模块+permissions）**89.86%**（services 97% / selectors 97% / views 99% / models 100%）。

**先红后绿证据**：修复前行为由 AtomCode P1-1/2/3 + 融合二次取证 V-1~V-3 留档；新增 56 用例锁死新行为（安全用例与修复同批落地）。过程内真实红→绿一例：`make_dept` 工厂漏物化 `path` 致 2 条下级部门用例红（`get_all_descendants` 返回空），补 `path`/`level` 构造后转绿。

```markdown
[审计票 - 必填项]（BF-038 批）
- 读取规范：已读 根/后端 AGENTS & Rules（backend-business 4.5 细则 :179-209 为修复依据；用户裁定 F-P1-7 扩读写全路径 + dm update 收紧）
- CT-1[√ 新增 56 用例] CT-3[√/H-1 仍待裁定] CT-5[√ 1368 passed 0 failed]
- DR-1[√ get_user_role/is_system_admin/resolve_viewset_permissions/get_queryset_for_user 单源] DR-5[√ 改动文件最大 services.py 414≤500]
- SC-1[√] SC-3[√ 零 raw SQL]
- 跨端契约：未破坏（响应根结构/枚举/分页/时间零变更；schema 基线仅 docstring 描述 1 行）
- 红线触发：无（未增删状态机节点/字段/枚举键名；权限收紧不属跨端契约变更）
- 建议提交：是（BF-038 闭环；后端一提交，文档登记随根仓一提交）
```

```markdown
[审计票 - 自检项]（BF-038 批）
- 测试：CT-2[√ 模块 89.86%；Service 97%≥90%] CT-4[√ 行为留档+56 锚；path 工厂红→绿实录] CT-6[√ 无迁移]
- DRY：DR-2[N/A] DR-3[√ 查询收敛 Selector] DR-4[√] DR-6[√ 链 ≤3]
- 安全：SC-2[~] SC-4[√] SC-5/6[N/A] SC-7[√] SC-8[√]
- 可观测性：OC-1[√] OC-2[√] OC-3[√] OC-4[~] OC-5[~] OC-6[√] OC-7[~]
- AI鲁棒性：AR-1[√] AR-2[~] AR-3[N/A 无新外部调用] AR-4[N/A] AR-5[√ 自我修正 3 起已闭环]
- AI行为：Fact-1[√]（全部数字为本批实测）
- 写作风格：Style-1~Style-3[√]
- 覆盖率：整体沿用 84.40% 基线未重跑全仓 --cov；本批模块专项 89.86%（全量 1368 passed 0 failed）；变异 T8[F-P2-4 开放环境待定]
```

---

## 修复追踪（门禁三连红批 2026-09-24；活账本 BF-039）

| 条目 | 状态 | 改动面 | 验证 |
|:-----|:----:|:-------|:-----|
| **F-P1-3** CI 静态门禁三连红 | ✅ 已修 | 四提交：① `9fecb97` ruff check 归零（F401+I001/E402）；② `210cfa7` ruff format 107 文件全量纯格式化；③ `272f8e6` mypy --strict 208 文件 0 错（types-channels 新增 + `models.py:122` 自引用校验真缺陷修复 **行为变化** + 带原因 type: ignore）；④ `3377c91` C90 归零 | `ruff check .` / `ruff format --check .` / `mypy . --strict` / `ruff check . --select C90 --config lint.mccabe.max-complexity=10` 四命令 exit 0 |
| **F-P2-7** 生产 C90 11>10 | ✅ 已修 | `update_user` 抽 `_validate_unique_constraints`；`_create_role_permissions` 抽 `_resolve_perms_to_add`；pyproject ruff exclude 加 `.trae`（12 处存量非生产）；`clear_database.py` per-file 加 C901；`ci.yml` C90 命令改 `--config lint.mccabe.max-complexity=10`（防 CLI --exclude 顶掉 pyproject 配置）；`AGENTS.md:29` 门禁命令同步 | 定向 `apps/authusermanagement` + `test_init_production_data` **88 passed**；C90 全仓 0 错 |
| **F-P3-1** loadtest/test F401+I001 | ✅ 已修 | `9fecb97`：`test_ws_consumer.py` 删未用 asyncio；两 locustfile `import json` 上移顶部+I001 | `ruff check .` exit 0 |

**门禁实测（2026-09-24 门禁批）**：`ruff check .` **All checks passed**（exit 0）；`ruff format --check .` **449 files already formatted**（exit 0）；`mypy . --strict` **Success: no issues found in 208 source files**（exit 0）；`ruff check . --select C90 --config lint.mccabe.max-complexity=10` **exit 0**（15 错→0：2 生产拆分 + 12 `.trae` exclude + 1 clear_database per-file）；定向测试 `test_department_service + test_services + test_service_coverage + notification/tests` **82 passed**、`apps/authusermanagement + test_init_production_data` **88 passed**（均串行单进程）。

**models.py:122 行为变化（Commit 3 显式标注）**：原 `parent_id == self.pk` 恒 False（parent FK 指向 `recordcode` 字段而非 `pk`，类型也不匹配），自引用校验从未生效；修复后比对 `self.recordcode` + `filter(recordcode=self.parent_id)`，校验**从永假→生效**。CT-4 锚 `test_department_clean_self_parent_rejected` 先红后绿证实。脏数据 ORM 扫描 `Department.objects.filter(parent_id=F('recordcode'))` = 0 条，存量无脏数据风险。

```markdown
[审计票 - 必填项]（门禁批 BF-039）
- 读取规范：已读 根/后端 AGENTS & Rules
- CT-1[√ 定向 82+88 passed] CT-3[N/A 未触状态机] CT-5[√ 串行单进程绿]
- DR-1[√ 唯一性校验/权限展开各抽单源] DR-5[√ 改动文件最大 services.py ≤500]
- SC-1[√] SC-3[√ 零 raw SQL]
- 跨端契约：未破坏（响应结构/枚举/分页/时间零变更；纯静态门禁+内部重构）
- 红线触发：无
- 建议提交：是（后端四提交 `9fecb97`/`210cfa7`/`272f8e6`/`3377c91`；父仓 ci.yml+子模块 bump+文档登记一提交）
```

```markdown
[审计票 - 自检项]（门禁批 BF-039）
- 测试：CT-2[√ 沿用基线未重跑全仓 --cov] CT-4[√ models 自引用锚先红后绿] CT-6[√ 无迁移]
- DRY：DR-2[N/A] DR-3[√] DR-4[√] DR-6[√ 拆分后链 ≤3]
- 安全：SC-2[~] SC-4[√] SC-5/6[N/A] SC-7[√] SC-8[√]
- 可观测性：OC-1[√] OC-2[√] OC-3[√] OC-4[~] OC-5[~] OC-6[√] OC-7[~]
- AI鲁棒性：AR-1[√] AR-2[√ 无新增高风险标注] AR-3[N/A] AR-4[N/A] AR-5[√ 四命令自我验证]
- AI行为：Fact-1[√]（全部数字为本批实测 exit code）
- 写作风格：Style-1~Style-3[√]
- 覆盖率：整体沿用 84.40% 基线；定向 82+88 passed（未重跑全仓 --cov）
```

---

## 九、来源与变更日志

- **mimo-v2.6-flash-free-2026-09-23.md**：门禁全量数字、F-P1-1~4、F-P2-1~7、旧 46 项核对、H-1、五矩阵骨架。  
- **AtomCode-2026-09-23.md**：F-P1-5~7、F-P2-8~13、AC 抽查口径、B11-B14 细则、G-4 缺口、FSM 锚点细目。  
- **二次取证（本融合）**：确认 V-1~V-5、V-9~V-11、V-14；推翻 V-7、V-8；修正 V-6、V-13。  
- 历史基线：`docs/Review/full-review-report-2026-09-17.md`（46 项）。  
- 外部参考：OWASP Foundation, "OWASP Top 10," OWASP, 2021（权限/安全维度参照）。

---

*融合审查报告结束。生成：mimo-v2.6-flash-free（对照合并 AtomCode 报告）| 2026-09-23*
