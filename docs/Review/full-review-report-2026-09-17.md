# 全量代码审查报告

> 审查时间：2026-09-17 | 审查方式：7 轮分审（code-quality-check skill）
> 审查范围：全项目（asset_management_backend + vue-assetmanagement）
> 审计员：AI Code Quality Check

---

## P0 — 阻断级（2 件）

| # | 文件:行号 | 问题 | 规则 |
|:--|:----------|:-----|:-----|
| 1 | ~~`serializers/asset_crud_serializers.py`~~ ~~+ `services/asset_service.py`~~ | ~~`AssetCreateSerializer` 将 `asset_current_status` 暴露在写入字段集，客户端可 POST `{"asset_current_status":"scrapped"}` 直接创建终态资产，绕过 FSM 全部校验~~ ✅ **已修复 2026-09-17**（详见「修复追踪」） | ~~B5, B9~~ |
| 2 | `services/out_asset_service.py:66-84,215-245` | cancel_outasset 快照只记录出库单的申请人/保管人，未记录出库前资产原值；取消后资产被错误覆写为出库单上的人员而非原始人员，数据不可逆 | B7, 数据完整性 |

## P1 — 严重级（10 件）

| # | 文件:行号 | 问题 | 规则 |
|:--|:----------|:-----|:-----|
| 3 | `selectors/asset_selector.py:73-229` | `get_available_assets`/`search_assets` 等 5 个查询方法未内置行级隔离，完全依赖 View._scoped() 包裹；任何未来 Service 直接调用即泄露跨部门数据 | B12 |
| 4 | `services/recycle_asset_service.py:100-138` | 回收时 is_broken/is_lost 产生二次 FSM 转换（recycled_pending→broken/lost），第二次转换无 `AuditLogger.log_state_change` 审计 | B7 |
| 5 | ~~`services/asset_service.py:358-390`~~ ~~+ `views/asset_view.py:314`~~ | ~~`change_outasset_employee`/`transfer_asset_to_storage` 审计记录 operator_jobcode=None，无法追溯操作人~~ ✅ **已修复 2026-09-20**（详见「修复追踪」） | ~~B7~~ |
| 6 | ~~`composables/useOutAssetForm.ts:286-289` + `stores/createEntityStore.ts:340`~~ | ~~编辑外借资产提交时 key 不匹配（payload 用 `asset_recordcode`，store 期望 `recordcode`），更新必定失败~~ ✅ **已修复 2026-09-20**（详见「修复追踪」；靶点修正：真实生产路径另有 `components/componentsdetails/detils/OutAssetForm.vue:413` 同款 bug） | ~~CT-4~~ |
| 7 | ~~`components/componentsdetails/detils/AssetBatchImport.vue:146,269`~~ | ~~组件内直接 `request.post('/assets/assets/batch-create/')` 重复定义已有 API 端点，绕过 unwrapResponse，违反 FR-3 和 F11~~ ✅ **已修复 2026-09-20**（详见「修复追踪」；端点收敛至 `assetStore.batchCreateAssets`） | ~~FR-3, F11, B1~~ |
| 8 | 缺失 `.github/workflows/security-scan.yml` | SC-7 要求 pip-audit/npm audit 依赖漏洞扫描阻断高危合并，CI 尚未配置 | SC-7 |
| 9 | ~~`services/asset_service.py:217-242 vs 273-347`~~ | ~~`delete_asset` 与 `batch_delete_asset` 重复实现同一套删除校验逻辑，且错误码分叉~~ ✅ **已修复 2026-09-20**（详见「修复追踪」；靶点修正：真实分叉为 `ASSET_HAS_OUTASSET` vs `HAS_OUTASSET_RECORDS`，报告原述 `IN_USE` vs `ASSET_IN_USE` 有误，行号亦修正） | ~~DR-1~~ |
| 10 | ~~`services/damaged_asset_service.py:138-147/213-221/277-285`~~ | ~~approve/reject/cancel 三处手写 `DamagedAsset.objects.filter(...)` 绕过已有的 `DamagedAssetSelector.get_asset_recordcode_for_update`~~ ✅ **已修复 2026-09-20**（详见「修复追踪」；靶点修正：原 Selector `get_asset_recordcode_for_update` 自身不可用——`with_asset_details()` 的可空 `select_related` + `select_for_update` 在 PostgreSQL 抛 `NotSupportedError`，需先修 Selector 再收敛调用点） | ~~DR-3~~ |
| 11 | ~~`apps/usermanagement/services.py`~~ ~~+ `services/` 包~~ | ~~277 行影子版与包各存一套 EmployeeService/DepartmentService，包优先解析致 `.py` 版为影子死代码~~ ✅ **已修复 2026-09-20**（详见「修复追踪」；靶点修正：影子可正常导入——`BusinessLogicError` 存在于 `core/exceptions.py:72`、`ValidationError` 为其别名 `:119`，死因纯系包优先解析；旧审查文档 `services.py:527/182` 行号属更老 527 行版本快照） | ~~DR-1~~ |
| 12 | ~~`views/asset_view.py`（512 行）~~ | ~~超过 DR-5 500 行上限，且无 `# TECHNICAL_DEBT` 标注~~ ✅ **已核实 2026-09-20**（详见「修复追踪」；标注已存在于第 1 行 `# TECHNICAL_DEBT: >500 lines`，DR-5 存量豁免成立；行数现 512、超限 2.4%；报告「502 行 / 无标注」两点均为过时描述，方案 B 拆分留档建议项） | ~~DR-5~~ |

## P2 — 中等级（18 件）

| # | 文件:行号 | 问题 | 规则 |
|:--|:----------|:-----|:-----|
| 13 | ~~`tests/test_state_machine.py`~~ | ~~5+ 条合法 FSM 路径（outasset、cancel_outasset、cancel_recycle、in_store→broken、in_store→lost）无测试用例~~ ✅ **已修复 2026-09-20**（详见「修复追踪」；靶点修正：5 条路径确认合法——`VALID_TRANSITIONS`（`transitions.py:22`）声明 `IN_STORE→IN_USE/BROKEN/LOST`、`cancel_outasset` 支持 `in_use→{in_store,recycled_pending}`、`cancel_recycle` 支持 `recycled_pending→in_use`；FSM 方法不调 `save()`，直调断言内存态与既有 6 处一致；outasset 终态为 `in_use` 非 "out 值"） | ~~CT-3~~ |
| 14 | ~~`tests/` 缺 `test_out_asset_service.py`~~ | ~~核心 Service 层 out_asset_service 无独立单测文件，仅通过快照/API 测试间接覆盖~~ ✅ **已修复 2026-09-20**（详见「修复追踪」；新建 `tests/test_out_asset_service.py` 8 用例：create 双源成功 + `original_*` 快照落库 + 仓库清空、非法态 `ILLEGAL_OUTASSET`、`batch_delete_outasset` 快照恢复（仅 in_store 源恢复仓库）、批量 fail_items 结构） | ~~CT-1~~ |
| 15 | ~~`services/damaged_asset_service.py:51,54`~~ | ~~`DamagedAsset.objects.create` 在 `select_for_update` 之前执行，存在 TOCTOU 并发竞态~~ ✅ **已修复 2026-09-20**（详见「修复追踪」；双 PR：PR-1 锁前置重排（锁→锁内 RBAC→锁后查重→create，IntegrityError 兜底映射业务码）；PR-2 OneToOne→FK + 部分唯一索引 + reject 软删释放槽位，方案 X「拒绝记录软删、拒绝理由仅 DB 保留」） | ~~B5~~ |
| 16 | ~~`views/damaged_asset_view.py:130-150` + `out_asset_view.py:197-203`~~ | ~~update/partial_update 直接传 `request.data` 给 Service，Serializer 仅用于响应而非输入校验~~ ✅ **已修复 2026-09-20**（详见「修复追踪」；双 View update/partial_update 改走 serializer 门禁 `is_valid(raise_exception=True)` → `validated_data` 传 Service；out_asset 补 `get_object()` 行级 RBAC；`OutAssetUpdateSerializer` 增 `outasset_using_location`（Gap A 保留可写）；`DamagedAssetUpdateSerializer` 收窄 `asset_recordcode`/`is_active` 为只读；未知字段决策=静默忽略（Gap B）） | ~~B8~~ |
| 17 | ~~`views/_lifecycle_base.py:100-134`~~ | ~~batch_delete 在 View 层含业务循环+错误分类，应在 Service 层实现~~ ✅ **已修复 2026-09-20**（详见「修复追踪」；三视图集批量删除编排收敛至 Service 层 `AssetLifecycleMixin.batch_delete_lifecycle_asset`，响应组装统一走 `BatchResponseHelper.delete_response`；`repair_asset_view.batch_delete` 同一反模式一并收敛；行为变更清单已按用户拍板留痕） | ~~§1.2~~ |
| 18 | ~~`views/asset_view.py:182,190` + ~~`views/contract_view.py:153`~~ | ~~`getassetbyname`/`getassetbyrecordcode`/`getcontractByname` URL 使用 camelCase 连写，而非项目 snake_case 约定~~ ✅ **已修复 2026-09-20**（详见「修复追踪」；靶点修正：原报告行号 `:179,187` 过时，实为 `:182,190`；另发现合同侧同类污点 `getcontractByname`（`contract_view.py:153`，非死端点——前端 `contract.ts:86`→`contractStore.ts:55`→`useAssetFormHelpers.ts:95` 真实调用链）一并整改，三端点统一为 `get_asset_by_name`/`get_asset_by_recordcode`/`get_contract_by_name`，路径参数 + snake_case 对齐 `by-asset/{code}` 既有约定） | ~~B2~~ |
| 19 | ~~`services/damaged_asset_service.py:68` + ~~`serializers/asset_crud_serializers.py:302`~~ | ~~状态值使用字符串字面量 `"damaged"`/`"in_store"` 而非枚举引用 `Asset.AssetStatus.*`~~ ✅ **已修复 2026-09-20**（详见「修复追踪」；靶点修正：报告 `damaged_asset_service.py:68` 过时实为 `:80`；`asset_crud_serializers.py:302` 指控不成立——:302 为 `asset_purchase_number` 的 default，该文件零状态字面量，删除该靶点；另核实真实命中 4 处生产 + 测试文件收敛 16 处） | ~~B9~~ |
| 20 | ~~`config/settings/base.py:25,196`~~ | ~~`SECRET_KEY` 默认空字符串，误用 base.py 的部署环境 JWT 签名可被伪造~~ ✅ **已修复 2026-09-20**（详见「修复追踪」；靶点修正：`SIMPLE_JWT` 物化键实为 `:212`（原报 `:196` 为 `SIGNING_KEY` 值引用，物化赋值在 `SIMPLE_JWT` dict 内）；**范围扩大**——不止"误配 base"：`SIMPLE_JWT["SIGNING_KEY"]` 在 base 加载时快照 `config("SECRET_KEY", default="")`，development/生产正常路径亦可能落到空串，实证空密钥可解任意 token） | ~~SC-1~~ |
| 21 | ~~13 处后端方法 >50 行~~ | ~~`batch_delete_asset`(66)、`create_recycle_asset`(85)、`create_outasset`(68)、`approve_asset_recordcode`(58) 等~~ ✅ **已修复 2026-09-21**（详见「修复追踪」；实测口径修正：原"13 处"计数过时且口径不统一 — 实测物理行口径 38 处、BR-4 语义逻辑行口径 19 处；本项以「guard 门禁 + 台账分批拆分」固化落地） | ~~BR-4~~ |
| 22 | 9 处前端 Composable >200 行 | `useOutAssetForm`(358)、`useNotification`(312)、`useDashboardPage`(275) 等 | FR-6 |
| 23 | `stores/createEntityStore.ts`（501 行） | 超 DR-5 上限且无 `TECHNICAL_DEBT` 标注 | DR-5 |
| 24 | 10 处重复 `XxxBatchCreateResult` 接口 | `api/asset.ts`、`outAsset.ts`、`department.ts` 等 10 个文件定义相同形状 | DR-1/DR-4 |
| 25 | `stores/entityStoreTypes.ts:60` vs `types/common.ts:26` | 两套分页响应类型 `ListResponse`/`PaginatedResponse` 并存，Store 映射不一致 | DR-1 |
| 26 | `composables/useExcelExport` / `useOperationLogExcelExport` / `useUserExcelExport` | 三处 Excel 导出流程重复实现，仅列配置不同 | DR-1/FR-2 |
| 27 | 前端 ~80+ 处 `console.error` 无结构化 | catch 块均使用 `console.error('...:', error)`，无 trace_id、无 JSON 格式 | OC-2 |
| 28 | `core/permissions.py:63-80` | 无部门的 dept_manager/asset_admin 角色返回 None → 完全锁定（无任何权限），无测试覆盖此边界 | RBAC |
| 29 | `LoginDialog.vue:16-17` | 用户名/密码 reactive 绑定到 input 但从未提交，死代码或安全隐患 | SC-1 |
| 30 | `views/out_asset_view.py:170-172` | recyclable 未分页时返回裸列表 `data=[...]`，与其他 list 端点 `data={count,results}` 不一致 | §3 |

## P3 — 低级/优化建议（16 件）

| # | 问题摘要 | 规则 |
|:--|:---------|:-----|
| 31 | `broken_lost_repair.py:27` docstring 漏写 `in_use` 源状态 | — |
| 32 | `scrapping.py:131-186` 5 个 `reject_to_*` 方法为死代码 | — |
| 33 | `clear_database.py:130,205` f-string SQL（有白名单缓解） | SC-3 |
| 34 | `development.py:24-27` 弱 dev key 未加入 `_INSECURE_KEYS` | SC-1 |
| 35 | `request_context.py:79-81` 信任 X-Forwarded-For 首值无配置守卫 | SC |
| 36 | `core/constants.py:15-24` 状态枚举与 Model 重复定义 | DR-1 |
| 37 | `asset_service.py:133` 循环内 `import json` | DR-4 |
| 38 | `asset_service.py:337` 访问 FSM 私有方法 `_transition` | AR-1 |
| 39 | `out_asset_view.py:172` recyclable 分页形状不一致 | §3 |
| 40 | `_export_mixin.py:51,69` 无界 queryset 导出有 OOM 风险 | OC-7 |
| 41 | `exception_handler.py:53-78` error_code 未进入单条错误响应 | B1 |
| 42 | `repair_asset_service.py:177,238` 查询未加 `is_deleted=False` | — |
| 43 | `recycle_asset_service.py:348` 调用链 View→Service→Service→FSM=5 层 | BR-7 |
| 44 | `common-forms.scss:11-30` SCSS 重复定义 CSS 变量 | DR-1/F1 |
| 45 | `variables.css` 暗色模式未覆盖 `--gradient-card-*` | F13 |
| 46 | 7 个 composable 无测试（useChartTheme 等） | CT-1 |

---

## 规则符合性矩阵

| 规则 | 状态 | 说明 |
|:-----|:-----|:-----|
| CT-1 | [~] | 核心模块大部分有测试，out_asset_service 缺专用测试 |
| CT-2 | [~] | 整体估计 ≥80%，Store 层 ≥90%，未经 CI 实测 |
| CT-3 | [x] | 5+ 条 FSM 路径无测试（见 #13） |
| CT-4 | [√] | 回归屏障存在（快照测试），但 out-asset edit key 契约未覆盖 |
| CT-5 | [√] | 测试失败阻塞机制存在 |
| CT-6 | [N/A] | 本次审查无迁移文件变更 |
| DR-1 | [x] | 多处重复：删除校验、Selector 绕过、枚举重复、接口重复（#9,#10,#11,#24,#25,#36） |
| DR-2 | [√] | 组件原子化基本合规 |
| DR-3 | [x] | damaged_asset_service 绕过 Selector（#10） |
| DR-4 | [x] | 批量结果接口 10 份重复、Excel 导出 3 份重复（#24,#26） |
| DR-5 | [x] | asset_view.py 502 行、createEntityStore 501 行（#12,#23） |
| DR-6 | [√] | 嵌套层级基本合规 |
| SC-1 | [~] | dev key 弱但有缓解，base.py 默认空串需关注（#20） |
| SC-3 | [√] | 核心查询均参数化，clear_database 有白名单缓解 |
| SC-7 | [x] | CI 未配置依赖扫描（#8） |
| OC-1 | [√] | trace_id 全链路透传 |
| OC-2 | [x] | 前端 ~80+ 处 console.error 无结构化（#27） |
| OC-3 | [√] | 后端日志脱敏，前端有泄露风险（#43） |
| OC-6 | [√] | /health /ready 端点已实现 |
| AR-1~AR-5 | [√] | AI 鲁棒性整体合规 |

---

## 统计

| 严重级别 | 数量 |
|:---------|:-----|
| P0 | 2 |
| P1 | 10 |
| P2 | 18 |
| P3 | 16 |
| **合计** | **46** |

## 建议修复优先级

1. **立即修复**（P0）：FSM 绕过（#1）、cancel 数据完整性（#2）
2. **本迭代修复**（P1）：行级隔离加固（#3）、审计补全（#4,#5）、前端 edit key 修复（#6）、前端导入端点收敛（#7）、重复代码收敛（#9,#10,#11，已全部完成）
3. **下迭代规划**（P2）：DR-5 拆分（#23）、BR-4 函数拆分（#21 — 门禁已落地 2026-09-21，拆分为台账 B1/B2/B3 分批推进）、FR-6 composable 拆分（#22）、CI 依赖扫描（#8）
4. **持续改进**（P3）：前端结构化日志（#27）、死代码清理（#31,#32）、暗色模式补全（#45）

---

## 修复追踪

| # | 修复日期 | 改动文件 | 措施 | 验证 |
|:--|:---------|:---------|:-----|:-----|
| 1 | 2026-09-17 | `serializers/asset_crud_serializers.py` `AssetCreateSerializer` | ① 从 `Meta.fields` 移除 `asset_current_status`；② 移除 `extra_kwargs` 中的字符串默认值 `"in_store"`；③ 类 docstring 标注状态归 Service 注入（防回归） | — |
| 1 | 2026-09-17 | `services/asset_service.py` `create_asset` | 防污染拷贝 `dict(asset_data)` 之后、批量循环之前，注入 `asset_data["asset_current_status"] = Asset.AssetStatus.IN_STORE`（枚举引用，满足 B9） | — |
| 1 | 2026-09-17 | `tests/test_services.py` | ① `test_create_asset` 增加 DB 断言；② 新增 `test_create_asset_forces_initial_status`（传 `"scrapped"` → 落库 `in_store`，含调用方 dict 防污染断言）；③ 批量用例断言全部 `in_store` | `pytest test_services.py` 8 passed |
| 1 | 2026-09-17 | `tests/test_asset_view_api.py` | ① `test_create_asset` 移除状态字段并增加 DB 断言；② 新增 `test_create_asset_ignores_status_field`（POST `scrapped` → 201 + DB `in_store`） | `pytest test_asset_view_api.py` 32 passed |
| 5 | 2026-09-20 | `services/asset_service.py` `change_outasset_employee`/`transfer_asset_to_storage` | 签名追加可选 `operator_jobcode`/`operator_name`（排布对齐 `change_asset_status`）；`log_asset_update` 由硬编码 `None` 改为透传形参 | `pytest test_asset_service.py -k "change_employee or transfer"` 2 passed（先红 3 次：`None==adminuser` / `TypeError`） |
| 5 | 2026-09-20 | `views/asset_view.py` `change_outasset_employee` | View 调用补 `operator_jobcode=resolve_operator(request.user)[0], operator_name=resolve_operator(request.user)[1]`（复用既有 `:138-139` 写法） | `pytest test_asset_view_api.py` 32 passed |
| 5 | 2026-09-20 | `tests/test_asset_service.py` | `test_change_employee_success` 增 OperationLog operator 断言；新增 `TestTransferAssetToStorage`（transfer 方法首次覆盖，含 operator 审计断言） | 全量 1166 passed；ruff 0 错误；mypy 目标两文件 0 新增错误 |
| 6 | 2026-09-20 | `components/componentsdetails/detils/OutAssetForm.vue` `submitForm` | 编辑分支 `outAssetStore.update({ asset_recordcode: recordcode, ... })` → `{ recordcode, ... }`，与 `createEntityStore` idKey 契约对齐（审查报告原靶点遗漏此真实生产路径） | `npx vitest run .../OutAssetForm.spec.ts` 1 passed（先红 3 次：断言 `recordcode` 实收 `asset_recordcode`） |
| 6 | 2026-09-20 | `composables/useOutAssetForm.ts` `submitForm` | 同款键名 `asset_recordcode` → `recordcode`（该 composable 经确认为零生产调用方的遗留重复实现，按 DR-2 另立任务，本次仅同步修正） | `npx vitest run .../useOutAssetForm.spec.ts` 32 passed（先红 3 次：2 failed） |
| 6 | 2026-09-20 | `components/.../__tests__/OutAssetForm.spec.ts`（新增）、`composables/__tests__/useOutAssetForm.spec.ts` | 修正两处被 store mock 掩盖的错误断言 `asset_recordcode` → `recordcode`；新增组件级回归用例（真实组件 `submitForm` → 断言 update 收到 `recordcode` 且不含 `asset_recordcode`） | 相关套件 254 passed；`type-check`/`lint`/`format:check` 均通过 |
| 7 | 2026-09-20 | `components/componentsdetails/detils/AssetBatchImport.vue` `handleSubmit` | 删除内层手动 `request.post` + `res.data` 拆包，改为 `await assetStore.batchCreateAssets(apiDataList)`；删除 `:260` 误导注释；删除 `:146` 的 `request` import（端点仅在 `api/asset.ts:353` 定义，400 仍由 axios 拦截器 reject 透传） | `npx vitest run .../AssetBatchImport.spec.ts` 3 passed（先红 3 次：`batchCreateAssets` 未被调用） |
| 7 | 2026-09-20 | `components/.../__tests__/AssetBatchImport.spec.ts`（新增） | 组件级回归：mock store 后真实挂载 → 点提交，覆盖 ① 全成功（调 store、不调 request.post、成功提示、返回、刷新标记）② 部分失败按 `index` 行级错误 ③ 400 明细行级错误且不抛错 | 相关套件 225 passed；`type-check`/`lint`/`format:check` 均通过 |
| 9 | 2026-09-20 | `services/asset_service.py` | 新增 `_delete_guarded`（状态/出库/待报废守卫 + 审计 + 软删除的唯一实现，DR-1）；`delete_asset` 复用之；`batch_delete_asset` 手写循环整段收敛至 `BatchOperationMixin.batch_delete_execute`（与 B-5 其余 6 服务一致）；错误码 `HAS_OUTASSET_RECORDS` → `ASSET_HAS_OUTASSET`；顶层收编 `DamagedAsset/OutAsset` import、移除冗余 `MAX_BATCH_SIZE` | `pytest test_asset_service.py` 30 passed（先红 3 次：2 用例实收 `HAS_OUTASSET_RECORDS`） |
| 9 | 2026-09-20 | `tests/test_asset_service.py` | 新增 3 用例：批量出库失败码统一、混合成功/失败逐条独立、TOCTOU 锁内不可见 → `NOT_FOUND`（不泄漏底层码、不误伤守卫） | 全量 `pytest apps` 972 passed、整体覆盖率 84.16%（≥80%）；隔离/视图/快照套件 68 passed |
| 10 | 2026-09-20 | `selectors/damaged_asset_selector.py` | `get_asset_recordcode_for_update` 去掉 `with_asset_details()`：可空 `select_related` + `select_for_update` 在 PostgreSQL 抛 `NotSupportedError(FOR UPDATE ... nullable side of an outer join)`，改为仅锁主表单查询，并加禁止叠加 `select_related` 的说明注释 | 新用例先红：3 failed（`NotSupportedError`，实测 `... LIMIT 21 FOR UPDATE`）；修复后 3 passed |
| 10 | 2026-09-20 | `services/damaged_asset_service.py` | approve/reject/cancel 三处手写「filter + 判空 + select_for_update 重取」统一替换为 `DamagedAssetSelector.get_asset_recordcode_for_update`，`except DamagedAsset.DoesNotExist → AppValidationError(DAMAGED_ASSET_NOT_FOUND) from None`；错误码/文案逐字不变；未改任何方法签名 | `test_damaged_asset_service.py` + `test_damaged_asset_view_api.py` + selector 测试 54 passed |
| 10 | 2026-09-20 | `tests/test_recycle_damaged_waste_selector.py` | 复用既有 `TestDamagedAssetSelector` 新增 3 用例（命中加锁实例 / 缺失 DoesNotExist / 软删等同不存在），填补该 Selector 此前 0 调用方、0 覆盖 | 全量 `pytest apps` 975 passed、整体 84.17%；Service 层 95.15%（≥90） |
| 11 | 2026-09-20 | `apps/usermanagement/services.py` + `__pycache__/services.cpython-313.pyc` | 删除影子文件（277 行）及其编译产物。影子为纯不可达死代码：实测 `FileFinder.find_spec('services')` → 包 `__init__.py`（包优先解析）；10 个 import 落点（5 生产 + 5 测试）全落包；方法清单 diff 为包严格子集（EmployeeService 2 法 / DepartmentService 5 法，零独有逻辑）；包内 4 个 service 与既有未提交改动（`services/employee_service.py`、`tests/test_service_coverage.py`）不动 | 基线 `pytest apps/usermanagement -q` 99 passed → 删后 99 passed（净变化 0）；`ruff` 0→0 `All checks passed!`；`mypy` 仅存量 `models.py:122` 零新增；`manage.py check` no issues；符号冒烟 `usermanagement.services.__file__` = `services\__init__.py` |
| 12 | 2026-09-20 | `views/asset_view.py`（仅文档登记，零代码改动） | 核实：第 1 行已含 `# TECHNICAL_DEBT: >500 lines`（逐字命中 DR-5 存量豁免标准标注）⇒ 豁免成立；现 512 行（超限 2.4%）、28 个 `def`/20 个 `@action`、无巨型函数。报告「502 行 / 无标注」两处为过时描述。方案 B（写操作切 `_asset_mutation_actions.py`，主文件降至 ~290 行）留档为建议项 | 零代码改动，无门禁/测试变化；结论写入活账本 BF-018 |
| 13 | 2026-09-20 | `tests/test_state_machine.py`（仅追加，生产代码零改动） | 追加 10 用例补 5 类 CT-3 路径：`TestOutAssetTransition`（in_store/recycled_pending 双源 → in_use 参数化）、`TestCancelOutAsset`（参数化恢复 + 2 负路径：非法 previous_status / 非 in_use 源态，均 `InvalidTransitionError`）、`TestCancelRecycle`（+ 非 pending 负路径）、`TestMarkBrokenFromInStore`、`TestMarkLostFromInStore`；FSM 直调纯内存态断言（与既有 6 处一致，`core._transition` 不 save） | 定向 `pytest test_state_machine.py + test_out_asset_service.py` 62 passed（其中本文件 54 = 基线 44 + 10）；`test_out_asset_view_api.py` 13 passed 零回归 |
| 14 | 2026-09-20 | `tests/test_out_asset_service.py`（新建，163 行，8 用例） | CT-1 独立单测：① create 双源成功（previous_status 记录 + `original_*` 快照 + storage 清空 + in_use 流转）② 非法源态 `ILLEGAL_OUTASSET` ③ `MISSING_ASSET_CODE` ④ cancel 恢复契约（in_store → 恢复状态/仓库/原人员/原地点；recycled_pending → 不恢复仓库）⑤ 批量 fail_items 结构（create `ILLEGAL_OUTASSET`、delete `NOT_FOUND` + `id` 键） | 定向 62 passed（含本 8 用例）；API 回归 13 passed；ruf 新文件 `All checks passed!`；mypy 测试目录由 pyproject `exclude=(.*test.*)` 豁免 |
| 15 | 2026-09-20 | `services/damaged_asset_service.py` `create_damaged_asset`（PR-1） | TOCTOU 重排：① `select_for_update` 前置于行首（并发在资产行锁上串行化）；② RBAC `ensure_asset_visible` 移入锁内（锁后实例，杜绝旧快照越权判定）；③ 查重移至锁后（消除原 :46→:51 窗口 1）；④ `original_status` 在锁后以服务端权威覆盖式写入 `damaged_data`；⑤ 内层 `with transaction.atomic()` + `except IntegrityError`：命中 `asset_recordcode` 列 token → `AppValidationError(DUPLICATE_DAMAGED_RECORD) from exc`，其余 `raise`（复用 `hard_disk_sn_service.py:46-55` 既有兜底模式）；⑥ 删除原 :57-58 锁后回填 `save` | `pytest test_damaged_asset_service.py + test_damaged_asset_view_api.py` 40 passed；Service 覆盖率 95.24%（damaged_asset_service 100%） |
| 15 | 2026-09-20 | `models/damaged_asset.py` + `migrations/0023_*`（PR-2） | ① `asset_recordcode`：`OneToOneField` → `ForeignKey(unique=False)`（null/blank/PROTECT/related_name 保留，反向访问器零使用面已核实）；② `Meta.constraints` 增加 `UniqueConstraint(fields=["asset_recordcode"], condition=Q(is_deleted=False), name="uq_damaged_asset_active_asset")`（仅非软删行参与唯一，软删释放槽位）；③ CT-6 三步：makemigrations 仅 AlterFK+AddConstraint、`--dry-run` `No changes detected`、`migrate --plan` 顺序正确、`migrate` OK（dev 库 0 条无违反）—— AlterField 隐含 Drop 旧 OneToOne 唯一索引，已人工确认数据无损 | `python manage.py check` no issues；CT-6 ①②③ 全过；dev 库迁移成功 |
| 15 | 2026-09-20 | `services/damaged_asset_service.py` `reject_asset_recordcode`（PR-2） | 方案 X：状态回退 + 审计（`trigger="reject"`）与通知次序不变，追加 `damaged_asset.delete()` 软删释放唯一槽位（与 cancel `:297` 同语义，标注 `# [HALT]`）；返回实例内存态 `approval_status="rejected"` 供视图序列化；拒绝理由仅保留于 tombstone（DB 层可查、前端不可见，按用户定案写入文档） | `pytest` damaged 双套件 46 passed |
| 15 | 2026-09-20 | `tests/test_damaged_asset_service.py`（PR-2 新增 6 用例 + 兜底回归） | ① `test_create_integrity_error_mapped_to_business_code`（mock create 抛 IntegrityError → 业务码，复用 `test_hard_disk_sn_service_integrity.py:27-48` 既定 mock 约定）② `test_create_non_damaged_integrity_error_propagates` ③ 重提：`test_reapply_after_cancel_succeeds` / `test_reapply_after_reject_succeeds` ④ 软删+tombstone：`test_reject_soft_deletes_and_keeps_tombstone`（默认 manager 不可见 + `exists_by_asset_code` False + `all_objects` 可查 + 资产级审计 `触发: reject` 保留）⑤ `test_tombstone_and_active_coexist` ⑥ `test_approved_record_still_blocks_reapply` | 定向 46 passed（先红：cancel 逗号参数被误传 damaged recordcode / 陈旧实例 `is_deleted=False` 断言，均修正）；全量 `pytest apps` 1001 passed |
| 16 | 2026-09-20 | `views/damaged_asset_view.py` update/partial_update | 双方法改走 serializer 门禁：`self.get_serializer(obj, data=request.data, partial=(self.action == "partial_update"))` + `is_valid(raise_exception=True)` → `serializer.validated_data` 传 Service（原裸 `request.data` 直塞）；响应序列化器 `DamagedAssetUpdateSerializer(updated).data` 不变；partial_update 独立携带 partial=True 语义 | `test_damaged_asset_view_api.py`（既有 11 + 新 3）全绿 |
| 16 | 2026-09-20 | `views/out_asset_view.py` update/partial_update | 同款 serializer 门禁 + 补 `self.get_object()`（原直接按 `kwargs["recordcode"]` 调 Service，绕过行级 RBAC `get_queryset` 过滤，越权面关闭）；`partial_update` 不再转发 `update`，独立实现 `partial=True` | `test_out_asset_view_api.py`（既有 13 + 新 3）全绿 |
| 16 | 2026-09-20 | `serializers/out_asset_serializers.py` `OutAssetUpdateSerializer` | `fields` 追加 `outasset_using_location`（Gap A 决策：保留可写；Serializer 可写集 == Service 白名单 `OUTASSET_UPDATE_ALLOWED_FIELDS` 完全对齐，`FIELD_NOT_ALLOWED` 分支变不可达纯兜底） | 定向两文件 30 passed |
| 16 | 2026-09-20 | `serializers/damaged_asset_serializers.py` `DamagedAssetUpdateSerializer` | `extra_kwargs` 增 `asset_recordcode`/`is_active` 为 `read_only`（可写集收窄至 Service 白名单 3 字段；响应渲染不变，字段仍在响应 schema） | 定向两文件 30 passed |
| 16 | 2026-09-20 | `tests/test_damaged_asset_view_api.py` + `test_out_asset_view_api.py` | 新增 6 用例：负路径 ×2（`damaged_asset_number="abc"` / `outasset_date="bad-date"` → 400 DRF 错误体，不落 Service）；CT-4 锁定 ×3（damaged PUT 带 `is_active=false` → is_active 不被修改；out_asset `outasset_using_location` 更新成功；两端未知字段 `foo` 静默忽略无副作用）；既有 PUT/PATCH 单字段用例零回归（模型字段全 null/blank/default，无 required 拦截，已核验 `outasset_description`/`return_date` 均 `blank=True`） | 定向 30 passed（先红无：一次全绿；既有 24 + 新 6）；全量 `pytest apps` **1007 passed**；Service 覆盖率 95.24%（damaged_asset_service 100%） |
| 16 | 2026-09-20 | `api-schema-baseline.json` | M-3 重导（`spectacular --validate`）：`DamagedAssetUpdateRequest`/`PatchedDamagedAssetUpdateRequest` 移除 `is_active`+`asset_recordcode`（read_only 收窄，响应 schema 仍含两者）；`OutAssetUpdateRequest`/`PatchedOutAssetUpdateRequest` 新增 `outasset_using_location`（Gap A）；24 errors 均为 `public_scan_view`/auth/notification APIView `unable to guess serializer` 存量 | 重导成功，diff 与修改预期逐字段一致 |
| 17 | 2026-09-20 | `services/asset_lifecycle_mixin.py` | 新增 `@staticmethod` `batch_delete_lifecycle_asset(ids, delete_service_method, operator_jobcode="", operator_name="")` 与 `batch_delete_repair_asset(ids, ...)`（# [HALT]）：逐条 `transaction.atomic` 调既有 `delete_*_asset`；Broken/Lost/Found/RepairAsset `DoesNotExist` → `AppValidationError(NOT_FOUND, "记录 X 不存在")`；`REPAIR_IN_PROGRESS` 不包裹原样进 fail_items（不重定义业务文案，DR-1）；Exception 兜底经 `logger.error` 落盘后转 `INTERNAL_ERROR` | 定向 47 passed（含新 6 用例） |
| 17 | 2026-09-20 | `views/_lifecycle_base.py` `batch_delete` | 手写循环+错误分类+dict 组装整段收敛：改调 `batch_delete_lifecycle_asset` + `BatchResponseHelper.delete_response`（键集 total/success_count/fail_count/success_ids/fail_items 与全仓 8 端点逐字段对齐）；`if not ids` 守卫与 `缺少 ids 参数` 保留；docstring 契约注明中文标准模板 #17 定案 | grep 收尾：_lifecycle_base 内 success_ids/fail_items/BatchOperationMixin 零残留 |
| 17 | 2026-09-20 | `views/repair_asset_view.py` `batch_delete` | 同一反模式一并收敛（用户拍板）：改调 `batch_delete_repair_asset` + `BatchResponseHelper.delete_response`；移除内联 `_delete_one` 闭包与 `BatchOperationMixin` import；@extend_schema 示例 message 更新（success_ids 示例 `["RP001","RP002","RP003"]`） | `test_lifecycle_view_api.py` 全绿（含新冒烟） |
| 17 | 2026-09-20 | `tests/test_batch_contract_snapshot.py` | 2 断言随契约更新：`message == "批量删除完成,成功 1 条,失败 0 条"`、`error_message == "记录 NO_SUCH_RECORDCODE 不存在"`；文件头 + TestLifecycleBatchContract docstring 注明 2026-09-20 #17 定案 | 先红证据：git stash 反转旧英文断言对新代码 2 FAILED（实收中文新文案） |
| 17 | 2026-09-20 | `tests/test_asset_lifecycle_service.py`（新建） | 7 用例：broken 混合成功+NOT_FOUND、lost/found 参数化、101 条 → `BATCH_SIZE_EXCEEDED`；repair 成功软删+审计（completed 前置）、`REPAIR_IN_PROGRESS` 文案透传、NOT_FOUND 分类 | 定向 47 passed（含本文件全绿） |
| 17 | 2026-09-20 | `tests/test_lifecycle_view_api.py` | 新增 `TestRepairAssetViewSet.test_batch_delete` 视图冒烟：repair 成功（completed 前置）+ 外来 recordcode NOT_FOUND 混合、标准中文文案断言、`is_deleted=False` 不残留 | 视图套件全绿 |
| 18 | 2026-09-20 | `views/asset_view.py:182,190` | `url_path` 两处连写 camelCase → snake_case：`"getassetbyname/(?P<name>[^/.]+)"`→`"get_asset_by_name/(?P<name>[^/.]+)"`、`"getassetbyrecordcode/(?P<recordcode>[^/.]+)"`→`"get_asset_by_recordcode/(?P<recordcode>[^/.]+)"`；方法名 `get_asset_by_name`/`get_asset_by_recordcode` 不变（python 侧照旧 snake_case）；extend_schema 备注不动 | 先红基线：三测试文件 69 passed（老 URL 全绿） |
| 18 | 2026-09-20 | `views/contract_view.py:153` | 同类污点一并整改：`"getcontractByname/(?P<name>[^/.]+)"`→`"get_contract_by_name/(?P<name>[^/.]+)"`；方法名 `getcontractByname` 保留（reverse 名 `contracts-getcontractByname` 随方法名不变，`test_contract_view_api.py:140` 零改动） | 与上同批 |
| 18 | 2026-09-20 | `tests/test_asset_view_rbac.py:105,113` | 两条用例 URL 断言同步：`getassetbyname`→`get_asset_by_name`、`getassetbyrecordcode`→`get_asset_by_recordcode`（语义零变，仅路径迁移） | 三文件 69 passed |
| 18 | 2026-09-20 | `vue-assetmanagement/src/api/asset.ts:171,177` | 注释 + URL 模板同步 → `/assets/assets/get_asset_by_name/${...}/`；函数名 `getAssetByName` 不变（前端 TS 标识符 camelCase 合规） | `npx vitest run asset.spec.ts contract.spec.ts` 28 passed |
| 18 | 2026-09-20 | `vue-assetmanagement/src/api/contract.ts:80,86` | 注释 + URL 模板同步 → `/assets/contracts/get_contract_by_name/${...}/`；函数名 `getContractByName` 不变 | 与上同批 |
| 18 | 2026-09-20 | `src/api/__tests__/asset.spec.ts:78-80` + `contract.spec.ts:42-46` | 两处 it 描述与 URL 断言同步至新路径（先红后绿：改前运行确认旧 URL 断言实际命中再迁移） | 2 spec 28 passed |
| 18 | 2026-09-20 | `api-schema-baseline.json` | M-3 重导（`spectacular --validate`）：三处路径（`:1584` asset by name、`:1695` asset by recordcode、`:2818` contract by name）+ 对应 operationId 更新；剩余 24 errors 均为 `public_scan_view`/auth/notification APIView `unable to guess serializer` 存量（与 #16/#17 重导同集合），非本次引入 | 重导成功；JSON 有效（paths 183）；URL 相关行仅 12（6 对 -/+/operationId） |
| 19 | 2026-09-20 | `services/damaged_asset_service.py:80` | `to_state="damaged"` 字面量 → `to_state=Asset.AssetStatus.DAMAGED`（AuditLogger.log_state_change 参数；文件 :8 已 import Asset，同文件 :184 既有枚举先例，局部不一致消除） | 定向套件见下 |
| 19 | 2026-09-20 | `serializers/out_asset_serializers.py:208` | `OutAssetBatchItemSerializer` queryset 过滤 `asset_current_status="in_store"` → `Asset.AssetStatus.IN_STORE`（文件 :18 已 import Asset，B9 报告未列此命中，事实基线补充） | 与上同批 |
| 19 | 2026-09-20 | `selectors/asset_selector.py:116` | `get_available_assets`（DR-3 可用资产查询核心入口）`Q(asset_current_status="in_store")|Q(...="recycled_pending")` → `Asset.AssetStatus.IN_STORE`/`RECYCLED_PENDING`（B9 报告未列，事实基线补充） | 与上同批 |
| 19 | 2026-09-20 | `selectors/dashboard_selector.py:346` | dashboard 最近在用资产过滤 `asset_current_status="in_use"` → `Asset.AssetStatus.IN_USE`（B9 报告未列，事实基线补充） | 与上同批 |
| 19 | 2026-09-20 | `tests/test_damaged_asset_service.py` | 16 处状态字面量收敛为枚举引用：构造侧 7（`in_use`/`recycled_pending`/`broken`/`damaged`×4）+ 断言侧 9（`damaged`×4/`scrapped`/`broken`×2/`lost`/`in_use`）——仅本文件，其余 29 测试文件维持字面量（值等价零风险，用户裁断最小收敛） | `:335` 参数化 `expected_status` 非字面量不涉及 |
| 20 | 2026-09-20 | `config/settings/base.py:34-41` | SC-1 守卫：`SECRET_KEY` 定义后检测 `DJANGO_SETTINGS_MODULE == config.settings.base` 则 `raise ImproperlyConfigured`（base 为抽象基类禁止直接部署，模块加载即 fail-fast） | 先红后绿见下 |
| 20 | 2026-09-20 | `config/settings/base.py:212`（删） | 删除 `SIMPLE_JWT["SIGNING_KEY"] = SECRET_KEY` 物化键 → simplejwt `api_settings` 无用户键时回落 `DEFAULTS["SIGNING_KEY"] = settings.SECRET_KEY`（运行时解析），各环境已覆写 `SECRET_KEY` 故取真实密钥；修复"空密钥签名"根因（非仅 base 误配场景，dev/生产正常路径亦中招） | 见下 |
| 20 | 2026-09-20 | `config/settings/base.py:24-27,203-205` | 修正 :24 不实注释（原称"直接使用 base 将抛 ImproperlyConfigured"，实为空密钥静默可用）；新增守卫与 SIGNING_KEY 回落语义说明 | 见下 |
| 20 | 2026-09-20 | `config/tests/test_base_settings_guard.py`（新） | 4 条：守卫子进程断言非零退出+ImproperlyConfigured / development 导入不受影响 / SIGNING_KEY 回落 == settings.SECRET_KEY / 空密钥解码 token 抛 InvalidSignatureError | 先红 4 FAILED → 后绿 4 PASSED |
| 21 | 2026-09-21 | `scripts/check_function_length_guard.py`（新增） | BR-4 门禁：AST 语义节点扫描 `asset_management_backend/apps`（不含迁移/tests），逻辑行口径（物理跨度 − 空行 − `#`注释，docstring 计入代码行）>50 即超限；台账为唯一豁免源，双向断言"超限未登记即红、已拆分未移除即红" | `python scripts/check_function_length_guard.py` PASS（19 个超长函数 / 13 个文件全部登记）；负向实测：台账缺条目→FAIL、登记行数≤50→FAIL |
| 21 | 2026-09-21 | `Rules_Fiels/BR4_function_length_ledger.md`（新增） | 19 条台账（B1 状态机关键路径 6 函数 / B2 usermanagement+unregisteredasset 9 / B3 selectors+services 尾部 4），行号/函数名与 AST 扫描一一对应 | guard PASS（台账↔AST 一一对应） |
| 21 | 2026-09-21 | `asset_management_backend/pyproject.toml` | `lint.ignore` 显式加入 `PLR0915`（ruff 无函数**行数**规则，此为防御性关闭，防未来启用 PLR 时被存量淹没；避免双轨口径） | `ruff check` 目标目录 0 新增错误（存量 7 处均在 tests+loadtest，非本次引入） |
| 21 | 2026-09-21 | `.github/workflows/duplicate-guard.yml` | 追加 `function-length-guard` job，push/PR 均执行 `python scripts/check_function_length_guard.py`（stdlib-only，ubuntu-latest 直跑） | workflow 校验通过（checkout submodules: true） |
| 21 | 2026-09-21 | `Rules_Fiels/backend-business-rules.md` | v1.14 changelog：BR-4 实施方式固化（guard + 台账 + 计数口径说明），`[PATCH-BE]` 留痕（实施固化非规则文本修改） | 文档 diff 自审通过 |
| 21 | 2026-09-21 | `Rules_Fiels/BR4_function_length_ledger.md`（升格） | 台账升格：+3 列（拆分目标/回归测试锚/风险标注）+ B1 六条完整拆分设计（create_recycle_asset 归一化+双分支 DR-1 合并、create_outasset 快照/校验/落库三 helper、batch_delete_outasset↔_delete_one hoist+快照恢复、approve 最小 helper、move_department 层级/根移动）+ 头部嵌套计数口径与 hoist 约定；guard count 列保持第 5 列不破坏解析 | guard PASS（19 条数量不变）；负向 count 列改 45 → FAIL「台账条目非法 + 未登记」；B1 行号与 helper 区间（:53-93/:68-102/:100-160/:107-132/:230-288/:190-201/:167-189/:147-157）逐条对源码核对一致 |
| 21 | 2026-09-21 | 4 服务文件（`out_asset_service.py`/`recycle_asset_service.py`/`damaged_asset_service.py`/`department_service.py`） | B1 拆分落码：`create_outasset`→`_validate_outasset_source`/`_build_outasset_snapshot`/`_apply_outasset_to_asset`；`batch_delete_outasset`↔`_delete_one` **hoist** 至类级 staticmethod + `_restore_asset_fields`（快照恢复 loop 化）+`_resolve_snapshot_employee`；`create_recycle_asset`→`_normalize_recycle_input`（+`OUTASSET_ASSET_MISSING` 防御守卫）`_normalize_recycle_refs`/`_finalize_broken_or_lost`（broken/lost 双分支 DR-1 合并）；`approve_asset_recordcode`→`_notify_waste_approved`；`move_department`→`_validate_move_hierarchy`/`_move_to_root`；六函数全部 ≤50 行 | guard FAIL→PASS（先红：6 台账条目已拆未移除 + `_finalize_broken_or_lost` 51>50 → docstring 压缩后 PASS）；`pytest apps` **1017 passed**；Service 合计 96%（recycle 91 / out 90 / damaged 100 / department 100）；ruff 0；C90 0（`_restore_asset_fields` 13→7 loop 化、`_normalize_recycle_input` 11→9）；mypy 4 目标文件 0 新增（仅 pre-existing `asset_lifecycle_mixin`） |
| 21 | 2026-09-21 | `tests/test_recycle_asset_service.py` | 新增 `TestDefensiveBranches` 2 用例：① 二次 FSM 转换失败（mock `AssetFSM.mark_broken` 抛 `InvalidTransitionError` → `INVALID_STATE_TRANSITION`）② 出库记录无关联资产（mock Selector → `OUTASSET_ASSET_MISSING`）；CT-3/CT-4 配对，覆盖拆分引入的防御分支 | 21 passed；`recycle_asset_service` 覆盖率 89%→**91%** |
| 21 | 2026-09-21 | `Rules_Fiels/BR4_function_length_ledger.md` | B1 六行移除（create_recycle_asset / create_outasset / batch_delete_outasset / _delete_one / approve_asset_recordcode / move_department）；头部「19 处」→「13 处」+ B1 完成注记；剩余 13 行行号与 AST 复核零漂移 | guard PASS（13 函数 / 9 文件全部登记） |
| 21 | 2026-09-21 | `usermanagement` 4 方法 | B2 拆分落码（一）：`assign_role`（role_service）→ `_validate_assign_role_input`/`_apply_role_assignment`；employee `bind_auth_user`/`replace_auth_user`/`unbind_auth_user` 收敛为共享 helper（DR-1 唯一实现） | guard FAIL→PASS（先红：2 台账条目已拆未移除）；`tests/test_service_coverage.py` + `tests/test_role_service.py` 定向 21 passed；mypy 目标文件 0 新增 |
| 21 | 2026-09-21 | `unregisteredasset` services.py + handlers.py + views.py | B2 拆分落码（二）：handlers S1/S2/S3 拆分（共享 `_handle_s1_*`/`_handle_s3_*` 工厂）；services `create_unregistered`/`update_unregistered`/`approve_and_handle` 拆分（`_validate_create_scenario`/`_apply_whitelist_edits`/`_prepare_approval`/`_execute_handle` + 审计 helper `_log_create/update/approve_audit`）；新增 `batch_delete_unregistered`（委托 `BatchOperationMixin.batch_delete_execute`，NOT_FOUND/STATUS_NOT_ALLOWED error_code）；views `batch_delete` 收缩为薄封装并移除 `AppValidationError` import | guard FAIL→PASS（先红：services 3 行 + views 1 行已拆未移除）；`tests/test_handlers.py`(3) + `tests/test_batch_delete_view.py`(7) + `tests/test_services.py` + `tests/test_concurrent.py` 定向 49 passed；mypy 目标文件 0（views.py:137 存量 `type: ignore[union-attr]` 清理为仅 arg-type） |
| 21 | 2026-09-21 | `Rules_Fiels/BR4_function_length_ledger.md` | B2 九行移除（assign_role / bind / replace / unbind / handlers S1/S2/S3 / unregistered services 3 / views batch_delete）→ B2 完成注记；头部「13 处」→「4 处」；剩余 4 行全部为 B3（log_operation/create_asset_type/combine_search/mark_asset_lost），行号与 AST 复核零漂移 | guard PASS（4 函数 / 4 文件全部登记） |
| 21 | 2026-09-21 | `services/operation_log_service.py` `log_operation` | B3 拆分（一）：提模块级 `_validate_operation_params`（类型白名单 + 编码非空）+ `_insert_operation_log`（create + logger.info 成功 / logger.error 失败重抛），置于 `_to_json_safe` 后；docstring 压缩；逻辑行 65→36 | guard FAIL→PASS（先红：docstring 压缩前 52 行未触发 guard，压缩后命中台账）；`tests/test_operation_log_service.py` 35 + `tests/test_operation_log.py` 14 定向 49 passed；ruff 0；mypy 目标文件 0 |
| 21 | 2026-09-21 | `services/asset_type_service.py` `create_asset_type` | B3 拆分(二)：提模块级 `_resolve_parent_asset_type`（parent_type_code/parent 双解析 + PARENT_ASSET_TYPE_NOT_FOUND）+ `_compute_level_path`（层级/物化路径 + MAX_ASSET_TYPE_LEVEL 上限校验）；逻辑行 59→36 | guard FAIL→PASS（先红命中台账）；`tests/test_asset_type_service.py` 23 passed；ruff 0；mypy 目标文件 0 |
| 21 | 2026-09-21 | `selectors/asset_selector.py` `combine_search` | B3 拆分（三）：提模块级 `_build_fuzzy_q`（FIELD_NAME_MAPPING + AND 组合，返回 Q|None）+ `_apply_exact_filters`（asset_type 双尝试 / category 展开 / 空集 none()）；`queryset` 显式注解 `QuerySet[Asset]` 防重新赋值类型冲突；逻辑行 55→18 | guard FAIL→PASS（先红命中台账）；三锚 `test_asset_selector` + `test_asset_view_api:test_search_assets` + `test_asset_view_rbac` 60 passed；mypy 新暴露 :390 `Incompatible types in assignment` 已修复；ruff 0 |
| 21 | 2026-09-21 | `services/asset_lifecycle_mixin.py` `mark_asset_broken`/`mark_asset_lost` + 两闭包 | B3 拆分（四）+ 双胞胎统一（DR-1/B-23 关闭）：抽模块级 `_run_lifecycle_transition`（幂等分支/FSM/子记录/审计收敛，六差异经 `target_status`/`fsm_method`/`record_model`/`record_kwargs`/`operation_type`/`audit_description` 注入）；mark_asset_broken 21 / mark_asset_lost 26 行；保留 `AssetOperationLog.objects.create`（不走 AuditLogger）；闭包注解 :316/:340 → `BrokenAsset`/`LostAsset`（26→24）；helper 返回 Any 引发 2 条 no-any-return 经 `cast` 消除 | guard FAIL→PASS（先红命中台账）→ B3 全拆 guard 0/0；`test_asset_lifecycle` + `test_state_machine` 78 passed（lost 幂等 :99 锚 + FSM :130 锚原样）；ruff 0；mypy 目标文件 0；duplicate invariants PASS |
| 21 | 2026-09-21 | `Rules_Fiels/BR4_function_length_ledger.md` | B3 四行移除（log_operation / create_asset_type / combine_search / mark_asset_lost）→ BR-4 存量清零（guard 0/0）；头部「4 处」→「0 处」+ B3 完成注记 | guard PASS（0 函数 / 0 文件） |
| 21 | 2026-09-21 | `unregisteredasset/services.py` `_safe_call_audit` + `Rules_Fiels/Duplicate_Codes/complete-patterns.md` | C10 收尾①（B-22 关闭）：新增模块级 `_safe_call_audit(operation, *args, **kwargs)` 唯一 try/except + `getattr(UnregisteredAssetAuditAdapter, f"log_{operation}")` 分派；三 `_log_*_audit` helper 改薄委托、delete 内联替换为直接调用；`审计日志记录失败` 文案全仓唯一 :96 | `rg "审计日志记录失败"` 仅 1 处命中；`pytest apps/unregisteredasset` 86 passed；guard 0/0；duplicate invariants PASS；ruff 0；mypy 目标文件 0 |

**#21 验证结果（B1 拆分落地）**：六函数全部按台账设计拆分至 ≤50 行（含嵌套 `_delete_one` hoist 至类级 staticmethod，外层 `batch_delete_outasset` 仅剩编排 10 行）；DR-1 双分支合并——`create_recycle_asset` broken/lost 两分支收敛为 `_finalize_broken_or_lost`（trigger/to_state/子记录类逐字段与合并前一致，`test_recycle_asset_service.py` broken/lost 审计断言原样通过）。先红实证：guard 拆分未更新台账时 FAIL（6 条「已拆分未移除」+ `_finalize_broken_or_lost` 51 行未登记）→ 同提交台账移除后 PASS。门禁：全量 `pytest apps` **1017 passed**（assetmanagement 724 + usermanagement 99 + unregisteredasset 194）、Service 层合计 **96%**（≥90%，四个改动模块 recycle 91 / out 90 / damaged 100 / department 100）；`ruff check` 4 服务 + 测试文件 `All checks passed!`；`ruff --select C90` 0；`mypy` 4 目标文件 0 新建（存量 `asset_lifecycle_mixin` 6 errors 与改动文件无关）；`guard` PASS。跨端契约：纯服务层内部重构，API 响应/端点/状态枚举/schema 零变化，`api-schema-baseline.json` 无需重导出。

**#21 验证结果（B2 拆分落地）**：13 函数全部按台账设计拆分至 ≤50 行（usermanagement 4：assign_role/bind/replace/unbind；unregisteredasset 9：handlers S1/S2/S3 + services create/update/approve + views batch_delete），其中审计留痕块已部分收敛（B-22 登记，create/update/approve 提为 `_log_*_audit` helper）；新增 `batch_delete_unregistered` 使视图层仅剩空列表/超限双 400 守卫 + resolve_operator + 委托（AppValidationError import 移除）。先红实证：guard 拆分未更新台账时 FAIL（「已拆分未移除」逐条命中）→ 同提交台账移除后 PASS。门禁：`pytest` usermanagement+unregisteredasset 定向 49 passed + 全量 assetmanagement 726 + usermanagement 99 + unregisteredasset 194；`ruff` 0；`mypy` 目标文件 0（全仓 26 存量零新增，views.py:137 存量 ignore 清理）；`guard` PASS 4/4。跨端契约：批量删除消息模板沿用 #9/#17 中文定案，视图 400 守卫契约（空列表/超限文案）不变，`api-schema-baseline.json` 无需重导出。

**#21 验证结果（B3 拆分落地 + B-22 收尾）**：B3 四函数全部按台账设计拆分至 ≤50 行（log_operation 65→36 / create_asset_type 59→36 / combine_search 55→18 / mark_asset_lost 52→26），台账四行移除后 **guard 0/0，BR-4 存量清零**；其中 mark_asset_broken/lost 双胞胎（B-23）同步统一为 `_run_lifecycle_transition`（六差异参数注入，mark_asset_broken 21 / mark_asset_lost 26 行，保留 `AssetOperationLog.objects.create` 不走 AuditLogger）转 **A-24 关闭**，闭包注解 :316/:340 修正使 mypy 全仓存量 26→20（helper 新增 2 条 no-any-return 经 `cast` 消除）。双胞胎统一过程中 `select_related` 后 `queryset` 重新赋值触发的新暴露 mypy error（`asset_selector.py:390 Incompatible types in assignment`）已显式注解修复，非存量。C10 收尾①：B-22（unregisteredasset 审计留痕 ×4）收敛为模块级 `_safe_call_audit`（唯一 try/except + `getattr` 分派，三 `_log_*_audit` helper 薄委托、delete 内联转调），`审计日志记录失败` 文案全仓唯一，转 **A-25 关闭**，重复代码账本 **B 区清空**（complete-patterns 升 v2.9.30）。先红实证：guard 拆分未更新台账时逐条 FAIL → 同提交台账移除后 PASS。门禁：全量 `pytest apps` **914 passed**（assetmanagement 729 + usermanagement 99 + unregisteredasset 86）；定向锚 23/60/78/49 passed；`ruff check .` 7 errors 全为存量（loadtest/notification）；`mypy` 全仓 20 errors 全存量（本批三文件 0）；`guard` 0/0；`duplicate invariants` PASS。跨端契约：纯服务层内部重构，API 响应/端点/状态枚举/schema 零变化，`api-schema-baseline.json` 无需重导出。

**#18 验证结果**（B2 URL 命名一致性）：后端定向三文件 **69 passed**（先红基线 69 → 改后 69，含 RBAC scoped 两条迁移断言）；assetmanagement 全模块 **724 passed** 零回归；`manage.py check` no issues；`ruff check` 3 文件 `All checks passed!`；`mypy` 目标 2 文件 6 errors 全部位于 `asset_lifecycle_mixin.py`，经任务前基线（`baseline-be.patch` 3223 行 diff）判定为 **pre-existing**（#5~17 工作区未提交态所致，非本次引入）。前端：api 定向 2 spec 28 passed；相关套件（asset/contract/useAssetFormHelpers/contractStore/assetStore）**104 passed**；`npm run type-check` 0 错；`eslint` 4 文件干净。grep 收尾：`getassetbyname` 后端 py/json 0（前端 src 0，docs 已同步）；`getassetbyrecordcode` 仅活账本 D-5 历史登记 + 测试方法名/注释（非 URL）；`getcontractByname` 仅后端方法名 `:154` 与测试 reverse 引用（非 URL，按用户留档决策保留）。跨端契约：URL 变更属破坏性（operationId/路径），前后端同批已定；schema 已重导随改随交，CI `api-schema-check` 需人工放行。

**#19 验证结果**（B9 枚举约束收敛）：定向 6 套件（test_damaged_asset_service / test_asset_service / test_asset_selector / test_selectors / test_dashboard_view_api / test_outasset_selector）**145 passed**；assetmanagement 全模块 **724 passed** 零回归；`ruff check` 5 改动文件 `All checks passed!`；`mypy` 同一 4 源文件 20 errors 全部为**既有存量**（dateutil stubs / var-annotated 字段声明，经 pre-fix 副本独立运行比对新引入 0 条，`asset_selector.py`/`damaged_asset_service.py` 零 error）。抗舆审核：等价性——TextChoices 成员与字符串值等价，`==`/filter/构造三语义零变化；`to_state="<状态>"` 生产字面量全仓归零、`asset_current_status="<状态>"` 生产归零。覆盖淨：本修改纯标识符替换不改语句/分支数量，覆盖率数学上不变（[PENDING] 标记，CI 未配置）。豁免留档（非本规则射程）：`state_machine/constants.py` `AssetState` 为与 `Asset.AssetStatus` 值平行的第二套枚举（DR-1 更大矛盾面，需架构决策另立任务）；`batch_serializers.py:100` 仓库类型 choices、`operation_log_service.py:306` 记录类型字段、`operation_log_views.py:65` API enum 参数均非资产状态语义；`from_state=` 字面量全仓零命中。契约：纯内部等价重构，响应/端点/状态枚举/schema 零变化，`api-schema-baseline.json` 无需重导出。

**#20 验证结果**（SC-1 空密钥签名修复，方案 1 运行时回落）：先红实证——修复前 `api_settings.SIGNING_KEY=''`，development 下空密钥可成功 decode 任意 token（`jwt.decode(raw, key='')` 返回 user_id，伪造可行）；守卫测试 4 条全 FAILED。后绿——`SIMPLE_JWT` 无 SIGNING_KEY 键，`api_settings.SIGNING_KEY` == `settings.SECRET_KEY`（dev key 非空），空密钥 decode 抛 `InvalidSignatureError`，dev key decode 成功。门禁：`config/tests` **19 passed**（含新 4 条 + production 12）、authusermanagement 全量 **80 passed**（含双通道 38，JWT 最相关）、assetmanagement **724 passed**、usermanagement/unregisteredasset/notification **211 passed**；`manage.py check` no issues；`ruff check` 改动文件 `All checks passed!`；`mypy config apps core utils` 23 errors in 10 files 经 **git stash 前后比对完全一致**（改动前亦 23/10），`config/settings/base.py` 零 error → 本次零新增。对抗审核结论：① 时序正确——simplejwt `api_settings`（`settings.py:11` 快照 `USER_SETTINGS`）无 `SIGNING_KEY` 键时 `__getattr__` 回落 `DEFAULTS["SIGNING_KEY"]`（`settings.py:20` = `settings.SECRET_KEY`，在 settings 完整加载后求值），development/production/test 三环境实证回落正确；② 无越界——改动仅 base.py（删 1 键 + 守卫 + 注释）+ 新增测试文件；③ 依赖面确认——全仓仅 `authusermanagement/services.py:206` 读 `settings.SIMPLE_JWT`（取 ROTATE/BLACKLIST 不读 SIGNING_KEY），测试用 `**settings.SIMPLE_JWT` 展开后无键同样回落，语义不变；④ 无 migration、无 `[HALT]`、跨端契约零变化，`api-schema-baseline.json` 无需重导出。残余风险留痕：`DJANGO_SETTINGS_MODULE` 未设置的纯脚本直接 `import config.settings.base` 不触发守卫（保留给工具脚本场景），但此时然不签发 token,风险面为零。

**#18 建议留档（非阻塞）**：`contract_view.py` 方法名 `getcontractByname` 为 camelCase，是全仓方法名命名违例孤立样本（asset 侧方法名均已 snake_case），本次明确不改；后续若统一改名会变更 reverse 名（`contracts-getcontractByname`），需同步测试 `test_contract_view_api.py:140`，届时一并处理。

**修复设计决策**：CreateSerializer 未加 `StrictUnknownFieldMixin`，未知字段采用「静默忽略 + Service 强制兜底」（与 Update 侧显式 400 区分）。依据：前端 create/import 链路均未实际提交该字段（已静默丢弃），保守策略最安全。

**验证结果**：全量 655 passed；Service 层覆盖率 94.23%（≥90%）；`ruff check` 通过；`api-schema-baseline.json` 已重导出（create 端点写字段移除）。

**#5 验证结果**：全量 1166 passed（较基线 1165 净增 1 条 transfer 用例）；`ruff check` 3 文件 0 错误；`mypy` 目标两文件 0 错误（其余 22 错误为既有工作区改动所致，非本次引入）；`api-schema-baseline.json` 无需重导出（请求/响应形状与跨端契约不变，仅 OperationLog.operator 由 NULL 变有值）。

**#6 验证结果**：前端相关套件 254 passed（含新增组件回归 `OutAssetForm.spec.ts` 1 passed、`useOutAssetForm.spec.ts` 32 passed）；`npm run type-check`、`npm run lint`、`npm run format:check` 三项均通过；改动为前端请求体内部键名，未触及后端端点、响应形状与跨端契约，`api-schema-baseline.json` 无需重导出。

**#7 验证结果**：新增组件回归 `AssetBatchImport.spec.ts` 3 passed（先红 3 次：修复前 3 failed，均为 `assetStore.batchCreateAssets` 未被调用）；相关套件 225 passed（`detils/__tests__/` + `useAssetBatchImport.spec.ts` + `assetStore.spec.ts`，10 files）；`npm run type-check`、`npm run lint`、`npm run format:check` 三项均通过（`format:check` 首轮仅新增 spec 报格式 → `prettier --write` → 全绿）；改动为前端请求路径收敛，端点、响应形状与跨端契约不变，`api-schema-baseline.json` 无需重导出。

**#9 验证结果**：`pytest apps` 972 passed、整体覆盖率 84.16%（≥80%）；`test_asset_service.py` 30 passed（先红 3 次：实收 `HAS_OUTASSET_RECORDS`）；隔离 + 视图 + 批量契约快照 68 passed；Service 层覆盖率 95.00%（`asset_service.py` 96%，≥90%）；`ruff check apps/assetmanagement core` 0 错误；`mypy apps/assetmanagement core --strict` 目标文件 0 错误（其余 10 条为既有存量、非本次引入）；`rg HAS_OUTASSET_RECORDS`（`*.py`）0 残留；重复护栏 `scripts/check_duplicate_invariants.py` PASS。对外可见变化：批量 `fail_items[].error_code` 由 `HAS_OUTASSET_RECORDS` 变为 `ASSET_HAS_OUTASSET`、批量出库失败 `error_message` 统一为单条文案；前端不消费 `error_code`（G-4 无需注册），响应结构/端点/状态枚举不变，`api-schema-baseline.json` 无需重导出。

**#10 验证结果**：`pytest apps` 975 passed、整体覆盖率 84.17%（≥80%）；`test_recycle_damaged_waste_selector.py` + `test_damaged_asset_service.py` + `test_damaged_asset_view_api.py` 54 passed；Service 层覆盖率 95.15%（≥90%）；`ruff check` 变更 3 文件 0 错误；`mypy --strict` 变更两文件 0 错误（8 条均为 `asset_lifecycle_mixin.py`/`notification` 存量）；`rg "asset_recordcode__recordcode=" apps/assetmanagement/services` 0 残留（Selector 内 1 处为唯一实现）。新用例先红证据：修复前 3 failed，实收 `django.db.utils.NotSupportedError: FOR UPDATE 不能应用于外连接的可空侧`（SQL `... LIMIT 21 FOR UPDATE`）。对外契约零变化（错误码、detail、响应结构全保留，方法签名未动），`api-schema-baseline.json` 无需重导出。附带效果：消除原「存在性检查 → 加锁」两步之间的 TOCTOU 窗口。

**#11 验证结果**：`pytest apps/usermanagement -q` 99 passed（基线 99，净变化 0）；`ruff check apps/usermanagement` 基线/删后均 `All checks passed!`（0→0）；`mypy apps/usermanagement --strict` 1 处存量 `models.py:122`、零新增；`python manage.py check` 冒烟 no issues。符号冒烟：`apps.usermanagement.services.__file__` = `services\__init__.py`，EmployeeService/DepartmentService 方法全集覆盖影子（影子 2+5 方法均为包属）。契约零变化：纯删除不可达文件，未动响应/端点/状态机/枚举，`api-schema-baseline.json` 无需重导出。附带：全仓 `apps/usermanagement` 下 `services.py` 无残留、`.github/workflows` 与 pyproject/setup.cfg 零引用；`services.py:527/182` 等旧行号属更老 527 行版本快照。建议项（未实施）：`scripts/check_duplicate_invariants.py` 补一条 `services.py` 不复现检查（仿 G-2 的 `Path.exists()`，约 5 行）。

**#12 验证结果**：核实完成，零代码改动。`views/asset_view.py` 第 1 行已含 `# TECHNICAL_DEBT: >500 lines`（逐字命中 DR-5 存量豁免标注），现 512 行（超限 2.4%）、28 个 `def`/20 个 `@action`、无巨型函数。报告「502 行 / 无标注」两处为过时描述。方案 B（按 URL 职责切 `_asset_mutation_actions.py`，主文件降至 ~290 行）留档为建议项：下一次对该文件新增 ≥50 行的功能改动时执行（DR-5 同步拆分条款将其设为强制边界），届时补全量回归（`test_asset_view_api.py`/`test_asset_view_rbac.py`/`test_asset_selector_isolation.py`）与 schema 装饰器位置核对。

**#13 验证结果**：`test_state_machine.py` 追加 10 用例（54 = 基线 44 + 10），覆盖 outasset 双源→in_use、cancel_outasset 恢复+负路径、cancel_recycle、in_store→broken、in_store→lost。门禁四项全过：① 定向 `pytest test_state_machine.py + test_out_asset_service.py` 62 passed；② `test_out_asset_view_api.py` 13 passed 零回归；③ Service 层 `--cov=apps.assetmanagement.services` **95.22%**（≥90%），顺序全量 1190 passed；④ ruff 新增/改动文件 0 错误。生产代码零改动、FSM 零改动、跨端契约零变化，`api-schema-baseline.json` 无需重导出。真实缺口确认（报告原清单准确）：此前争论的 `TestMarkBrokenFromInUse`/`TestMarkLostFromInUse` 源态为 `in_use`（旧 :99/:122），非报告所指缺口；`VALID_TRANSITIONS`（`transitions.py:22`）声明的 in_store→broken、in_store→lost 与 outasset/cancel_outasset/cancel_recycle 确实为零覆盖。附环境说明：曾与全量 pytest 并行跑覆盖导致共享测试库冲突（1096 条无关 ERROR），顺序执行后全部恢复。

**#14 验证结果**：新建 `tests/test_out_asset_service.py`（8 用例）覆盖 create 双源成功+`original_*` 快照+仓库清空与 in_use 流转、`ILLEGAL_OUTASSET`（in_use 源态）、`MISSING_ASSET_CODE`、cancel 恢复契约（in_store 才恢复仓库）、批量 fail_items 结构（create `ILLEGAL_OUTASSET` / delete `NOT_FOUND`+`id` 键，与 `batch_mixins.py:108-218` 逐字段对齐）。`OutAssetService` 无 cancel 方法——取消走 `batch_delete_outasset`（`out_asset_service.py:199-299`，`_delete_one` 内 `AssetFSM.cancel_outasset` + 快照恢复）——报告「out_asset_service 仅间接覆盖」成立。门禁：定向 62 passed、API 回归 13 passed、Service 覆盖率 95.22%（≥90%）、ruff 0。生产代码零改动、契约零变化，`api-schema-baseline.json` 无需重导出。测试目录已由 mypy 配置豁免（pyproject `exclude`），mypy 门禁不受影响。

**#15 验证结果**：双 PR 落地。PR-1 门禁：`pytest test_damaged_asset_service.py + test_damaged_asset_view_api.py` 40 passed；Service 层覆盖率 95.24%（≥90%，`damaged_asset_service.py` 100%）；ruff 变更 4 文件 `All checks passed!`、mypy 目标文件 0 新增错误（11 条为既有存量：authusermanagement/usermanagement models 与 dateutil/lifecycle_mixin，非本次引入）；C90 复杂度通过。PR-2：模型唯一约束改为部分唯一（`is_deleted=False` 参与），OneToOne→FK 反向访问器零使用面、dev 库 0 条无违反（AlterField 隐含 Drop 原唯一索引，CT-6 ③ 已人工确认数据无损）；`manage.py check` no issues；reject 软删后重提成功（先红证据：初版测试误传 damaged recordcode 至 cancel/reject 形参、陈旧实例断言 `is_deleted=False`，均已修正）。门禁汇总：定向 46 passed、邻域套件（recycle/waste/out/recycle_service/lifecycle/selector）95 passed、全量 `pytest apps` **1001 passed**、Service 覆盖率 95.24%（≥90%）、重复护栏 `scripts/check_duplicate_invariants.py` PASS。方案 X 语义：驳回/取消记录软删释放唯 一槽位、审批人/拒绝理由保留 tombstone（`all_objects` 可查）、资产级 `AssetOperationLog(trigger=reject)` 审计完整；UI 侧「已拒绝」记录自列表/统计/by_asset/导出消失为既定取舍（用户拍板）。跨端契约零变化（响应形状/端点/状态枚举不变），`api-schema-baseline.json` 无需重导出。附对抗审核登记：`create_damaged_asset` 的 IntegrityError→业务码兜底与 `hard_disk_sn_service.py:46-55` 同构，为全仓第二实例，按 §1.8 新发现义务已登记活账本待核查（建议后续提取公共兜底 helper，本次未超规格改动）。

**#16 验证结果**（B8 输入门禁修复）：双 View update/partial_update 由裸传 `request.data` 改走 DRF serializer 输入校验（`is_valid(raise_exception=True)` → `validated_data`），对齐全仓既有模式（`asset_view.py`/`hard_disk_sn_view.py`/`recycle_asset_view.py`）。对抗审核结论：① RBAC——out_asset update/partial_update 补 `self.get_object()`，行级过滤缺口关闭（此前任意有写权限者可跨数据范围写任意 recordcode）；② 白名单对齐——Serializer 可写集与 Service 白名单 `OUTASSET_UPDATE_ALLOWED_FIELDS` 逐字段一致，`FIELD_NOT_ALLOWED` 分支变不可达纯兜底（契约变更：未知字段 400 → 200 静默忽略，测试已锁定新语义）；③ 前端零依赖——`is_active`/`asset_recordcode` 仅在 types/响应侧引用，无 PUT 载荷提交，收窄只读无破坏；④ C90 复杂度（默认 max-complexity 10）通过；⑤ mypy 22 错误中仅 2 条落在改动文件且为 `OutAssetBatchItemSerializer` 存量 var-annotated（216/219 行，非本次编辑区），视图与响应序列化器 0 错误。门禁：既有 view 套件 24 → 新 6 用例后 **30 passed**；全量 `pytest apps` **1007 passed**、Service 覆盖率 **95.24%**（≥90%，damaged_asset_service 100%；out_asset_service 83% / recycle_asset_service 88% 为存量单模块缺口、本次仅动 View/Serializer 未触及）；ruff 6 文件 `All checks passed!`；`manage.py check` no issues；重复护栏 `scripts/check_duplicate_invariants.py` PASS；`rg update_data=request.data` 全仓 0 残留。跨端契约：响应形状/端点/状态枚举不变；请求 schema 变更（见修复追踪 `api-schema-baseline.json` 行）已按 M-3 重导随改随交，非破坏。

**#17 验证结果**（§1.2 分层违例修复）：三视图集（broken/lost/found）与 repair 的 batch_delete 编排全量下沉 Service 层，View 仅保留参数守卫与 `BatchResponseHelper.delete_response` 响应组装。行为变更清单（用户 2026-09-20 拍板，已留痕）：① lifecycle 5 端点批量删除 message `Batch delete done: X success, Y fail` → `批量删除完成,成功 X 条,失败 Y 条`（与全仓 13 端点对齐，B5 基线锁定）；② repair message 静态文案 → 动态标准模板；③ repair 缺失记录 `INTERNAL_ERROR`/`Server error` → `NOT_FOUND`/`记录 X 不存在`（与 damaged/waste B5 同向）；④ lifecycle >100 条原静默全删 → 400 `BATCH_SIZE_EXCEEDED`（与全仓批量上限契约一致）；⑤ Exception 兜底补 `logger.error`（OC-2 结构日志透传）。门禁：定向 3 套件 **47 passed**（含新 7 服务用例 + 1 视图冒烟，次修 repair fixture 默认 in_progress → completed 前置）；先红证据——git stash 反转旧英文契约断言对本次新代码 2 FAILED（实收中文新文案），恢复后全绿；邻域套件（状态机 + 五类生命周期 selector）47 passed；全量 `pytest apps` **1015 passed**（较 #16 的 1007 净增 8）；Service 覆盖率 **95.32%**（≥90%）；ruff 含 C90 0 错误；mypy 3 源文件 **0 新增**（22 条错误全为依赖模块/存量方法行 50/85/114/151/317/341，非本次编辑区；`_lifecycle_base.py` 与 `repair_asset_view.py` 零错误）；`manage.py check` no issues；重复护栏 `scripts/check_duplicate_invariants.py` PASS。合同快照责任：`RepairAssetViewSet` 此前零批量删除覆盖，本次补冒烟；b5 基线文件（`test_b5_baseline_snapshot.py`）内标准文案未改动。跨端契约：响应形状/端点/状态枚举/错误码全不变，仅 message 文本与缺失记录错误分类对齐既有约定；`api-schema-baseline.json` 已按 M-3 重导（非破坏性描述变更）。遗留留档（不进本次范围）：`delete_*_asset` 系列无 user/RBAC 参数（单删行级守卫仍依赖 View `get_object()`）；destroy 硬删 vs batch 软删语义不对称；broken/lost/found 无 BatchDeleteSerializer，`if not ids` 守卫与 repair 的 serializer 门禁不对称，建议后续统一入口。CT-6 不适用（本次无迁移变更）。
