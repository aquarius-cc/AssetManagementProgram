> ⚠️ 安全整改 2026-08-26：本文件原含明文凭据，已按根级规范 SC-1 脱敏；相关凭据已轮换失效。

# 资产管理系统 — 全面审查报告（第八轮）

> **审查 Agent**: opencode (big-pickle)  
> **审查日期**: 2026-08-26  
> **审查范围**: 代码质量、环境配置、前后端协同、安全、功能数据、性能部署、监控运维（七维全覆盖）  
> **审查依据**: AGENTS.md v3.5.0 + Rules_Fiels 全部规范文件

---

## 一、代码与质量审查

### 1.1 依赖审查

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| 后端依赖版本锁定 | ✅ | `requirements/base.txt` 全部使用 `==` 精确锁定（21个包） |
| `sentry-sdk` 未精确锁定 | ⚠️ | 使用 `>=2.0,<3.0` 范围约束，其余包均为精确版本 |
| **`pyproject.toml` 版本漂移** | 🔴 | `pyproject.toml` 声明 Django==6.0.5 / Pillow==11.3.0 等旧版本，与 `requirements/base.txt` 不一致（后者含 CVE 修复）。`pip install -e .` 会安装旧版有漏洞版本 |
| 前端依赖 | ✅ | `package.json` 使用 `^` 语义化版本，`uuid` 通过 overrides 锁定 |
| npm audit | ✅ | CI 流水线配置了 `npm audit --audit-level=high`（`security-scan.yml`） |
| pip-audit | ✅ | CI 流水线配置了 `pip-audit --fail-on=high` |

**修复建议**：同步 `pyproject.toml` 与 `requirements/base.txt` 的版本号，或将 `sentry-sdk` 精确锁定。

### 1.2 调试代码审查

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| 后端 `print()` | ✅ | 仅在独立脚本（`clear_database.py`、`run_graphify.py`）中使用，业务代码无 `print()` |
| 后端 `breakpoint/pdb` | ✅ | 未发现 |
| 前端 `console.log` | ⚠️ | `useUserExcelExport.ts` 中有 3 处遗留调试 `console.log`（Vite 生产构建 `terser drop_console:true` 会剥离，但源码应清理） |
| 前端 `debugger` | ✅ | 未发现 |
| 前端 `alert()` | ✅ | 未发现（仅测试文件中有 XSS 测试用例） |

### 1.3 TypeScript 类型与代码规范

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| TypeScript 严格模式 | ✅ | 全部 strict 标志已启用（`noImplicitAny`、`strictNullChecks` 等） |
| 类型定义覆盖 | ✅ | `src/types/` 下 30 个类型文件，覆盖所有业务实体 |
| ESLint | ✅ | flat config 格式，包含 `vue-ts/recommended` + `no-unused-vars`（`_` 前缀豁免） |
| Prettier | ✅ | 统一格式化（无分号、单引号、100 字符宽） |
| **路由文件超限** | ⚠️ | `router/index.ts` 737 行，超过 DR-5 的 500 行上限（已有 `TECHNICAL_DEBT` 标注） |
| **`createEntityStore.ts` 接近上限** | ⚠️ | 496 行，距 500 行红线仅差 4 行 |

---

## 二、环境与配置审查

### 2.1 环境隔离

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| Django 环境路由 | ✅ | `DJANGO_ENV` 环境变量路由到 `development.py` / `production.py` / `test.py` |
| 前端环境文件 | ✅ | `.env.development`、`.env.production`、`.env.production.test` 分离 |
| 生产配置 `DEBUG=False` | ✅ | 硬编码，不可覆盖 |
| 测试用 SQLite | ✅ | `test.py` 使用 `:memory:` SQLite，与生产 PostgreSQL 隔离 |

### 2.2 敏感信息

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| 后端 `.env` 含真实密码 | ⚠️ | `DB_PASSWORD=********（已脱敏，凭据已轮换失效）********`、`REDIS_PASSWORD` 明文存在。虽 `.gitignore` 排除了 `.env`，但需**确认从未被 git 提交** |
| 前端 `.env.development` 含密钥 | ⚠️ | `VITE_TOKEN_CRYPTO_KEY=********（已脱敏，凭据已轮换失效）********` 已被 git 跟踪（`.gitignore` 是后添加的） |
| 生产代码硬编码密钥 | ✅ | **零发现**。所有生产路径均从环境变量读取 |
| 开发 fallback 密钥保护 | ✅ | `development.py` 含 `_INSECURE_KEYS` 黑名单校验，拒绝弱密钥 |

### 2.3 配置校验

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| 后端生产启动校验 | ✅ | 缺少 `SECRET_KEY`/`ALLOWED_HOSTS`/`DB_PASSWORD`/`REDIS_URL` 时抛出 `ImproperlyConfigured` 阻止启动 |
| 前端生产校验 | ✅ | `check-env.mjs` 检查占位符、localhost、密钥长度等 |
| Docker Compose 必填校验 | ✅ | `${VAR:?error message}` 语法确保必填变量不为空 |

### 2.4 部署路径

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| Vite `base` 配置 | ✅ | `env.VITE_BASE_URL || '/'`，支持子路径部署 |
| Nginx SPA fallback | ✅ | `try_files $uri $uri/ /index.html` |
| 前端 `index.html` | ⚠️ | `<title>Vite App</title>` 未动态化；`lang=""` 应改为 `lang="zh-CN"` |

---

## 三、前后端协同与 API 审查

### 3.1 API 契约

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| 统一响应格式 | ✅ | `{"code": 0, "data": {}, "message": "str"}` 前后端一致 |
| 分页参数 | ✅ | `page` / `page_size` 跨端一致 |
| OpenAPI 文档 | ✅ | `drf-spectacular` 生成 Swagger/ReDoc，`/api/v1/schema/` 导出 |
| 状态枚举键名 | ✅ | `AssetCurrentStatus` TypeScript 枚举与后端 Model 一致 |

### 3.2 接口地址

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| 前端 API 基础地址 | ✅ | `VITE_API_BASE_URL=/api/v1` |
| 开发代理 | ✅ | Vite proxy `/api` → `VITE_API_TARGET || 'http://127.0.0.1:8000'` |
| 生产代理 | ✅ | Nginx `/api/` → upstream `asset_backend`（web:8000） |
| WebSocket | ✅ | `ws://` 通过 Nginx `/ws/` 路径代理，带 Upgrade 头 |

### 3.3 错误响应

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| 全局异常处理器 | ✅ | `core/exception_handler.py` 统一转换为 `{code, message, data}` |
| 业务异常类 | ✅ | 6 个自定义异常类（`AppValidationError`、`NotFoundError` 等） |
| HTTP 状态码 | ✅ | 400/401/403/404/409/500 均有对应处理 |
| **error_code 字段** | ✅ | `AppValidationError` 和 `BusinessLogicError` 支持 `error_code` |

### 3.4 CORS 配置

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| 生产 CORS 策略 | ✅ | `CORS_ALLOW_ALL_ORIGINS = False`，从 `CORS_ALLOWED_ORIGINS` 环境变量读取白名单 |
| 开发 CORS | ✅ | 仅允许 `localhost:3000`/`localhost:8080` 等本地地址 |
| 测试 CORS | ⚠️ | `CORS_ALLOW_ALL_ORIGINS = True`（仅测试环境，风险可控） |

---

## 四、安全审查

### 4.1 HTTPS 强制

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| Nginx HTTP→HTTPS | ✅ | 301 重定向 |
| TLS 版本 | ✅ | 仅 TLS 1.2 + 1.3 |
| Django `SECURE_SSL_REDIRECT` | ✅ | 生产环境启用 |
| OCSP Stapling | ✅ | 启用 |
| 证书自动续期 | ✅ | Certbot 容器每 12 小时检查 |

### 4.2 身份鉴权

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| JWT 双通道认证 | ✅ | Cookie（PC）+ Bearer（移动端），`JWTCookieAuthentication` |
| Token 黑名单 | ✅ | 登出后 Token 加入黑名单 |
| Refresh 轮转 | ✅ | Refresh token 使用后轮转+旧 token 加入黑名单 |
| **后端 RBAC** | ✅ | 4 层角色（system_admin > dept_manager > asset_admin > auditor），行级数据隔离 |
| **路由守卫** | ✅ | 前端 `meta.requiredMinRole` + `roleWhitelist` 静态+动态双重校验 |
| 登录锁定 | ✅ | 5 次失败→15 分钟锁定（`LoginLockoutThrottle`） |

### 4.3 输入校验

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| ORM 参数化查询 | ✅ | 使用 Django ORM，无 `raw()` SQL 拼接 |
| 文件上传限制 | ✅ | Django 10MB + Nginx `client_max_body_size 10m` 双重限制 |
| 文件名重命名 | ✅ | UUID 重命名（SC-6） |
| 速率限制 | ✅ | 应用层（DRF Throttle）+ Nginx 层双重限流 |
| XSS 防护 | ✅ | 无 `v-html`/`eval()`/`innerHTML` 使用 |

### 4.4 安全响应头

| 头部 | 状态 | 实现位置 |
|:---|:---:|:---|
| `Strict-Transport-Security` | ✅ | Nginx（2年 + includeSubDomains + preload） |
| `X-Content-Type-Options: nosniff` | ✅ | Nginx |
| `X-Frame-Options: DENY` | ✅ | Nginx |
| `Referrer-Policy` | ✅ | Nginx（strict-origin-when-cross-origin） |
| `Permissions-Policy` | ✅ | Nginx（禁用 camera/microphone/geolocation） |
| `Content-Security-Policy` | ✅ | Django 自定义中间件（`core/csp_middleware.py`），生产启用严格策略 |

---

## 五、功能与数据审查

### 5.1 核心功能

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| 资产状态机 | ✅ | 完整覆盖 in_store → in_use → recycled_pending → broken/lost → repairing → damaged → scrapped |
| 资产全生命周期 | ✅ | 入库、领用、外借、回收、损坏、遗失、报废、维修、找回均有对应 API 和前端页面 |
| 404 路由处理 | ✅ | `/:pathMatch(.*)*` → `NotFound.vue` |
| **History 模式刷新** | ✅ | Nginx `try_files $uri $uri/ /index.html` 确保 SPA 刷新不 404 |
| 数据导入导出 | ✅ | ExcelJS（前端）+ 后端批量处理 |

### 5.2 权限系统

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| RBAC 数据初始化 | ✅ | `docker-entrypoint.sh` 调用 `init_production_data` 创建默认角色和权限 |
| 4 层角色 | ✅ | system_admin、dept_manager、asset_admin、auditor |
| 前端按钮级控制 | ✅ | `meta.requiredMinRole` 控制路由可见性 |
| 后端接口级控制 | ✅ | `IsSystemAdmin`、`IsDeptManagerOrAbove` 等权限类 |
| 行级数据隔离 | ✅ | 部门作用域角色限制查询范围 |
| 超级管理员快捷方式 | ✅ | Superuser 跳过 DB 权限查询 |

### 5.3 数据初始化

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| 数据库迁移 | ✅ | 6 个 App、约 68 个迁移文件 |
| 自动迁移 | ✅ | `docker-entrypoint.sh` 执行 `migrate --noinput` |
| RBAC 初始数据 | ✅ | 通过 `init_production_data` 管理命令注入 |
| 超级管理员 | ✅ | 支持通过环境变量自动创建（`DJANGO_SUPERUSER_*`） |

### 5.4 数据备份

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| PostgreSQL 备份 | ✅ | 每日 02:00，`pg_dump --format=custom`，30 天保留 |
| Redis 备份 | ✅ | 每日 02:30，RDB 快照归档 |
| 媒体文件备份 | ✅ | 每日 03:00，tar.gz 归档 |
| S3 异地备份 | ✅ | 3 次重试 + 完整性校验 |
| 备份验证 | ✅ | 每周日 `pg_restore -l` 可读性检查 |
| **恢复演练** | ✅ | 每季度执行一次恢复测试（`restore_test.sh`），校验 RTO < 3600s |

---

## 六、性能与部署审查

### 6.1 资源优化

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| Gzip 压缩 | ✅ | Nginx gzip level=6 + `gzip_static`（预压缩 Vite 产物） |
| Brotli 压缩 | ✅ | Vite `vite-plugin-compression`（阈值 10KB） |
| 路由懒加载 | ✅ | 除登录页外所有路由使用 `() => import(...)` |
| 代码分块 | ✅ | echarts/exceljs/element-plus/vendor 独立 chunk |
| **`stats.html` 泄露** | ⚠️ | `dist/stats.html`（1.5MB）是打包分析产物，不应部署到生产 |
| **`vite-plugin-vue-devtools`** | ⚠️ | 无条件加载，生产构建会包含 devtools 代码 |
| CSS 按需加载 | ✅ | `unplugin-vue-components` 自动按需引入 Element Plus |

### 6.2 后端性能

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| 数据库连接池 | ✅ | `CONN_MAX_AGE=600`（10 分钟复用）+ `CONN_HEALTH_CHECKS=True` |
| Redis | ✅ | 用于 WebSocket Channel Layer + 缓存 |
| 请求限流 | ✅ | 应用层（20/100 req/min）+ Nginx 层（30 req/s） |
| ASGI 服务器 | ✅ | Daphne 支持 WebSocket 原生连接 |
| Nginx upstream keepalive | ✅ | 32 个长连接 |

### 6.3 服务托管

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| Docker 多阶段构建 | ✅ | builder + runtime，非 root 用户 `appuser` |
| Docker Compose | ✅ | 6 服务（web/postgres/redis/nginx/certbot/backup） |
| 资源限制 | ✅ | 生产覆盖文件设置 CPU/Memory 上限 |
| 自动重启 | ✅ | `restart: always`（生产）/ `unless-stopped`（开发） |
| 优雅停机 | ✅ | Daphne + Docker `SIGTERM` + 45s/60s grace period |

### 6.4 健康检查

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| `/health/` 端点 | ✅ | 检查 PostgreSQL + Redis，返回 200/503 |
| `/ready/` 端点 | ✅ | 带结构化日志的就绪检查 |
| Docker HEALTHCHECK | ✅ | web/postgres/redis/nginx 全部配置健康检查 |
| Nginx 豁免 | ✅ | `/health/` 和 `/ready/` 无速率限制、无访问日志 |

### 6.5 发布与回滚

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| CI/CD 流水线 | ✅ | `ci-cd.yml` 自动构建 Docker 镜像并推送 |
| 镜像标签策略 | ✅ | `latest` + commit SHA 双标签 |
| 镜像清理 | ✅ | 自动保留最近 5 个 SHA 标签 + latest + 当前生产版本 |
| 回滚方案 | ✅ | `docs/RollbackSOP.md` 定义了 5 步回滚流程 |
| **蓝绿/金丝雀部署** | ⏳ | 待实施（文档中标注为后续计划） |

---

## 七、监控与运维审查

### 7.1 日志与监控

| 项目 | 状态 | 说明 |
|:---|:---:|:---|
| 结构化 JSON 日志 | ✅ | `StructuredJSONFormatter` 输出 time/level/logger/module/message/trace_id |
| 日志轮转 | ✅ | `RotatingFileHandler` 10MB × 5 份（~60MB 上限） |
| Docker 日志轮转 | ✅ | `json-file` 驱动，50MB/5 文件（web/postgres/nginx） |
| **trace_id 透传** | ✅ | `RequestContextMiddleware` → ThreadLocal → `TraceIDFilter` → 日志 + 响应头 `X-Request-ID` |
| Prometheus 指标 | ✅ | `/metrics/` 端点：请求计数、延迟直方图、活跃请求、5xx 错误 |
| Grafana 仪表盘 | ✅ | 预配置 Dashboard（QPS/P95 延迟/5xx 率/DB 连接/Redis 连接） |
| Sentry 集成 | ✅ | 可选（`SENTRY_DSN` 环境变量），10% 采样率 |
| **Prometheus 中间件** | ✅ | 路径归一化（UUID→`{uuid}`，数字→`{id}`）防止高基数标签 |

### 7.2 告警配置

| 告警规则 | 级别 | 触发条件 | 状态 |
|:---|:---:|:---|:---:|
| `HighErrorRate` | Critical | 5xx > 5% 持续 2min | ✅ |
| `HighLatencyP95` | Warning | P95 > 2s 持续 5min | ✅ |
| `HighLatencyP99` | Critical | P99 > 5s 持续 5min | ✅ |
| `ServiceDown` | Critical | Django 不可达 1min | ✅ |
| `RequestBacklog` | Warning | 活跃请求 > 50 持续 3min | ✅ |
| **通知渠道** | ⚠️ | Alertmanager webhook 为占位符（`host.docker.internal:9095`），**未接入实际通知渠道** |

---

## 八、CI/CD 流水线审查

| 流水线 | 状态 | 说明 |
|:---|:---:|:---|
| 后端 Lint（ruff） | ✅ | `backend-lint` job |
| 后端类型检查（mypy --strict） | ✅ | `backend-type-check` job |
| 后端复杂度（max-complexity=10） | ✅ | `backend-complexity` job |
| 后端测试（80%/90% 覆盖率门禁） | ✅ | `backend-test` job + PostgreSQL service |
| 后端变异测试（80% killed 门禁） | ✅ | `backend-mutation` job |
| 前端 Lint（ESLint） | ✅ | `frontend-lint` job |
| 前端类型检查（vue-tsc） | ✅ | `frontend-type-check` job |
| 前端测试（80%/90% 覆盖率门禁） | ✅ | `frontend-test` job |
| 前端变异测试 | ✅ | `frontend-mutation` job |
| 迁移安全检查（CT-6） | ✅ | `migration-check.yml` 三步验证 |
| 依赖安全扫描（SC-7） | ✅ | `security-scan.yml` pip-audit + npm audit + 密钥扫描 |
| 重复代码守卫 | ✅ | `duplicate-guard.yml` |
| Docker 构建推送 | ✅ | `ci-cd.yml` |
| **CODEOWNERS** | ⏳ | 未配置 |
| **PR 模板** | ⏳ | 未配置 |

---

## 九、问题汇总与优先级

### 🔴 高优先级（建议上线前修复）

| # | 问题 | 位置 | 影响 |
|:---|:---|:---|:---|
| H-1 | `pyproject.toml` 与 `requirements/base.txt` 版本不一致，`pip install -e .` 安装旧版有漏洞依赖 | `pyproject.toml` | 安全漏洞 |
| H-2 | 后端 `.env` 含真实数据库/Redis 密码，需确认从未被 git 提交 | `asset_management_backend/.env` | 敏感信息泄露 |
| H-3 | Alertmanager 通知渠道为占位符，告警无法触达运维人员 | `config/alertmanager.yml` | 监控失效 |
| H-4 | 前端 `dist/stats.html`（1.5MB）不应部署到生产 | `dist/` | 信息泄露 + 体积浪费 |

### ⚠️ 中优先级（建议尽快处理）

| # | 问题 | 位置 | 影响 |
|:---|:---|:---|:---|
| M-1 | `sentry-sdk` 未精确锁定版本（`>=2.0,<3.0`） | `requirements/base.txt` | 依赖不确定性 |
| M-2 | `router/index.ts` 超 500 行（737 行），违反 DR-5 | `src/router/index.ts` | 可维护性 |
| M-3 | `vite-plugin-vue-devtools` 无条件加载，生产构建包含开发工具 | `vite.config.ts` | 体积 + 安全 |
| M-4 | 前端无全局 Vue 错误处理器（`app.config.errorHandler`） | `src/main.ts` | 生产可观测性 |
| M-5 | `useUserExcelExport.ts` 遗留 3 处 `console.log` | `src/composables/` | 代码整洁 |
| M-6 | 前端 `.env.development` 含密钥已被 git 跟踪 | `vue-assetmanagement/.env.development` | 密钥管理 |
| M-7 | `index.html` 中 `lang=""` 和 `<title>Vite App</title>` | `index.html` | 用户体验 |
| M-8 | 安装了 `less` + `less-loader` 但项目仅使用 SCSS | `package.json` | 无用依赖 |

### 💡 低优先级（建议后续迭代）

| # | 问题 | 位置 | 影响 |
|:---|:---|:---|:---|
| L-1 | 无 CODEOWNERS / PR 模板 / Issue 模板 | `.github/` | 团队协作规范 |
| L-2 | 无蓝绿/金丝雀部署策略 | — | 发布风险控制 |
| L-3 | 前端无 CSP meta 标签（依赖 Nginx 层） | `index.html` | 多层防护深度 |
| L-4 | 前端 `.env.production` 未设置 `VITE_WS_BASE_URL` | `.env.production` | WebSocket 配置完整性 |

---

## 十、总体评估

| 维度 | 评级 | 说明 |
|:---|:---:|:---|
| **代码质量** | ⭐⭐⭐⭐ | 严格 TypeScript、完善 Lint/类型检查、mutation testing |
| **安全** | ⭐⭐⭐⭐⭐ | 多层安全防护（Django + Nginx + 应用层），密钥零硬编码，RBAC 完整 |
| **可观测性** | ⭐⭐⭐⭐ | trace_id 透传、结构化日志、Prometheus/Grafana/Sentry，告警待接入 |
| **CI/CD** | ⭐⭐⭐⭐ | 10 job 并行流水线、覆盖率门禁、迁移安全检查、依赖扫描 |
| **部署** | ⭐⭐⭐⭐⭐ | Docker 多阶段构建、非 root、健康检查、资源限制、优雅停机 |
| **备份恢复** | ⭐⭐⭐⭐⭐ | 全量备份 + S3 异地 + 周验证 + 季度恢复演练 |
| **文档** | ⭐⭐⭐⭐ | 完整的需求/设计/运维文档，回滚 SOP |

**整体结论**：项目架构成熟度高，安全与运维体系完善。**上线前需重点关注 H-1（依赖版本漂移）和 H-2（.env 历史提交确认）**，其余为锦上添花项。

---

> 本报告由 opencode (big-pickle) Agent 于 2026-08-26 自动生成，审查依据 AGENTS.md v3.5.0 全部规范条款。
