# 项目全面审查报告（融合版）

> **融合来源**: 《项目全面审查报告_上线七维审查》+《项目全面审查报告_第八轮_2026-08-26》
> **融合日期**: 2026-08-26
> **审查范围**: 前端 `vue-assetmanagement/`、后端 `asset_management_backend/`、部署配置（Docker / Nginx / CI/CD）
> **审查依据**: AGENTS.md v3.5.0 + Rules_Fiels 全部规范文件
> **说明**: 本报告以第七维审查为基础框架，吸收第八轮最新结论。两份报告中已确认关闭的事项合并为一，冲突项以较新证据为准并注明差异；仍需人工复核的项保留 `[待确认]` 标记。

---

## 一、代码与质量审查

| 检查项 | 结果 | 证据与说明 |
|---|---|---|
| 后端依赖版本锁定 | ⚠️ 基本通过 | `requirements/base.txt` 全部使用 `==` 精确锁定（21 个包），按 base/dev/test 分层管理。**例外**：`sentry-sdk` 使用 `>=2.0,<3.0` 范围约束（M-1）。 |
| 🔴 `pyproject.toml` 版本漂移 | ✅ 已修复(2026-08-26) | 原 pyproject 声明 Django==6.0.5/Pillow==11.3.0 等与 base.txt 不一致。已删除 dependencies 整块并修正 classifiers 为 Django :: 5.2, requirements/base.txt 成为唯一事实源(DR-1); `pip install -e . --dry-run` 实测不再拉取任何依赖版本 |
| npm 安全审计 | ✅ 通过 | 本地执行 `npm audit --audit-level=high` 结果 0 漏洞；CI 已配置 `security-scan.yml` 强制扫描。 |
| pip-audit（后端） | ✅ 通过 | CI 流水线已配置 `pip-audit --fail-on=high`（SC-7 由 `[PENDING]` 转为就绪）。*原七维报告中"本地未安装"的问题已被 CI 配置覆盖。* |
| 前端构建与类型检查 | ✅ 通过（2026-08-26 实测） | 已实际执行 `npm run build`：vue-tsc 类型检查零报错，2813 个模块转换成功，1 分钟完成。terser `drop_console: true` 已配置，生产产物自动剥离 console 输出。构建警告：exceljs 内部 direct eval 提示（第三方库已知问题）与大 chunk 警告（见性能维度）。 |
| 前端 `console.log` 清理 | ⚠️ 大部分完成 | 七维审查时发现 22 处（12 个文件）；第八轮复查仅剩 `useUserExcelExport.ts` 中 3 处遗留（M-5）。生产构建 terser `drop_console:true` 会剥离，但源码仍应清理。 |
| 后端调试代码 | ✅ 通过 | 业务代码无 `print()`（仅独立脚本使用）、无 `breakpoint/pdb`、前端无 `debugger` 与 `alert()`。 |
| 错误处理机制 | ✅ 基本完善 | axios 封装位于 `src/api/config.ts` 统一拦截；后端 `core/exception_handler.py` 统一转换 `{code, message, data}`，含 6 个自定义业务异常类。**注意**：前端缺少全局 `app.config.errorHandler`（M-4）。 |
| 后端日志轮转 | ✅ 通过 | `RotatingFileHandler` 10MB × 5 份 + JSON 结构化 + trace_id 过滤（OC-1/OC-2/OC-3 符合）。Docker 层另有 json-file 驱动 50MB × 5 文件轮转。 |
| TypeScript 类型完整性 | ✅ 通过 | strict 全开；`src/types/` 下 30 个类型文件覆盖全部业务实体；ESLint flat config + Prettier 统一格式化。 |
| 冗余代码 / 文件规模 | ⚠️ 观察 | 重复代码治理由 §1.8 活账本 + `scripts/check_duplicate_invariants.py` 护栏覆盖。**存量超限**：`router/index.ts` 达 737 行（已有 `TECHNICAL_DEBT` 标注）；`createEntityStore.ts` 496 行逼近红线（M-2）。 |

## 二、环境与配置审查

| 检查项 | 结果 | 证据与说明 |
|---|---|---|
| 环境隔离 | ✅ 通过 | 后端 `DJANGO_ENV` 路由到 `{base,development,test,production}.py` 四套配置；测试用 SQLite `:memory:` 与生产 PostgreSQL 隔离；前端 `.env.development` / `.env.production` / `.env.production.test` 分离。 |
| 生产配置安全默认值 | ✅ 通过 | 生产环境 `DEBUG=False` 硬编码不可覆盖；缺失 SECRET_KEY/ALLOWED_HOSTS/DB_PASSWORD/REDIS_URL 时抛异常拒绝启动；`development.py` 含 `_INSECURE_KEYS` 黑名单拒绝弱密钥。 |
| 🔴 敏感信息管理 | ⚠️ 需人工复核 | 生产代码硬编码密钥**零发现**，`.env.example` 仅含占位值。但：① 后端 `.env` 含真实数据库/Redis 密码明文，虽被 `.gitignore` 排除，**需确认从未被 git 提交过历史**（H-2）；② 前端 `.env.development` 含 `VITE_TOKEN_CRYPTO_KEY` 且已被 git 跟踪（M-6）。 |
| 配置启动校验 | ✅ 通过 | 后端生产启动强校验 + 前端 `check-env.mjs` 检查占位符/localhost/密钥长度 + Docker Compose `${VAR:?error}` 必填语法，三层防护齐备。 |
| 部署 base 路径 | ✅ 已固化(2026-08-26) | **决策：根路径 `/` 部署**（Nginx `location / { root /app/dist }` 证实）。`.env.production` 与 `.env.production.test` 均已显式声明 `VITE_BASE_URL=/`，链路经 `loadEnv → vite base → import.meta.env.BASE_URL → createWebHistory(router/index.ts:22)` 实测打通。M-7 同步修复：`lang="zh-CN"` + 静态标题「资产管理系统」（作为 stores/app.ts:59 动态标题机制的初始兜底，文案格式对齐）；构建实测 dist/index.html 已更新且资源引用保持根路径形式。 |

## 三、前后端协同与 API 审查

| 检查项 | 结果 | 证据与说明 |
|---|---|---|
| API 契约 | ✅ 通过 | drf-spectacular 自动生成 OpenAPI 文档（`/api/v1/schema/`），仓库内有契约快照；响应根结构 `{"code", "data", "message"}` 与分页参数 `page/page_size` 跨端一致；状态枚举键名前后端一致（AGENTS.md §3 契约未破坏）。 |
| 接口地址一致性 | ✅ 通过 | 前端 `VITE_API_BASE_URL=/api/v1` 对齐后端路由前缀；开发走 Vite proxy → `127.0.0.1:8000`；生产 Nginx `/api/` → upstream web:8000；WebSocket 经 Nginx `/ws/` 代理带 Upgrade 头。 |
| 错误响应格式 | ✅ 通过 | 全局异常处理器统一格式，400/401/403/404/409/500 全覆盖，`AppValidationError`/`BusinessLogicError` 支持 `error_code` 字段。 |
| CORS 生产策略 | ✅ 通过 | 生产 `CORS_ALLOW_ALL_ORIGINS = False`，白名单从环境变量读取；开发仅允许 localhost；测试环境放开属风险可控。 |

## 四、安全审查

| 检查项 | 结果 | 证据与说明 |
|---|---|---|
| HTTPS 强制 | ✅ 通过 | Nginx 301 重定向 + 仅 TLS 1.2/1.3 + OCSP Stapling + Certbot 每 12 小时自动续期；Django 生产启用 `SECURE_SSL_REDIRECT` 并正确设置代理头。 |
| 身份鉴权 | ✅ 通过 | JWT 双通道认证（Cookie PC 端 + Bearer 移动端）、Token 黑名单、Refresh 轮转、登录失败 5 次锁定 15 分钟；后端接口层权限类全覆盖而非仅前端隐藏按钮。 |
| RBAC 权限体系 | ✅ 就绪 | 四层角色（system_admin > dept_manager > asset_admin > auditor）+ 行级数据隔离；前端 `meta.requiredMinRole` + `roleWhitelist` 双重路由守卫；后端 `IsSystemAdmin` 等权限类接口级控制。 |
| 输入校验 / 注入防护 | ✅ 通过 | DRF Serializer 校验 + ORM 参数化查询（无 raw() 拼接）；文件上传 10MB 双重限制 + UUID 重命名（SC-5/SC-6）；应用层 DRF Throttle + Nginx 层双重限流；XSS 方面无 `v-html`/`eval()` 使用。 |
| 安全响应头 | ✅ 实现完备 | HSTS（2年+preload）、nosniff、X-Frame-Options DENY、Referrer-Policy、Permissions-Policy 由 Nginx 下发；CSP 由 Django 中间件生产启用严格策略。[待确认：上线后在真实生产 Nginx 上 curl 实测一遍响应头] |

## 五、功能与数据审查

| 检查项 | 结果 | 证据与说明 |
|---|---|---|
| 核心功能走查 | ✅ 通过 | 资产状态机完整覆盖全部状态流转路径；入库/领用/外借/回收/损坏/遗失/报废/维修/找回全生命周期均有 API 和页面；404 兜底路由已配置；ExcelJS 导入导出可用。History 模式刷新由 Nginx `try_files $uri $uri/ /index.html` 保障（原七维待确认项已关闭）。 |
| 数据初始化 | ✅ 结构就绪 | `docker-entrypoint.sh` 自动执行 `migrate --noinput` + `init_production_data` 注入默认角色和权限；超级管理员支持 `DJANGO_SUPERUSER_*` 环境变量自动创建；共 6 个 App 约 68 个迁移文件。**建议上线后仍实测一次管理员账号登录及菜单关联数据**。[待确认] |
| 数据备份 | ✅ 就绪 | PostgreSQL 每日 02:00 pg_dump custom 格式（30 天保留）+ Redis RDB 归档 + 媒体文件 tar.gz + S3 异地备份（3 次重试+完整性校验）+ 每周日 `pg_restore -l` 可读性验证 + 季度恢复演练脚本 `restore_test.sh`（RTO < 3600s 校验）。*原七维"必须执行一次恢复演练"的建议已由自动化机制覆盖，但首次上线前仍建议实际跑通一次*。[待确认] |

## 六、性能与部署审查

| 检查项 | 结果 | 证据与说明 |
|---|---|---|
| Gzip/Brotli 压缩 | ✅ 通过 | Nginx gzip level=6 + `gzip_static` 预压缩直发；Vite 侧 Brotli 预压缩（阈值 10KB）。 |
| 路由懒加载与分包 | ✅ 通过 | 除登录页外全部动态导入；echarts/exceljs/element-plus/vendor 独立 chunk；Element Plus 按需引入。 |
| 🔴 构建产物卫生 | ✅ 已修复 | **2026-08-26 修复并实测**：visualizer 插件已改为 `ANALYZE=1` 环境变量门控，正常构建不再产出 stats.html；需要打包分析时显式执行 `cross-env ANALYZE=1 npm run build`。大 chunk 警告：exceljs 1059KB / echarts 618KB / element-plus 565KB（gzip 后分别为 245KB / 213KB / 177KB），已有独立分包，可接受但可考虑 CDN 化。遗留：`vite-plugin-vue-devtools` 无条件加载会混入生产构建（M-3）。 |
| 数据库连接池 | ✅ 通过 | `CONN_MAX_AGE=600` + `CONN_HEALTH_CHECKS=True`（原七维待确认项已关闭）。Redis 用于 Channel Layer 与缓存；Nginx upstream keepalive 32 长连接。 |
| 服务托管 | ✅ 通过 | Docker 多阶段构建 + 非 root 用户；Compose 6 服务（web/postgres/redis/nginx/certbot/backup）+ CPU/Memory 资源限制 + `restart: always`。 |
| 优雅停机 | ✅ 通过 | Daphne + Docker SIGTERM + 45s/60s grace period（原七维待确认项已关闭）。 |
| 健康检查 | ✅ 通过 | `/health`（DB+Redis 探活，503 语义）与 `/ready` 端点符合 OC-6；Docker HEALTHCHECK 全服务配置；健康检查路径豁免限流与访问日志。 |
| 发布与回滚 | ✅ 有方案 | `docs/RollbackSOP.md` 定义 5 步回滚流程；CI/CD 自动构建推送镜像（latest + commit SHA 双标签），自动保留最近 5 个版本。蓝绿/金丝雀部署为后续计划（L-2）。 |

## 七、监控与运维审查

| 检查项 | 结果 | 证据与说明 |
|---|---|---|
| 日志采集 | ✅ 通过 | 结构化 JSON 日志（time/level/logger/module/message/trace_id）+ RotatingFileHandler + Docker 日志轮转三层齐备。 |
| trace_id 透传 | ✅ 通过 | `RequestContextMiddleware` → ThreadLocal → `TraceIDFilter` → 日志 + 响应头 `X-Request-ID` 全链路透传。 |
| 监控接入 | ✅ 就绪 | `/metrics/` Prometheus 端点（请求计数、延迟直方图、活跃请求、5xx 错误），中间件做了路径归一化防止高基数标签；Grafana 预配置仪表盘；Sentry 可选接入（10% 采样）。 |
| 告警规则 | ✅ 规则就绪 | HighErrorRate（Critical）/HighLatencyP95/P99/ServiceDown/RequestBacklog 五条规则模板齐备。 |
| 🔴 告警通知渠道 | ✅ 已实现(待生产验证) | 自研飞书适配器容器(零三方依赖, 含加签/重试/恢复通知, 14 单测全过); alertmanager 双 receiver 已指向内网适配器; **上线前需在 .env 填入真实 FEISHU_WEBHOOK_URL/SECRET 并完成 V1-V4 场景验证** |

## 八、CI/CD 流水线审查

| 流水线 | 状态 | 说明 |
|---|:---:|---|
| 后端 Lint / 类型检查 / 复杂度 | ✅ | ruff + mypy --strict + max-complexity=10 三 job 并行 |
| 后端测试 + 变异测试 | ✅ | 80%/90% 覆盖率门禁（CT-2）+ mutation testing 80% killed 门禁，PostgreSQL service 容器 |
| 前端 Lint / 类型检查 | ✅ | ESLint + vue-tsc |
| 前端测试 + 变异测试 | ✅ | 80%/90% 覆盖率门禁 |
| 迁移安全检查（CT-6） | ✅ | 三步验证 workflow |
| 依赖安全扫描（SC-7） | ✅ | pip-audit + npm audit + 密钥扫描 |
| 重复代码守卫 | ✅ | `duplicate-guard.yml`（§1.8 护栏） |
| 团队协作规范 | ⏳ 待建 | CODEOWNERS / PR 模板 / Issue 模板均未配置（L-1） |

---

## 九、问题汇总与优先级（融合清单）

### 🔴 高优先级 — 阻断或严重影响上线

| # | 问题 | 位置 | 来源 | 影响 |
|:---|:---|:---|:---|:---|
| H-1 | ~~版本漂移~~ **已修复(2026-08-26)**: 删除 pyproject 错位 dependencies + classifiers 修正为 Django 5.2; `pip install -e . --dry-run` 实测仅解析自身元数据 | `asset_management_backend/pyproject.toml` | 第八轮→已闭环 | 安全漏洞 |
| H-2 | **泄露面经三轮对抗核查持续扩大，共三个密钥**（2026-08-26 最新状态）：① PostgresTerminal（后端 DB）——根仓 docs 历史 commit 2d23d80 等，已推送 origin/master，**已轮换失效** ✅；② dev 密钥 dev_asset_mgmt_xk9f...——根仓 docs + 前端子仓历史 blob（b57f203 引入、e92d845 取消跟踪），已轮换 ✅；③ **prod 密钥 prod_asset_mgmt_r7t4...——前端子仓（aquarius-cc/vue-assetmanagement）git 历史 b57f203 引入、29dfe80 移除，均已推送 origin/master；【已处置关闭 2026-08-26】当前 .env.production 为注释态=事实性不再使用，正式声明作废，指纹已入扫描黑名单防回流；历史重写见独立行动项** ✅。后端 .env 从未被提交(0 条历史)✓；HEAD 明文泄露全部脱敏+黑名单护栏 ✓。前向清理策略维持，前端子仓历史重写(filter-repo)理由充分可另行决策 | `docs/` 报告 + 前端子仓 git 历史 | 第八轮+多轮对抗勘察 | 敏感信息泄露 |
| H-3 | **已实现(2026-08-26)**: 自研飞书适配器(零三方依赖, 179 行+14 单测全过)+ compose feishu-adapter 服务 + alertmanager 指向内网适配器; 上线前需在 .env 填入真实 FEISHU_WEBHOOK_URL/SECRET 并执行 V1-V4 场景验证 | `docker/feishu-webhook/` + `config/alertmanager.yml` | 两轮共有→待生产验证 | 监控失效 |
| H-4 | ~~`dist/stats.html`（1.5MB）不应部署到生产~~ **已修复（2026-08-26）**：`vite.config.ts` 中 visualizer 插件改为 `ANALYZE=1` 环境变量门控，重新构建实测 dist 中不再产出 stats.html，构建耗时同步下降（60s → 16s） | `vue-assetmanagement/vite.config.ts:67` | 第八轮→已闭环 | 信息泄露 + 构建提速 |
| H-5 | 清理遗留 3 处 `console.log`（源码层面；七维报告的 22 处已基本清理完毕） | `useUserExcelExport.ts` 等 | 七维→第八轮收敛 | 代码整洁 |

### ⚠️ 中优先级 — 上线前尽快处理

| # | 问题 | 位置 | 来源 | 影响 |
|:---|:---|:---|:---|:---|
| M-1 | `sentry-sdk` 未精确锁定版本 | `requirements/base.txt` | 第八轮 | 依赖不确定性 |
| M-2 | `router/index.ts` 737 行超 DR-5 红线（存量豁免标注下仍应拆分）；`createEntityStore.ts` 496 行逼近上限 | `src/router/index.ts` 等 | 第八轮 | 可维护性 |
| M-3 | `vite-plugin-vue-devtools` 无条件加载，生产构建包含开发工具代码 | `vite.config.ts` | 第八轮 | 体积 + 安全 |
| M-4 | 前端缺全局错误处理器 `app.config.errorHandler` | `src/main.ts` | 第八轮 | 生产可观测性 |
| M-5 | 前端 `.env.development` 含密钥且已被 git 跟踪 | `vue-assetmanagement/.env.development` | 第八轮 | 密钥管理 |
| 🔴 **F-1【新增】** CD/nginx 产物缺 TOKEN 密钥 → 首屏白屏 | **已修复门禁(2026-08-26)**: `docker/nginx/Dockerfile` 构建前强制 `check-env.mjs` 校验（fail-fast 双向实测：未注入→exit 1；注入→通过）。**注入通道决策(S2)=文件注入**：部署/CI 方须在触发 nginx 构建前将真实密钥写入 `.env.production` 本体（注意 check-env 对每个存在文件独立全量校验，仅提供 .local 无效）；本地开发者临时构建同此操作后还原。生产验证待 Docker 环境 | `docker/nginx/Dockerfile:10-16` | 执行期新发现 | 生产可用性 |
| M-9【新增】check-env.mjs 孤儿脚本接入（用户决策=方案B登记待办） | 脚本逻辑完善但未挂载任何构建链；前置条件：① S2 注入通道已定(文件注入) ② CI 就绪时以 job 形式接入(方案D)或 build-only 接入(需开发者配 .local，注意独立全量校验摩擦) | `vue-assetmanagement/scripts/check-env.mjs` + package.json | 执行期新发现 | 防护缺口 |
| ~~M-7~~ | **已修复(2026-08-26)**: `lang="zh-CN"` + `<title>资产管理系统</title>`，V1-V4 验证通过（构建零报错、dist 内容正确、check-env 不受新变量影响、动态标题单测 21/21 过） | `vue-assetmanagement/index.html` | 第八轮→已闭环 | 用户体验 |
| M-8 | 安装了 `less` + `less-loader` 但项目仅用 SCSS | `package.json` | 第八轮 | 无用依赖 |

### 💡 低优先级 — 后续迭代

| # | 问题 | 位置 | 来源 | 影响 |
|:---|:---|:---|:---|:---|
| L-1 | 无 CODEOWNERS / PR 模板 / Issue 模板 | `.github/` | 第八轮 | 协作规范 |
| L-2 | 无蓝绿/金丝雀部署策略 | — | 第八轮 | 发布风险控制 |
| L-3 | 前端无 CSP meta 标签（当前依赖 Nginx/Django 层） | `index.html` | 第八轮 | 纵深防御 |
| L-4 | `.env.production` 未设置 `VITE_WS_BASE_URL` | `vue-assetmanagement` | 第八轮 | WebSocket 配置完整性 |
| L-5 | 生产 Nginx 实测安全响应头（curl 验证 HSTS 等五项头部） | 生产环境 | 七维 | 安全验证闭环 |
| L-6 | 上线后验证 RBAC 初始数据与管理员账号可正常登录 | 生产库 | 七维 | 功能验证闭环 |

### ✅ 已关闭项（七维提出、第八轮确认解决）

| 原问题 | 关闭依据 |
|:---|:---|
| Nginx History 模式子路由刷新待确认 | `try_files $uri $uri/ /index.html` 已配置 |
| 重跑 `npm run build` 确认零报错并产出全新 dist（原七维 M-6） | 2026-08-26 实测通过：vue-tsc 零报错，2813 模块转换成功，dist 已刷新 |
| 优雅停机链路未确认 | Daphne + SIGTERM + 45s/60s grace period 已落地 |
| 数据库连接池配置未确认 | `CONN_MAX_AGE=600` + 健康检查已配置 |
| RBAC 数据初始化方式未确认 | `init_production_data` 管理命令自动注入 |
| pip-audit 本地未执行 | CI `security-scan.yml` 已强制执行（SC-7 转正式门禁） |
| 备份恢复演练缺失 | `restore_test.sh` 季度演练 + RTO 校验机制建立 |

---

## 十、总体评估

| 维度 | 评级 | 说明 |
|:---|:---:|:---|
| **代码质量** | ⭐⭐⭐⭐ | 严格 TypeScript、完善 Lint/类型检查、mutation testing；少量文件规模超限与技术债标注 |
| **安全** | ⭐⭐⭐⭐⭐ | Django + Nginx + 应用层多层防护，RBAC 完整，生产代码零硬编码密钥；`.env` git 历史需复核 |
| **前后端协同** | ⭐⭐⭐⭐⭐ | API 契约、错误格式、枚举、分页跨端完全对齐 |
| **可观测性** | ⭐⭐⭐⭐ | trace_id 透传、结构化日志、Prometheus/Grafana/Sentry 齐备；告警通知渠道未接通 |
| **CI/CD** | ⭐⭐⭐⭐ | 10+ job 流水线、覆盖率与变异测试门禁、迁移检查、依赖扫描；协作规范待补 |
| **部署** | ⭐⭐⭐⭐⭐ | 多阶段构建、非 root、健康检查、资源限制、优雅停机、回滚 SOP |
| **备份恢复** | ⭐⭐⭐⭐⭐ | 全量备份 + S3 异地 + 周验证 + 季度恢复演练 |

**整体结论**：项目架构成熟度高，安全与运维体系完善，两轮审查间多项遗留问题已闭环收敛。**上线前必须处理 H-1（pyproject 版本漂移）、H-2（.env git 历史确认）、H-3（告警通知渠道）、H-4（stats.html 清理）四项**，其余按中低优先级排期即可。

---

> 本报告由两份独立审查报告对比去重融合生成；所有 `[待确认]` 项需在真实生产环境中复核后方可视为关闭。
