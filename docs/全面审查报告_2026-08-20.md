> ⚠️ 安全整改 2026-08-26：本文件原含明文凭据，已按根级规范 SC-1 脱敏；相关凭据已轮换失效。

# 全面审查报告

**审计日期**: 2026-08-20
**审计范围**: 前端 `vue-assetmanagement` + 后端 `asset_management_backend` + 部署配置

---

## 严重程度统计

| 等级 | 数量 | 说明 |
|:-----|:-----|:-----|
| **CRITICAL** | 3 | 必须立即修复，影响安全或部署 |
| **HIGH** | 6 | 上线前必须修复 |
| **MEDIUM** | 12 | 建议尽快修复 |
| **LOW** | 8 | 改善建议 |
| **PASS** | 25+ | 已符合要求 |

---

## 一、代码与质量审查

### 1.1 依赖审计

| 状态 | 发现 |
|:-----|:-----|
| PASS | 后端所有依赖使用 `==` 精确锁定 (`requirements/base.txt`) |
| PASS | 前端使用 `^` 范围锁定 + `package-lock.json`（前端标准做法） |
| PASS | 前端 Node 引擎约束: `^20.19.0 \|\| >=22.12.0` |
| LOW | 前端同时安装了 `sass` 和 `sass-embedded`，后者是更快的替代品，冗余 |
| LOW | `requirements/base.txt` 中 `qrcode[pil]==8.0` 与 `pyproject.toml` 中 `qrcode==8.2` 版本不一致 |
| LOW | `requirements-5.2-backup.txt` 是旧版备份文件，含未使用的 `drf-yasg`，应删除 |

### 1.2 调试代码清理

**前端** — 需清理的 `console.log` 残留:

| 文件 | 行号 | 内容 | 类型 |
|:-----|:-----|:-----|:-----|
| `src/components/componentsdetails/detils/UserForm.vue` | 240 | `console.log('departmentList:', ...)` | DEBUG残留 |
| `src/components/componentsdetails/detils/BasicAssetDetails.vue` | 266 | `console.log('assetCode', assetCode)` | DEBUG残留 |
| `src/components/componentsdetails/detils/OutAssetBasicDetails.vue` | 225 | `console.log('query code:', code)` | DEBUG残留 |
| `src/components/componentsdetails/detils/HardDiskSNBasicDetails.vue` | 192 | `console.log('loadDetail-code:', code)` | DEBUG残留 |
| `src/components/componentsdetails/detils/HardDiskSNBasicDetails.vue` | 194 | `console.log('loadDetail-detail:', detail)` | DEBUG残留 |
| `src/components/componentsdetails/detils/HardDiskSNBasicDetails.vue` | 216 | `console.log('onMounted-code:', code)` | DEBUG残留 |
| `src/components/componentsdetails/HardDiskSNDetails.vue` | 338 | `console.log('点击新增硬盘序列号')` | DEBUG残留 |
| `src/components/commoncomponents/HardDiskSNCard.vue` | 113 | `console.log('HardDiskSNCard点击新增...')` | DEBUG残留 |
| `src/components/componentsdetails/UserDetails.vue` | 501 | `console.log('用户关闭对话框，取消操作')` | DEBUG残留 |
| `src/components/componentsdetails/UserDetails.vue` | 505 | `console.log('用户取消操作或发生错误:', error)` | DEBUG残留 |
| `src/components/componentsdetails/UserDetails.vue` | 523 | `console.log('总数据量:', ...)` | DEBUG残留 |

**注释掉的 console.log（死代码，应清理）**:

| 文件 | 行号 |
|:-----|:-----|
| `src/views/LogIn.vue` | 197 |
| `src/components/componentsdetails/detils/AssetBatchImport.vue` | 627, 631, 641 |
| `src/components/componentsdetails/detils/StorageBatchImport.vue` | 314, 316 |
| `src/components/componentsdetails/detils/UserForm.vue` | 254, 266 |
| `src/components/componentsdetails/detils/OutAssetForm.vue` | 600 |
| `src/components/componentsdetails/detils/BasicAssetDetails.vue` | 209 |
| `src/composables/useRecycleFormAssociations.ts` | 61, 68 |
| `src/utils/SubmitBatch.ts` | 106 |

> **说明**: Vite 配置了 `drop_console: true`（terser），生产构建会自动剥离。但源码中保留 DEBUG 代码影响可读性，且开发环境下仍会输出。

**后端** — PASS: 无 `print()` 残留（仅有 migration 中的数据迁移反馈 print 和 docstring 示例），无 `pdb`/`ipdb`。

### 1.3 TypeScript 类型与规范

| 状态 | 发现 |
|:-----|:-----|
| PASS | `vue-tsc --build` 零错误（已修复） |
| PASS | Vitest 1420/1420 测试全部通过 |
| PASS | ESLint 无新增错误（仅 `vi` 未使用是预存问题） |
| HIGH | `apps/assetmanagement/models.py` **1498行**，超出 DR-5 500行上限近3倍 |
| MEDIUM | `apps/unregisteredasset/services.py` 611行，超出 DR-5 500行上限 |
| MEDIUM | `apps/assetmanagement/state_machine/core.py` 578行，超出 DR-5 500行上限 |
| MEDIUM | `apps/usermanagement/views/department_view.py` 523行，超出 DR-5 500行上限 |

### 1.4 构建验证

| 状态 | 发现 |
|:-----|:-----|
| PASS | 前端 `npm run build`（type-check + vite build）配置正确 |
| PASS | Terser 配置 `drop_console: true` + `drop_debugger: true` 生产剥离 |
| PASS | Gzip + Brotli 压缩已启用 |
| PASS | 代码分割 `manualChunks` 已配置 |
| PASS | Sourcemap 关闭 |

---

## 二、环境与配置审查

### 2.1 环境隔离

| 状态 | 发现 |
|:-----|:-----|
| PASS | 后端使用 `DJANGO_ENV` 选择 settings 模块（base/development/production/test） |
| PASS | `DEBUG=False`（production.py 第9行，base.py 默认 False） |
| PASS | 前端有 `.env.development`、`.env.production`、`.env.production.test` 三套配置 |
| PASS | `.env.development`、`.env.production` 在 `.gitignore` 中 |

### 2.2 敏感信息

| 状态 | 发现 |
|:-----|:-----|
| **CRITICAL** | 后端 `.env` 文件（含 `DB_PASSWORD=********（已脱敏，凭据已轮换失效）********`）**已被提交至 git 历史**（commit `6208685`）。当时密码为 `Terminal@8348*#*`（MySQL时代），现已修改但旧密码仍在历史中。**必须执行密码轮转 + `git filter-repo` 清除历史。** |
| **CRITICAL** | `backups/backup_asset_management_backend_20260610_194154.sql` **已被提交至 git**（commit `6208685`），含数据库完整结构和数据。`backups/` 不在 `.gitignore` 中。**必须添加 `.gitignore` 条目 + 清除 git 历史。** |
| PASS | 前端 `.env*` 文件未被 git 跟踪（正确 gitignore） |
| PASS | 前端无硬编码 API 密钥、Token |
| PASS | 后端应用代码无硬编码密码/密钥（测试文件中的 mock 值除外） |
| MEDIUM | `base.py` 第18行 SECRET_KEY 默认值为弱占位符 `"django-insecure-placeholder-see-env-settings"`，production.py 有防护但 base 层无兜底 |
| LOW | `docker-compose.yml` 第65行 PostgreSQL 弱默认密码 `assetpassword` |
| LOW | `docker-compose.monitoring.yml` 第19行 Grafana 默认密码 `admin` |
| INFO | 前端 `.env.development` 第17行 `VITE_TOKEN_CRYPTO_KEY=********（已脱敏，凭据已轮换失效）********` 是开发用密钥，已在 git 历史中（repo 如公开需轮转） |

### 2.3 配置校验

| 状态 | 发现 |
|:-----|:-----|
| PASS | production.py 启动时校验 `SECRET_KEY`（非空 + 非已知弱密钥 + 长度>=20） |
| PASS | production.py 启动时校验 `ALLOWED_HOSTS` 非空 |
| PASS | production.py 启动时校验 `CHANNEL_LAYERS` 非 InMemory |
| PASS | 后端 `migrate --noinput` 在 `docker-entrypoint.sh` 中自动执行 |

### 2.4 部署路径

| 状态 | 发现 |
|:-----|:-----|
| MEDIUM | 前端 `vite.config.ts` **缺少 `base` 属性**。若部署在子路径（如 `/admin/`），资源路径会解析失败。建议添加 `base: env.VITE_BASE_URL || '/'` |
| PASS | 前端 History 模式 + 404 兜底路由 `/:pathMatch(.*)*` |

---

## 三、前后端协同与API审查

### 3.1 API 契约

| 状态 | 发现 |
|:-----|:-----|
| PASS | 响应格式统一: `{"code": 0, "data": {}, "message": "str"}` |
| PASS | 错误码: HTTP 状态码作为 `code` 值 |
| PASS | 全局异常处理器 (`core/exception_handler.py`) 统一包装所有异常 |
| PASS | 自定义异常类: `AppValidationError`(400), `NotFoundError`(404), `PermissionDeniedError`(403) 等 |
| PASS | 生产环境异常不泄露堆栈详情 |

### 3.2 接口地址

| 状态 | 发现 |
|:-----|:-----|
| PASS | 前端 `BASE_URL = '/api/v1'`，后端所有路由以 `/api/v1/` 为前缀 |
| PASS | 开发环境 Vite proxy `/api` → `http://127.0.0.1:8000` |
| PASS | 无硬编码外部 API URL |

### 3.3 错误响应

| 状态 | 发现 |
|:-----|:-----|
| PASS | Axios 拦截器统一处理 401（刷新Token重试）、429/5xx（暂态保留）、其他（ElMessage 提示） |
| PASS | 错误消息去重防止 toast 洪泛 |
| PASS | 401 刷新: 双通道Cookie + Bearer，最大重试2次，旋转竞态容忍 |

### 3.4 CORS

| 状态 | 发现 |
|:-----|:-----|
| PASS | 生产环境 `CORS_ALLOW_ALL_ORIGINS = False` |
| PASS | 白名单通过环境变量 `CORS_ALLOWED_ORIGINS` 配置 |
| PASS | `CORS_ALLOW_CREDENTIALS = True`（支持 Cookie 认证） |
| INFO | 测试环境 `CORS_ALLOW_ALL_ORIGINS = True`（仅测试用，可接受） |

---

## 四、安全审查

### 4.1 HTTPS 与安全头

| 状态 | 发现 |
|:-----|:-----|
| PASS | `SECURE_SSL_REDIRECT = True`（production.py） |
| PASS | `SECURE_HSTS_SECONDS = 31536000`（1年） + `INCLUDE_SUBDOMAINS` + `PRELOAD` |
| PASS | `SECURE_CONTENT_TYPE_NOSNIFF = True` |
| PASS | `SECURE_BROWSER_XSS_FILTER = True` |
| PASS | `X_FRAME_OPTIONS = "DENY"`（production） |
| PASS | CSP 中间件: 生产强制 Report-Only→Enforce |
| **HIGH** | 无 Nginx 反向代理配置 — 无 TLS 终结、无静态文件缓存、无 gzip、无安全头加固层 |

### 4.2 身份鉴权

| 状态 | 发现 |
|:-----|:-----|
| PASS | 默认权限 `IsAuthenticated`，所有 View 显式声明权限类 |
| PASS | 双通道 JWT: Bearer（移动端）+ HttpOnly Cookie（PC端） |
| PASS | JWT 2h Access / 7d Refresh，旋转+黑名单 |
| PASS | CSRF: Cookie通道非Safe方法强制校验，Login需 `X-Requested-With` |
| PASS | 限流: 全局 20/min(匿名) 100/min(用户)，登录 5/min + 5次锁定15分钟 |
| PASS | 公开端点（login/register/public_scan）均有补偿控制 |
| PASS | 注册序列化器强制移除 `is_staff`/`is_active`/`is_superuser` 防提权 |

### 4.3 输入校验

| 状态 | 发现 |
|:-----|:-----|
| PASS | 所有 API 通过 DRF 序列化器校验输入 |
| PASS | 零 `.raw()` 调用、零 `RawSQL`（仅健康检查用 `cursor.execute("SELECT 1")`） |
| PASS | FK 字段使用 `SlugRelatedField` + queryset 校验 |
| MEDIUM | `docker-entrypoint.sh` 第31-32行将 shell 变量直接拼入 Python 代码（若 DB_PASSWORD 含特殊字符有注入风险） |

### 4.4 安全头（Django 层）

| 状态 | 发现 |
|:-----|:-----|
| PASS | HSTS 1年 + includeSubDomains + preload |
| PASS | `SESSION_COOKIE_SECURE = True` |
| PASS | `CSRF_COOKIE_SECURE = True` |
| PASS | `X_FRAME_OPTIONS = "DENY"` |
| LOW | CSP 中间件排除 `/admin/` 路径 |
| INFO | JWT 使用 HS256（对称密钥），SECRET_KEY 同时用于 Django session 和 JWT 签名 |

---

## 五、功能与数据审查

### 5.1 核心功能

| 状态 | 发现 |
|:-----|:-----|
| PASS | 前端 History 模式 + 404 兜底路由 `/:pathMatch(.*)*` → `NotFound.vue` |
| PASS | 登录页为静态导入（首屏优化），其余 40+ 路由全部懒加载 |
| PASS | 后端 `/api/v1/schema/`、`/swagger/`、`/redoc/` 提供 OpenAPI 文档 |

### 5.2 权限系统

| 状态 | 发现 |
|:-----|:-----|
| PASS | RBAC 三层权限: 菜单权限 + 按钮权限 + 接口权限 |
| PASS | 写操作统一 `IsSystemAdmin`，读操作 `IsAuthenticated` |
| PASS | `AdminWritePermissionMixin` 复用写权限检查 |
| PASS | 未注册资产审批: `IsDeptManagerOrAbove`（部门经理+） |

### 5.3 数据初始化

| 状态 | 发现 |
|:-----|:-----|
| PASS | `docker-entrypoint.sh` 自动执行 `migrate --noinput` |
| PASS | 可选超级用户创建（通过 `DJANGO_SUPERUSER_*` 环境变量触发） |
| PASS | RBAC 权限表通过 data migration 初始化（`0005_rbac_permission_tables.py`） |

### 5.4 数据备份

| 状态 | 发现 |
|:-----|:-----|
| **CRITICAL** | `backups/backup_asset_management_backend_20260610_194154.sql` 已提交至 git 仓库（commit `6208685`），`backups/` 不在 `.gitignore` 中。**含数据库完整数据（MySQL dump），属于敏感信息泄露。** |
| MEDIUM | `scripts/backup.sh` 存在但无 cron 配置自动执行 |
| LOW | `backup.sh` 中 S3 桶名硬编码为 `s3://backup-bucket/postgres/` |
| INFO | 已有备份文件为 MySQL 格式（2026-06-10），与当前 PostgreSQL 不兼容 |

---

## 六、性能与部署审查

### 6.1 资源优化

| 状态 | 发现 |
|:-----|:-----|
| PASS | 前端 Gzip + Brotli 压缩已启用 |
| PASS | 路由懒加载（40+ 路由动态 import） |
| PASS | 代码分割 `manualChunks`（vue, element-plus, vendor, echarts 等） |
| PASS | 生产构建 Terser 压缩 + drop_console + drop_debugger |
| PASS | Sourcemap 关闭 |

### 6.2 服务托管与 Docker

| 状态 | 发现 |
|:-----|:-----|
| PASS | Daphne ASGI 服务器 + WebSocket 支持 |
| **HIGH** | Dockerfile **无 `USER` 指令** — 容器以 root 运行，违反 Docker 安全最佳实践 |
| **HIGH** | Dockerfile **无多阶段构建** — 构建工具（build-essential 等）残留在最终镜像中 |
| **HIGH** | `docker-compose.yml` 第48行 **health check 使用 `curl` 但镜像中未安装 `curl`** — 服务将永远显示 unhealthy |
| **HIGH** | `docker-compose.prod.yml` 第13行 使用 `:latest` 标签 — 非可复现构建 |
| MEDIUM | `docker-compose.prod.yml` 所有监控镜像使用 `:latest` |
| MEDIUM | web 端口 `8000:8000` 暴露在所有接口（未绑定 `127.0.0.1`） |
| LOW | 基础镜像 `python:3.12-slim` 未锁定补丁版本 |
| INFO | `docker-compose.monitoring.yml` 使用已废弃的 `version: "3.8"` |

### 6.3 健康检查

| 状态 | 发现 |
|:-----|:-----|
| PASS | `/health/` 端点: 检查 DB 连接 + 版本号 |
| PASS | `/ready/` 端点: DB 连接检查，失败返回 503 |
| PASS | Dockerfile `HEALTHCHECK` 使用 Python urllib（可用） |
| MEDIUM | `/health/` 端点即使 DB unhealthy 也返回 HTTP 200（应返回 503） |
| MEDIUM | `/health/` 和 `/ready/` 均不检查 Redis 连接（Redis 是 WebSocket 关键依赖） |
| HIGH | docker-compose health check 使用 `curl` 但容器中未安装 — 永远 unhealthy |

### 6.4 优雅停机

| 状态 | 发现 |
|:-----|:-----|
| PASS | `docker-entrypoint.sh` 以 `exec "$@"` 结尾，确保 Daphne 接收 PID 1 和 SIGTERM |
| PASS | Dockerfile CMD 使用 JSON 数组形式，Daphne 作为 PID 1 |
| MEDIUM | 无显式 `stop_grace_period` 配置（默认10秒），WebSocket 连接可能中断 |
| INFO | 无应用层 SIGTERM handler / atexit 处理 |

### 6.5 发布与回滚

| 状态 | 发现 |
|:-----|:-----|
| **HIGH** | **无 CI/CD 管道配置** — 无 GitHub Actions、Jenkinsfile 或其他 CI 配置。AGENTS.md 中引用的 `duplicate-guard.yml` 和 `security-scan.yml` 尚未实现 |
| INFO | 无版本记录文件（如 CHANGELOG） |
| INFO | 无回滚方案文档 |

---

## 七、监控与运维审查

### 7.1 日志与监控

| 状态 | 发现 |
|:-----|:-----|
| PASS | 后端 `RotatingFileHandler`: 10MB/文件, 保留5个备份 |
| PASS | 结构化 JSON 日志（time, level, logger, module, message） |
| PASS | `RequestContextMiddleware` 生成 trace_id 并存储于 thread-local |
| MEDIUM | `file` handler 已定义但**未挂载到任何 logger** — 文件日志实际未生效 |
| MEDIUM | `trace_id` 未自动注入到日志记录（需手动 `extra={"trace_id": ...}`） |
| INFO | Prometheus 配置存在但: ① 服务名 `app` 与 compose 服务名 `web` 不匹配 ② `/metrics/` 端点不存在 |

### 7.2 告警

| 状态 | 发现 |
|:-----|:-----|
| HIGH | **无告警配置** — 无 Sentry、无 Prometheus alerting rules、无错误率/CPU 监控 |
| INFO | Grafana/Prometheus docker-compose 存在但所有镜像用 `:latest`，监控端口暴露在所有接口 |

---

## 必须立即修复（CRITICAL + HIGH）

| # | 等级 | 问题 | 影响 | 建议修复 |
|:--|:-----|:-----|:-----|:-----|
| 1 | **CRITICAL** | `.env`（含DB密码）已提交至 git 历史（commit `6208685`） | 密码泄露 | ① 立即轮转数据库密码 ② 执行 `git filter-repo` 清除 ③ 通知所有协作者 |
| 2 | **CRITICAL** | `backups/` 目录含数据库备份已提交至 git | 数据泄露 | ① 添加 `backups/` 到 `.gitignore` ② `git filter-repo` 清除 ③ 评估备份是否含敏感信息 |
| 3 | **CRITICAL** | docker-compose health check 使用 `curl` 但镜像中未安装 | 服务永远 unhealthy，`depends_on: service_healthy` 阻塞 | 改用 Python urllib 或在 Dockerfile 中安装 curl |
| 4 | **HIGH** | Dockerfile 以 root 用户运行 | 容器逃逸风险 | 添加 `RUN useradd -m appuser && USER appuser` |
| 5 | **HIGH** | 无 Nginx / 反向代理配置 | 无 TLS 终结、无静态缓存、无 gzip、无安全头加固 | 配置 Nginx 反向代理 |
| 6 | **HIGH** | 无 CI/CD 管道 | 无自动化测试/扫描/部署 | 配置 GitHub Actions |
| 7 | **HIGH** | `/health/` 端点 DB 异常时仍返回 200 | 负载均衡器无法感知故障 | 返回 503 |
| 8 | **HIGH** | 生产镜像使用 `:latest` 标签 | 非可复现构建 | 使用 commit SHA 或语义版本标签 |
| 9 | **HIGH** | 无告警配置 | 故障无法及时发现 | 接入 Sentry + 配置 Prometheus alerting |
| 10 | **HIGH** | Dockerfile 无多阶段构建 | 构建工具残留在生产镜像 | 改为多阶段构建 |

---

## 建议修复优先级

| 优先级 | 项目 | 工作量 |
|:-------|:-----|:-------|
| P0 (立即) | 密码轮转 + git 历史清理（#1, #2） | 2小时 |
| P0 (立即) | docker-compose health check 修复（#3） | 15分钟 |
| P1 (上线前) | Dockerfile 非 root 用户（#4） | 30分钟 |
| P1 (上线前) | Nginx 反向代理配置（#5） | 2小时 |
| P1 (上线前) | `/health/` 返回 503（#7） | 10分钟 |
| P1 (上线前) | 镜像标签锁定（#8） | 15分钟 |
| P2 (尽快) | CI/CD 管道（#6） | 半天 |
| P2 (尽快) | Sentry + 告警（#9） | 2小时 |
| P2 (尽快) | 多阶段构建（#10） | 30分钟 |
| P3 (改善) | 清理前端 console.log（11处） | 20分钟 |
| P3 (改善) | 文件日志 handler 挂载 + trace_id 自动注入 | 1小时 |
| P3 (改善) | `/health/` 增加 Redis 检查 | 15分钟 |
| P3 (改善) | 前端 vite 添加 `base` 配置 | 5分钟 |
