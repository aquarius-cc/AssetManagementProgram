> ⚠️ 安全整改 2026-08-26：本文件原含明文凭据，已按根级规范 SC-1 脱敏；相关凭据已轮换失效。

﻿# 资产管理系统全面审查报告

> 审查日期：2026-08-21
> 审查范围：后端 (Django/DRF) + 前端 (Vue3/Element Plus) + Docker 部署 + 监控
> 审查方式：AI 自动化扫描 + 人工复核
> 版本基线：后端 commit 1e9effe / 前端 commit a640360

---

## 审查总览

| 类别 | CRITICAL | HIGH | MEDIUM | LOW | PASS |
|---|---|---|---|---|---|
| 一、代码与质量 | 0 | 3 | 4 | 3 | 5 |
| 二、环境与配置 | 0 | 1 | 2 | 1 | 3 |
| 三、前后端协同 | 0 | 0 | 1 | 2 | 4 |
| 四、安全审查 | 0 | 2 | 3 | 4 | 6 |
| 五、功能与数据 | 0 | 0 | 1 | 2 | 4 |
| 六、性能与部署 | 0 | 1 | 2 | 1 | 3 |
| 七、监控与运维 | 0 | 1 | 2 | 1 | 3 |
| **合计** | **0** | **8** | **15** | **14** | **28** |

**整体评价**：应用层代码质量成熟，安全防护体系完整（RBAC + JWT 双通道 + CSP + HSTS + 非 root 容器 + CI 门禁）。主要风险集中在**部署编排层的默认配置**（数据库/Grafana 弱口令、监控栈网络隔离）和**前端 console.log 残留与超限文件**。无 CRITICAL 级现存问题。

---

## 一、代码与质量审查

### 1.1 依赖版本锁定

| 检查项 | 状态 | 说明 |
|---|---|---|
| requirements/base.txt 全部 `==` 锁定 | PASS | 20 个依赖全部锁定版本 |
| requirements/dev.txt 全部 `==` 锁定 | PASS | 使用 `==` 锁定 |
| package.json 依赖 | PASS | 使用 `^` 锁定（npm 惯例） |
| package-lock.json 存在 | PASS | 已纳入版本控制 |
| pip-audit 安全审计 | **FAIL** | pip-audit 未安装，无法运行依赖漏洞扫描 |
| .venv 与 lockfile 漂移 | **HIGH** | drf-spectacular 锁定 0.28.0 但 venv 装了 0.29.0；PyJWT 锁定 2.10.1 但 venv 装了 2.12.1；另有 mysqlclient/drf-yasg 等未声明包存在于 venv |

**位置**：`requirements/base.txt`、`requirements/dev.txt`、`.venv/`

### 1.2 调试代码清理

| 检查项 | 状态 | 说明 |
|---|---|---|
| 后端 print/breakpoint/pdb | PASS | 应用代码中零命中 |
| 后端 TODO/FIXME/HACK | PASS | 零命中 |
| 前端 console.* 残留 | **MEDIUM** | 共 214 处，其中活跃 console.log 约 12 处（`HardDiskSNCard.vue:113`、`UserDetails.vue:501/505/523`、`BasicAssetDetails.vue:266`、`HardDiskSNBasicDetails.vue:192/194/216` 等）。生产构建 `vite.config.ts:136` 的 `drop_console: true` 会移除，但开发环境仍有调试残留 |
| 后端 migration print() | LOW | `0002_*.py:27,29,52` 和 `0010_*.py:113,116` 使用 print() 输出迁移进度（OC-2 缺口） |

### 1.3 错误处理

| 检查项 | 状态 | 说明 |
|---|---|---|
| 裸 except: | PASS | 全仓零命中 |
| try 无 handler | PASS | 仅合法 try/finally 清理块 |
| except Exception: pass | **HIGH** | `core/throttles.py:45-46,71-72` 登录限流静默失败；`apps/usermanagement/models.py:286-288` token 黑名单失败无日志（安全关键） |
| 前端空 catch | PASS | 零个完全空 catch |
| 前端仅 console 的 catch | LOW | 6 处 catch 仅打日志不上抛（`createEntityStore.ts:287`、`usePagedList.ts:74` 等） |

**位置**：`core/throttles.py:45,71`、`apps/usermanagement/models.py:286`

### 1.4 DR-5 文件规模（>500 行）

**后端超限文件（9 个，仅 1 个有豁免标记）**：

| 行数 | 文件 | 豁免 |
|---|---|---|
| 744 | `apps/assetmanagement/tests/test_outasset_selector.py` | NO |
| 607 | `apps/assetmanagement/state_machine/core.py` | NO |
| 592 | `core/tests/test_rbac.py` | NO |
| 578 | `apps/assetmanagement/migrations/0001_initial.py` | 自动迁移 |
| 576 | `apps/assetmanagement/selectors/outasset_selector.py` | YES |
| 574 | `apps/usermanagement/views/employee_view.py` | NO |
| 550 | `apps/assetmanagement/tests/test_asset_service.py` | NO |
| 532 | `apps/usermanagement/serializers.py` | NO |
| 521 | `apps/usermanagement/tests/test_service_coverage.py` | NO |
| 512 | `apps/assetmanagement/selectors/base_selector.py` | NO |

**前端超限文件（15 个，仅 1 个有豁免标记）**：

| 行数 | 文件 | 豁免 |
|---|---|---|
| 729 | `src/router/index.ts` | YES |
| 714 | `src/components/componentsdetails/detils/AssetBatchImport.vue` | NO |
| 630 | `src/components/componentsdetails/detils/ContractBatchImport.vue` | NO |
| 621 | `src/components/componentsdetails/detils/OutAssetForm.vue` | NO |
| 582 | `src/components/componentsdetails/detils/UserBatchImport.vue` | NO |
| 564 | `src/components/componentsdetails/UserDetails.vue` | NO |
| 551 | `src/components/componentsdetails/OperationLogDetails.vue` | NO |
| 538 | `src/components/componentsdetails/detils/UnregisteredAssetBasicDetails.vue` | NO |
| 518 | `src/components/componentsdetails/detils/UnregisteredAssetForm.vue` | NO |
| 506 | `src/components/componentsdetails/components/DepartmentEmployeeList.vue` | NO |

### 1.5 TypeScript 类型

| 检查项 | 状态 | 说明 |
|---|---|---|
| strict 模式 | PASS | tsconfig.app.json 显式开启 11 项严格检查 |
| 生产代码 any 泛滥 | PASS | 仅 5 处（`CommonList.vue:180-186` 的 emit 签名） |
| 测试代码 any 泛滥 | MEDIUM | 233 处，集中在 spec 文件 |
| 后端类型注解 | MEDIUM | views/employee_view.py 仅 1/20 函数有类型注解；models/asset.py QuerySet 方法零注解 |

### 1.6 代码重复（DR-1 违规）

| 重复模式 | 位置 | 规模 |
|---|---|---|
| 三重复制粘贴 ViewSet | `asset_lifecycle_view.py` BrokenAsset/LostAsset/FoundAsset | ~90 行重复 |
| MAX_BATCH_SIZE=100 重复 31 次 | 12+ 文件 | 应抽取为共享常量 |
| batch-result dict 组装 | 10+ 文件 | 应抽取为共享 helper |
| employee_service / department_service 镜像结构 | `employee_service.py:289-427` | 结构几乎相同 |
| throttles.py 重复限流逻辑 | `core/throttles.py` LoginRateThrottle vs LoginLockoutThrottle | 含相同的 silent pass |

### 1.7 命名规范

| 检查项 | 状态 | 说明 |
|---|---|---|
| PEP 8 CamelCase/snake_case | PASS | 全仓一致 |
| 违规 | LOW | `contract_view.py:154` 方法名 `getcontractByname`（应为 `get_contract_by_name`，但改名需同步前端） |

---

## 二、环境与配置审查

### 2.1 环境隔离

| 检查项 | 状态 | 说明 |
|---|---|---|
| production.py 独立配置 | PASS | DEBUG=False，SECRET_KEY/ALLOWED_HOSTS 强制环境变量 |
| development.py 独立配置 | PASS | DEBUG=True，仅允许 localhost |
| test.py 独立配置 | PASS | CORS_ALLOW_ALL_ORIGINS=True（测试专用） |
| docker-compose.prod.yml | PASS | 独立资源限制、日志轮转、健康检查 |

### 2.2 敏感信息

| 检查项 | 状态 | 说明 |
|---|---|---|
| 后端代码硬编码密码 | PASS | 仅测试夹具中有弱口令（可接受） |
| .env 未提交 git | PASS | git ls-files 无 .env；gitignore 已声明 |
| .env 历史记录 | PASS | git filter-repo 已清除旧密码历史 |
| 前端密钥入库 | LOW | `.env.development` 含真实加密密钥 `********（已脱敏，凭据已轮换失效）********`（SC-1 关注点，风险有限） |

### 2.3 配置校验

| 检查项 | 状态 | 说明 |
|---|---|---|
| SECRET_KEY 缺失即失败 | PASS | production.py:16-18 抛 ImproperlyConfigured |
| ALLOWED_HOSTS 缺失即失败 | PASS | production.py:37-40 |
| 弱密钥拦截 | PASS | production.py:25-34 黑名单 + 长度>=20 |
| DB_PASSWORD 缺失 | **HIGH** | production.py:48 默认 None，连接时才报错（应与 SECRET_KEY 同等严格） |
| assert 用于安全检查 | MEDIUM | production.py:12,94-95 用 assert 做 CSRF/CHANNEL_LAYERS 检查，python -O 下会跳过 |
| 前端 .env.production SERVER_URL | MEDIUM | `VITE_SERVER_URL=http://127.0.0.1:8000` 若被 network.ts 直连使用会指向用户本机 |

### 2.4 部署路径

| 检查项 | 状态 | 说明 |
|---|---|---|
| Vue base path | PASS | `vite.config.ts:23` 使用 `env.VITE_BASE_URL || '/'`，支持子路径部署 |
| Vue history mode | PASS | `createWebHistory(import.meta.env.BASE_URL)` |
| 前端 API baseURL | PASS | `VITE_API_BASE_URL || '/api/v1'`，与后端路由匹配 |

---

## 三、前后端协同与 API 审查

### 3.1 API 契约

| 检查项 | 状态 | 说明 |
|---|---|---|
| 前后端路径匹配 | PASS | 全部前端 API 调用均可找到对应后端路由 |
| 响应结构一致 | PASS | 后端 `{"code":0,"message":"","data":{}}` 与前端 `ApiResponse<T>` 匹配 |
| OpenAPI schema | PASS | drf-spectacular 配置完整，/api/v1/schema/ 可用 |
| 错误响应格式 | PASS | handler 统一返回 code/message/data，前端 pickErrorMessage 正确解析 |

### 3.2 接口地址

| 检查项 | 状态 | 说明 |
|---|---|---|
| API 基础地址 | PASS | 前端 `/api/v1` + axios baseURL 与后端 `/api/v1/` 路由匹配 |
| WebSocket 地址 | LOW | `useNotification.ts:41` 使用 `VITE_WS_BASE_URL || 'ws://127.0.0.1:8000'`（fallback 为回环地址） |

### 3.3 错误响应

| 检查项 | 状态 | 说明 |
|---|---|---|
| HTTP 状态码 | PASS | handler 按异常类型返回 400/403/404/500 |
| 业务错误码 | PASS | code 字段复用 HTTP 状态码 |
| 用户友好提示 | PASS | 前端按 401/403/404/500 分别弹 ElMessage |
| 表单校验错误 | PASS | handler 保留 field-errors dict 透传到前端 |

### 3.4 CORS 配置

| 环境 | 配置 | 状态 |
|---|---|---|
| base.py | 白名单 localhost 开发端口 | PASS |
| production.py | 仅环境变量，无 localhost 回退 | PASS |
| test.py | CORS_ALLOW_ALL_ORIGINS=True | 仅测试环境，可接受 |

---

## 四、安全审查

### 4.1 HTTPS 强制

| 检查项 | 状态 | 说明 |
|---|---|---|
| SECURE_SSL_REDIRECT | PASS | production.py:68 |
| SECURE_HSTS_SECONDS=31536000 | PASS | production.py:65-67 |
| SECURE_PROXY_SSL_HEADER | PASS | production.py:71，防重定向循环 |
| Nginx TLS 1.2/1.3 | PASS | default.conf.tpl:36-47 |
| HSTS 重复下发 | MEDIUM | Django + Nginx 同时下发 Strict-Transport-Security，应二选一 |

### 4.2 身份鉴权

| 检查项 | 状态 | 说明 |
|---|---|---|
| JWT 双通道认证 | PASS | Bearer + Cookie，authentication.py 实现 |
| CSRF 防护 | PASS | Cookie 通道对非安全方法强制 CSRF |
| Token 轮换 + 黑名单 | PASS | ROTATE_REFRESH_TOKENS=True |
| 刷新单飞 + 竞态宽容 | PASS | request.ts:111-136 |
| 后端接口权限校验 | PASS | permissions.py 7 类 RBAC，无部门/角色时走最严兜底 |
| 前端路由守卫 | PASS | guards.ts 实现角色 + 白名单 + 权限码三级校验 |
| /scan/:recordcode 免认证 | PASS | 公开扫码入口，后端 scan_view 无认证要求（设计意图） |

### 4.3 输入校验

| 检查项 | 状态 | 说明 |
|---|---|---|
| SQL 注入 | PASS | .raw()/.extra()/RawSQL 全仓零命中 |
| XSS 防护 | MEDIUM | CSP script-src 含 unsafe-inline + unsafe-eval（base.py:258） |
| 文件上传 | N/A | 当前无 FileField/upload_to；base.py 预置 10MB 上限 |
| 全局限流 | PASS | anon 20/min, user 100/min, login 5/min |

### 4.4 安全头

| 头 | 后端 | Nginx | 状态 |
|---|---|---|---|
| HSTS | production.py:65 | default.conf.tpl:50 | MEDIUM 重复 |
| X-Content-Type-Options | — | default.conf.tpl:51 | PASS |
| X-Frame-Options | — | default.conf.tpl:52 | PASS |
| X-XSS-Protection | — | default.conf.tpl:53 | LOW 已废弃 |
| CSP | csp_middleware.py | — (已移除) | MEDIUM 前端 SPA 无 CSP |
| Referrer-Policy | — | default.conf.tpl:54 | PASS |
| Permissions-Policy | — | default.conf.tpl:55 | PASS |

### 4.5 容器安全

| 检查项 | 状态 | 说明 |
|---|---|---|
| 非 root 用户 | PASS | Dockerfile USER appuser |
| 多阶段构建 | PASS | build-essential 不入最终镜像 |
| 固定基础镜像 | PASS | python:3.12.8-slim-bookworm |
| .dockerignore | PASS | .env 不进镜像 |
| Postgres 弱默认密码 | **HIGH** | docker-compose.yml:68 `POSTGRES_PASSWORD=${DB_PASSWORD:-assetpassword}` |
| Grafana 默认口令 + 公网暴露 | **HIGH** | docker-compose.monitoring.yml:17-19 |

---

## 五、功能与数据审查

### 5.1 核心功能路由

| 检查项 | 状态 | 说明 |
|---|---|---|
| 登录/注册/注销 | PASS | 前端 auth.ts + 后端 auth/urls.py 完整 |
| 主要 CRUD（资产/合同/仓库/部门/员工） | PASS | 前后端路径全部匹配 |
| History 模式刷新 | PASS | 404 catch-all 路由 `/:pathMatch(.*)*` → NotFound.vue |
| 懒加载 | PASS | 全部视图组件动态导入（Login.vue 静态导入可接受） |
| 公开扫码 | PASS | `/scan/:recordcode` 无认证 + 后端 public/scan/ |

### 5.2 权限系统

| 检查项 | 状态 | 说明 |
|---|---|---|
| RBAC 角色数据 | PASS | 数据迁移 0005 创建 5 角色 + 79 权限 |
| 角色-权限关联 | PASS | 迁移 0009/0012 补充 system_config:manage |
| 菜单权限控制 | PASS | AsideMenu.vue v-if 按权限码控制 |
| 按钮权限控制 | PASS | usePermission.ts hasRole/hasPermission |
| 接口权限控制 | PASS | DRF permission_classes 全覆盖 |

### 5.3 数据初始化

| 检查项 | 状态 | 说明 |
|---|---|---|
| 数据库迁移 | PASS | migrate --noinput |
| RBAC 初始数据 | PASS | 通过数据迁移自动创建 |
| 超管创建 | PASS | entrypoint.sh 按环境变量条件创建 |
| Demo 数据 | LOW | 无 fixtures/demo 脚本，新环境无示例数据 |

### 5.4 数据备份

| 检查项 | 状态 | 说明 |
|---|---|---|
| 备份脚本 | PASS | scripts/backup.sh (pg_dump + 30天保留) |
| S3 上传 | LOW | S3 桶名硬编码 `s3://backup-bucket/postgres/` |
| 自动调度 | MEDIUM | 无 cron/certbot 调度，依赖宿主机手动 crontab |
| 备份恢复测试 | MEDIUM | 未发现恢复测试文档或脚本 |

---

## 六、性能与部署审查

### 6.1 前端资源优化

| 检查项 | 状态 | 说明 |
|---|---|---|
| Gzip 压缩 | PASS | vite-plugin-compression (threshold 10240) |
| Brotli 压缩 | PASS | vite.config.ts:84 |
| 路由懒加载 | PASS | 全部组件动态导入 |
| sourcemap | PASS | 构建关闭 sourcemap |
| drop_console | PASS | terser 配置 drop_console + drop_debugger |
| vendor chunk | LOW | 全部 node_modules 打入单一 vendor chunk（echarts+element-plus+exceljs 体积大） |

### 6.2 后端性能

| 检查项 | 状态 | 说明 |
|---|---|---|
| 连接池 CONN_MAX_AGE=600 | PASS | base.py:126 |
| 连接健康检查 | PASS | CONN_HEALTH_CHECKS=True |
| Redis 通道层 | PASS | production.py 使用 RedisChannelLayer |
| select_related/prefetch_related | PASS | selector 层广泛使用 |
| 分页 | PASS | 自定义 CustomPageNumberPagination |

### 6.3 服务托管

| 检查项 | 状态 | 说明 |
|---|---|---|
| Docker 容器编排 | PASS | docker-compose.yml + prod override |
| 自动重启 | PASS | restart: unless-stopped |
| 优雅停机 | PASS | stop_grace_period: 30s |
| Daphne ASGI | PASS | WebSocket 支持 |
| 健康检查 | PASS | /health/ + /ready/ 检查 DB + Redis |

### 6.4 发布与回滚

| 检查项 | 状态 | 说明 |
|---|---|---|
| CI/CD workflow | PASS | .github/workflows/ 存在 |
| 镜像版本变量 | PASS | docker-compose.prod.yml 使用 IMAGE_TAG |
| 回滚方案 | LOW | 无显式回滚文档，可通过 git revert + 重新部署 |

---

## 七、监控与运维审查

### 7.1 日志

| 检查项 | 状态 | 说明 |
|---|---|---|
| JSON 结构化日志 | PASS | StructuredJSONFormatter 输出 time/level/module/message |
| trace_id 注入 | PASS | TraceIDFilter 自动注入 |
| file handler 挂载 | PASS | 3 个 logger 全部挂载 file handler |
| 日志轮转 | PASS | RotatingFileHandler 10MB x 5 |
| console 日志格式 | MEDIUM | 生产 console 使用 simple formatter（非 JSON） |

### 7.2 监控

| 检查项 | 状态 | 说明 |
|---|---|---|
| Prometheus 指标端点 | PASS | /metrics/ 暴露 5 个指标 |
| 请求指标采集 | PASS | PrometheusMiddleware 自动采集 |
| 路径归一化 | PASS | _normalize_path 防高基数标签 |
| Grafana 仪表盘 | MEDIUM | 无预配置仪表盘 JSON |
| 监控栈网络 | MEDIUM | docker-compose.monitoring.yml 未定义 networks，独立运行时服务发现失败 |

### 7.3 告警

| 检查项 | 状态 | 说明 |
|---|---|---|
| Prometheus alerting rules | PASS | 5 条规则：错误率/P95/P99/存活/积压 |
| Sentry 集成 | MEDIUM | 未接入，无错误追踪服务 |
| Grafana 告警 | LOW | 依赖手动配置 |

### 7.4 CI 安全门禁

| 检查项 | 状态 | 说明 |
|---|---|---|
| pip-audit 高危阻断 | PASS | .github/workflows/security-scan.yml |
| npm audit 高危阻断 | PASS | 同上 |
| 密钥模式扫描 | PASS | 检测 AK/SK/Token/密码模式 |
| 定时全量扫描 | PASS | 每周 cron 触发 |

---

## 问题汇总（按优先级排序）

### HIGH（8 项，建议上线前修复）

| # | 类别 | 问题 | 位置 |
|---|---|---|---|
| H-1 | 安全 | Postgres 弱默认密码 `assetpassword` | docker-compose.yml:68 |
| H-2 | 安全 | Grafana 默认口令 admin + 公网暴露 | docker-compose.monitoring.yml:17-19 |
| H-3 | 质量 | DR-5 超限文件：后端 8 个 + 前端 14 个无豁免标记 | 见 1.4 节 |
| H-4 | 质量 | 代码重复：三重 ViewSet + MAX_BATCH_SIZE 重复 31 次 | asset_lifecycle_view.py + 12 文件 |
| H-5 | 质量 | except Exception: pass 吞掉安全关键异常 | throttles.py:45,71 / models.py:286 |
| H-6 | 配置 | DB_PASSWORD 缺失不立即失败 | production.py:48 |
| H-7 | 质量 | .venv 与 lockfile 版本漂移 | .venv/ vs requirements/*.txt |
| H-8 | 部署 | 监控栈端口绑 0.0.0.0 + 网络隔离致服务发现失败 | docker-compose.monitoring.yml |

### MEDIUM（15 项，建议迭代修复）

| # | 类别 | 问题 | 位置 |
|---|---|---|---|
| M-1 | 安全 | CSP unsafe-inline + unsafe-eval | base.py:258 |
| M-2 | 安全 | HSTS 重复下发（Django + Nginx） | production.py:65 + default.conf.tpl:50 |
| M-3 | 安全 | 前端 SPA 无 CSP 保护 | Nginx 服务静态文件无 CSP 头 |
| M-4 | 安全 | Redis 无认证 | docker-compose.yml:91-98 |
| M-5 | 配置 | assert 用于安全检查（python -O 下跳过） | production.py:12,94 |
| M-6 | 配置 | .env.production VITE_SERVER_URL 指向 127.0.0.1 | .env.production:4 |
| M-7 | 质量 | 前端 console.* 残留 214 处 | 见 1.2 节 |
| M-8 | 质量 | 后端类型注解不完整 | employee_view.py 1/20 |
| M-9 | 质量 | 测试代码 any 泛滥 233 处 | src/stores/__tests__/*.spec.ts |
| M-10 | 功能 | 备份无自动调度 | scripts/backup.sh |
| M-11 | 功能 | 备份恢复测试无文档 | — |
| M-12 | 监控 | 无 Sentry 错误追踪 | — |
| M-13 | 监控 | 无预配置 Grafana 仪表盘 | — |
| M-14 | 部署 | 备份无自动调度（重复） | — |
| M-15 | 部署 | console 日志非 JSON 格式 | base.py console handler |

### LOW（14 项，可后续优化）

| # | 类别 | 问题 |
|---|---|---|
| L-1 | 安全 | JWT 密钥与 SECRET_KEY 耦合 |
| L-2 | 安全 | /admin/ 跳过 CSP |
| L-3 | 安全 | 登录计时侧信道用户枚举 |
| L-4 | 安全 | X-XSS-Protection 已废弃 |
| L-5 | 安全 | /metrics/ /health/ /ready/ 未鉴权（外部暴露有限） |
| L-6 | 配置 | CSRF_TRUSTED_ORIGINS 未设置 |
| L-7 | 配置 | 前端开发密钥入库 |
| L-8 | 配置 | DB_PASSWORD 默认 None |
| L-9 | 功能 | 方法名 getcontractByname 不规范 |
| L-10 | 功能 | Demo 数据无 fixtures |
| L-11 | 部署 | vendor 单 chunk 体积大 |
| L-12 | 部署 | 回滚方案无文档 |
| L-13 | 监控 | S3 桶名硬编码 |
| L-14 | 监控 | 无 Grafana 告警配置 |

---

## 修复优先级建议

### 上线前必须修复（H-1, H-2, H-6）

1. **H-1**：docker-compose.yml 中 `POSTGRES_PASSWORD` 改为 `${DB_PASSWORD:?请设置数据库密码}`，移除弱默认值
2. **H-2**：docker-compose.monitoring.yml 中 Grafana 端口改为 `127.0.0.1:3000:3000`，密码改用强随机值
3. **H-6**：production.py 中 DB_PASSWORD 添加 `if not DB_PASSWORD: raise ImproperlyConfigured(...)`

### 短期修复（1-2 周）

4. **H-5**：throttles.py/models.py 的 `except Exception: pass` 至少添加 logger.warning
5. **H-7**：重建 .venv 使其与 lockfile 一致
6. **M-1/M-3**：评估是否可移除 unsafe-eval；为前端 SPA 添加 CSP 头
7. **M-5**：assert 改为 if not ... raise ImproperlyConfigured

### 中期优化（1-2 月）

8. **H-3/H-4**：拆分超限文件，提取重复代码为共享模块
9. **M-7**：清理前端活跃 console.log
10. **M-10/M-11**：配置定时备份 + 编写恢复测试文档
11. **M-12**：接入 Sentry 或等效错误追踪服务