# 资产管理系统 — 第四轮复核结论（AtomCode）

- **审查 Agent**：AtomCode
- **复核日期**：2026-08-29
- **复核目标**：核查第三轮报告（`全面审查报告_AtomCode_第三轮_2026-08-28.md`）及 N-2/N-3 修复方案（`N-2_N-3_修复方案_AtomCode_2026-08-28.md`）交付后，全部遗留项（N-1~N-8 + M-3/M-4/M-6 残留）的当前修复状态
- **复核方式**：git 提交考古 + 文件落位逐一核对（file:line 证据）+ 后端 pytest / 前端 vitest 全量实测
- **关联文档**：`M_修订执行记录_2026-08-29_AtomCode.md`（二次对抗审核 V-1~V-6 修订记录）

---

## 0. 执行摘要

**实测证据（第四轮新鲜数据）**：

| 套件 | 结果 | 对比第三轮 |
|---|---|---|
| 后端 pytest | ✅ **1062 passed**（248s，82 warnings） | 第三轮 1 failed / 1061 passed → **N-1 回归已消除** |
| 前端 vitest | ✅ **102 文件 / 1421 passed**（75s） | 持平，全绿 |
| git 同步 | ✅ FE `master` == `origin/master` == `a2e51a3`；BE `master` == `origin/master` == `82da269`；根仓库 gitlink 已 bump 至两最新 commit | 全部修复已推送，无本地滞留 |

**一句话结论**：代码层红灯已全部清除（测试双全绿、无未提交修复），**当前无阻断发布的代码问题**；剩余 6 项均非代码阻断项，其中 N-8（CI 子模块 checkout）是唯一可能隐藏阻断项，需人工确认 CI 实际挂载仓库后定性。

---

## 1. 已完成项（第三轮报告之后新增，逐一验证）

| 编号 | 修复内容 | commit | 验证证据 |
|---|---|---|---|
| **N-1** | 测试断言更新为 `== "INFO"` 并改名 `test_console_level_is_info`，与 `production.py:95` INFO 级对齐（未回退 R-4 设置） | `2e0de61` | `config/tests/test_production_settings.py:76-77` 断言已改；pytest 1062 全绿 |
| **N-2** | 备份 textfile 管道全落地：`scripts/backup_status.sh`（EXIT trap 原子写 + flock + `|| true` 降级）+ 3 个日备脚本接入（含 `_BACKUP_SKIP=1` 排除跳过分支）+ `docker/backup/Dockerfile:13` COPY + `docker-compose.yml:187/198/231`（`BACKUP_STATUS_DIR` env + `backup_status_data` 卷）+ `docker-compose.monitoring.yml:72/77`（node-exporter ro 挂载 + `--collector.textfile.directory=/textfile`） | `373236b` | 5 处改动逐一核对在位；方案①~⑤ 全部实施，无缩水 |
| **N-3** | blackbox TLS 探针全落地：`docker/blackbox/` 4 文件（边车 Dockerfile / entrypoint.sh / blackbox.yml.tpl / probes.yml.tpl）+ monitoring 栈 2 新服务（`blackbox-config-renderer` + `blackbox`，`blackbox_rendered` 共享卷）+ `prometheus.yml:34-50`（`blackbox` file_sd job，30s 热加载）+ `alert_rules.yml` 3 条守卫规则（`BlackboxTargetDown` / `BlackboxExporterDown` / `TlsCertMetricMissing`）+ `.env.example:61` 补 `DOMAIN` | `82da269` | 方案①~⑧ 全部在位；原 2 条 TlsCert 规则表达式未动 |
| **N-7** | `.gitignore` 补 `*.log` / `*.log.*` / `django.log*`；BE 工作区干净 | `2e0de61` | `git status --short` = 0 行 |
| **N-5（脚本部分）** | locust 场景 3→5：补齐 `locustfile_dashboard.py` / `locustfile_asset_create.py`，与 README 所列三场景对齐 | `4d76f0e` | `scripts/loadtest/` 现有 5 文件 |
| **R-1（远端同步部分）** | 全部修复 commit 已推送远端，本地/远端/gitlink 三方一致；根仓库 gitlink 经 `700c087` 等 5 次 bump 与子模块尖端同步 | — | 两子模块 `git rev-list --left-right --count origin/master...master` 均 = `0 0` |

> 实施过程中另有 `M_修订执行记录_2026-08-29_AtomCode.md` 记载的 V-1~V-6 对抗审核修订（含迁移编号 0014→0017 修正），已包含在 `3c7d2fb` 等既有 commit 中。

---

## 2. 仍未完成项（剩余清单）

| 编号 | 状态 | 剩余动作 | 风险定性 |
|---|---|---|---|
| **N-4**（部分） | 4 处字面量只迁移 1 处 | `AsideMenu.vue:145-147`（`damaged:approve` / `unregistered:approve` / `auditlog:read`）与 `usePermission.ts:111`（`system_config:manage`）仍为裸字面量；已迁移处（`AsideMenu.vue:144`）采用 `PERMISSION_CODES.SYSTEM_CONFIG_MANAGE \|\| 'system_config:manage'` 冗余兜底（常量必然存在，`||` 分支可清理）。剩余 3 处为同构小迁移，零逻辑风险 | 🟡 低——拼写保护覆盖面 1/4，但后端鉴权不依赖前端常量，无安全影响 |
| **N-5 / M-4**（执行部分） | 脚本齐备，**基线未执行、无基线文档** | 预发环境执行 10/50/100 三档压测，产出基线文档（含 P95/P99、错误率、资源水位）。纯执行动作，需预发环境与 token 池（`gen_tokens.py` 已就绪） | 🟡 中——容量结论仍无量化依据，属发布窗口内动作 |
| **N-6 / M-3** | **未实施** | 无 `scripts/export_permission_codes.py`；全部 workflow 无 contract/spectacular diff job；契约快照 `asset-management-api.json` 仍为 2025-12-29 旧版。两阶段（重建基线 → CI 归一化 diff 门禁）需整体落地 | 🟠 中高——契约漂移无门禁约束，剩余项中工作量最大 |
| **M-6.2/6.3** | **未实施** | 无生成式常量脚本（后端 `MODULES_CONFIG` → 前端 `permissionCodes.ts`），前端常量仍为手工维护；无 CI 词表守卫（F ⊆ S 单向断言）。双源漂移风险仍在 | 🟡 中——0017 已消除潜伏缺陷，当前无功能/安全问题，属防退化建设 |
| **N-8** | **未实施（待定性）** | 全部 workflow 仍无 `submodules: true`，job 以 `working-directory: asset_management_backend` 运行而 BE 自身无 `.github`。**若 CI 实际挂载根 superproject 远端 → checkout 子模块目录为空、job 必然失败（阻断）；若 CI 实际挂载子仓库远端 → 不适用**。需人工确认 CI 实际运行仓库后定性 | 🔴 待定性——唯一可能隐藏的阻断项 |
| **R-1 收尾** | 远端残留分支 | FE 远端仍有 `origin/main`（孤立，与 master 无共同祖先）/ `origin/dev` / `origin/fix/npm-audit-high-vulnerabilities`，主干唯一性在远端层面未收尾 | 🟢 低——CI 已绑 master，功能无碍，纯卫生项 |
| 卫生项 | 根仓库 5 份文档未提交 | `docs/` 下 4 份审查报告 + 1 份方案/执行记录处于 untracked，建议随收尾一并纳入版本管理 | 🟢 低 |

---

## 3. 发布就绪度判断（第四轮）

1. **代码层：已就绪**。测试双全绿、构建/audit/lint 第三轮已验证、无未提交代码修复、无已知功能/安全缺陷。
2. **监控层：数据源已补齐，待生产首跑确认**。N-2/N-3 管道已落地，需在生产首跑后确认：`time()-backup_last_success_timestamp < 36000`、`probe_ssl_earliest_cert_expiry > time()`、`up{job=~"node|blackbox"} == 1` 三项均真；并确认生产 `.env` 已显式设置 `DOMAIN`（N-3 前置人工动作，renderer 对空值 fail-fast 不会静默渲染错配置）。
3. **唯一待定性阻断风险：N-8**。确认 CI 实际挂载仓库前，不能勾选发布检查单中"CI 门禁生效"一项。
4. **发布窗口内动作**：N-5/M-4 压测基线执行（如容量预估有变化则必须执行）。

### 建议收尾顺序

```
① N-8 定性（人工确认 CI 仓库，10 分钟）
② N-4 剩余 3 处字面量迁移 + 清理 || 兜底（单 commit，FE 侧零逻辑风险）
③ M-3 两阶段落地（契约基线重建 → CI diff job，含 N-8 结论修正的 checkout 配置）
④ M-6.2/6.3（生成式常量 + CI 词表守卫，与 ③ 的 CI job 可同批）
⑤ R-1 远端残留分支清理 + 根仓库 docs/ 文档提交（卫生项，随 ②④ 的 commit 批次）
⑥ 发布窗口内：M-4 压测基线执行 → 产出基线文档
```

*结论完 — AtomCode，2026-08-29 第四轮*