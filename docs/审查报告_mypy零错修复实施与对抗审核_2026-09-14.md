# 审查报告：mypy 零错修复 —— 实施与对抗审核

> 执行引擎：big-pickle | 实施日期：2026-09-14
> 范围：`asset_management_backend` 全仓 mypy 31→0（双门禁 + 0 unused-ignore），含 M-1 假签名收敛与"分类三"真实缺陷修复、脚本/容器范围内 13 处类型标注、CT-4 回归锚点。
> 前置依据：本会话对"两份修复方案"的逐条核验（对话内），未在仓库文档单独成文；本报告为实施落地后的唯一事实留痕。

---

## 一、目标与验收指标

| 门禁 | 命令 | 结果 |
|:--|:--|:--|
| 全仓 mypy | `mypy . --config-file pyproject.toml` | 0 issues / 208 files ✅ |
| 四入口 mypy（Makefile/CI 口径） | `mypy apps core config utils` | 0 issues / 204 files ✅ |
| unused-ignore | 依托 `warn_unused_ignores = true`（pyproject.toml） | 0 条 ✅ |
| 静态检查 | `ruff check` 于本次改动文件 | 全部通过 ✅ |
| 全量测试 | `pytest apps -q --reuse-db` | **927 passed** ✅ |
| 管理命令冒烟 | `python manage.py check_admin` | `[PASS]` exit 0 ✅（修复前运行即 FieldError 崩溃）|
| 权限库一致性 | `python scripts/generate_permission_codes.py --check` | BE/FE 均 sync ✅ |
| 重复代码护栏 | `python scripts/check_duplicate_invariants.py` | PASS ✅ |
| 容器脚本可编译 | `python -m py_compile docker/feishu-webhook/app.py` | ✅ |

> mypy 复跑统一使用隔离缓存目录（`MYPY_CACHE_DIR` 指向临时目录），杜绝 `.mypy_cache` 陈旧结果假象。

---

## 二、变更清单

### 2.1 M-1：batch_* 假签名收敛（batch_delete ×8 + batch_create ×1）

| 文件 | 变更 |
|:--|:--|
| `core/batch_mixins.py:286` | `delete_response` 返回类型 `-> Any` → `-> Response`（`success_response` 实际恒返 `Response`，见 `utils/response_utils.py`） |
| `asset_type_view / contract_view / storage_view / waste_asset_view / recycle_asset_view / out_asset_view / damaged_asset_view / department_view`（batch_delete） | 方法签名 `-> None` → `-> Response`；删除 `@action` 行 `ignore[type-var]` 与 return 行 `ignore[no-any-return]` 各 1 处 |
| `department_view.batch_create` | 同法收敛（原方案漏项，一并补全） |
| `employee_view`（2 处）、`unregisteredasset/views.py:353` | 收敛后不再必需的 `ignore[no-any-return]` 删除 |

> 净效果：删除 14 处 ignore 注释（8 × type-var + 8 × no-any-return，重合于 8 个 batch_delete + batch_create 相关）。

### 2.2 分类三：7 处真实缺陷修复（核验后为 8+ 处)

| 位置 | 修复 | 说明 |
|:--|:--|:--|
| `contract_view.py:193-199` | 金额转换 `float(amount)` → `Decimal(str(amount))` + `except (ValueError, InvalidOperation)` + `is_finite()` 拦截 + **量级上限护栏** | NaN/Inf/超 `DecimalField(max_digits=12)` 值统一 400，禁止穿透落库/DB DataError 500 |
| `storage_view.py:86` | `code = item["storage_type"] or ""` | 规避 `Storage.storage_type`（`null=True`）为 NULL 时的类型错乱 |
| `asset_selector.py:132` | `cast(Asset, ...)` | 与同文件 :120 现有 cast 风格一致 |
| `config/settings/base.py:274` | `LOGGING: dict[str, Any] = {...}` | 消除 settings 链上未注解结构类型 |
| `config/settings/production.py` / `test.py` | 删除连锁失效的 5 处 `ignore[index]` | 含 test.py:85（原方案漏项） |
| `apps/authusermanagement/models.py:134/142` | `groups` / `user_permissions` 附 `ignore[assignment]` | 精确压制 django-stubs 覆盖 related_name 的错报 |
| `check_admin.py` | `is_active` → `auth_is_active`；`-> Any` 签名；`admin.get_username()` / `admin.pk` | 顺带修复运行期 `FieldError`（`is_active` 非 AuthUser 字段，`filter(is_active=True)` 即时崩溃）；`username`/`id` 属性在 AuthUser 上不存在，原命令 PASS 分支运行即碎 |
| `consumer.py:66` | `asyncio.Task[Any] \| None` | 泛型参数补全 |

### 2.3 范围外（scripts / docker）13 处纯类型标注

| 文件 | 变更 |
|:--|:--|
| `scripts/generate_permission_codes.py`（5 处） | `dict` → `dict[str, Any]`；`:31` 用 `cast(dict[str, Any], ast.literal_eval(...))` |
| `docker/feishu-webhook/app.py`（8 处） | `dict` → `dict[str, Any]`、`tuple` → `tuple[bool, str]`、HTTP handler 显式 `-> None`、`log_message(fmt: str, *args: Any)` |

### 2.4 CT-4 回归锚点（新增测试 5 例）

- `test_contract_view_api.py`：`test_payment_record_cumulative_precision`（0.1×3 精确 = 0.30，float 回退必失败）；`test_payment_record_rejects_garbage` / `_nan` / `_inf` / `_overflow`（400 而非 500）。
- `test_contract_service.py`：`test_add_payment_decimal_cumulative_precision`（Decimal 累加 0.1×3 = 0.3 精确）。

---

## 三、行为变更（已确认/待确认清单）

| # | 变更 | 旧行为 | 新行为 | 状态 |
|:--|:--|:--|:--|:--|
| B1 | contract 金额校验 | 非数字 400；NaN/Inf/超大数可能穿透落库或 500 | 非数字/NaN/Inf/超上限一律 400 | 已确认 |
| B2 | check_admin 语义 | `filter(is_active)` 运行期 FieldError 崩溃 | 以 `auth_is_active` 判定真实有效标记，冒烟 `[PASS]` | 已确认 |
| B3 | storage 统计 NULL 键 | 序列化为 JSON 键 `"null"` | 输出键 `""`（仅当存在 `storage_type=NULL` 记录时） | **待前端确认空键渲染** |
| B4 | `unregisteredasset/views.py:138` | `ignore[arg-type]` | `ignore[union-attr, arg-type]`（双错误码合一；该行本就有真实未覆盖错误） | 已确认 |

---

## 四、对抗审核结论与处置

只读子代理对全部改动做了对抗审核（复跑双门禁/179 定向测试、逐条核查 9 个审查点）。结论：

1. **针对"mypy 31→0 任务本身"：通过。** 9 个审查点全部给出 √ / 有据可依，未发现新增类型隐患、行为回归或跨端根契约破坏。
2. **若以"干净 PR"整体论：不通过 → 已处置。** 对抗审核发现 2 项应修项，本次已全部修正：

| 编号 | 发现 | 严重度 | 处置 |
|:--|:--|:--|:--|
| M-1 | `payment_record` 量级溢出（`1e308` 过 is_finite）→ DB DataError 500 | 中 | **已修复**：追加 `amount > Decimal("9999999999.99")` → 400 + 回归测试 |
| M-2 | `authusermanagement/services.py:303` 注释含 U+FFFD 乱码 | 中 | **已修复**：恢复「黑名单」 |

3. **信息级事项（未触碰，涉其它会话在途工作）**：
   - **H-1/H-2（高）**：工作区 diff 同时包含 Django 6.0.5 升版、BE-02 改密 token 吊销、资产更新接口收紧（BE-01）三项**在途工作**（非本次 mypy 修复内容，系其它会话未提交变更，git 无法切分）。合并前建议拆分为独立提交/PR；其中资产更新收紧属跨端 API 行为变更，**须前端验收**后合入。
   - **L-1（低）**：`storage_view.statistics` NULL 键输出由 `"null"` 变 `""`——为契约知悉项，见 §三 B3。
   - **L-2（低）**：BE-02 `update()` 先吊销 token 后落库且未包事务，save 失败会导致 token 失效而资料未改（可选护栏：吊销移到 save 后或纳入事务）。
   - **L-3（低）**：`check_admin` 的 `assert` 在 `python -O` 下被剥离（管理命令场景可忽略）。
   - **L-4（低）**：pyproject 分类器仍标 `Django :: 5.2`，与运行时 6.0.5 不符（随 H-1 升版一并修正）。

---

## 五、规则符合性

- CT-1/CT-3/CT-5：核心修复均有测试覆盖（含状态机/金额精度路径）；测试失败即阻塞全程遵守；全量 927 passed 后收稿。
- CT-4：新增 5 例回归锚点，任一回退（float 化 / 删 is_finite / 删量级护栏）即测试失败。
- DR-1：响应组装统一走 `BatchResponseHelper`；DR-5：无改动文件超限（最高 `asset_view.py` = 500 行存量）、无新增函数超 50 行。
- SC-1/SC-3：无密钥入库；无 SQL 拼接；`Decimal` 路径避免了 NaN/Inf 落库风险。
- 跨端契约：API 根结构/分页/状态枚举/时间格式未变；仅 B3（`by_type` 空键）与 H-1 资产收紧属行为级知悉项。

---

## 六、遗留/后续

1. H-1/H-2 拆分提交与前端验收（在途其它会话的工作，非本次范围）。
2. B3 前端确认 `by_type` 空键 `""` 的渲染语义。
3. L-2 可选事务护栏（是否纳入由 BE-02 负责人决定）。
4. 本报告所述变更在子模块内未提交；提交时建议仅纳入本次 mypy 修复相关文件，避免混入 H-1 在途变更。