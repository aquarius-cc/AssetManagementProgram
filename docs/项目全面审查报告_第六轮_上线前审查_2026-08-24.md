# 上线前全面审查报告（第六轮）

> 审查日期：2026-08-24
> 审查范围：前端(vue-assetmanagement) + 后端(asset_management_backend) + 部署配置(Docker/Nginx/监控)
> 审查框架：代码质量 / 环境配置 / 前后端协同 / 安全 / 功能与数据 / 性能与部署 / 监控运维
> 状态标记：✅ 通过 | ⚠️ 有条件通过(附条件) | ❌ 未通过 | ⏳ 待办

---

## 总览

| 类别 | 结论 | 阻断项 |
|:--|:--|:--|
| 一、代码与质量 | ✅ 通过 | 无 |
| 二、环境与配置 | ⚠️ 条件通过 | G-1 生产 CORS 白名单需在部署 env 中显式设置 |
| 三、前后端协同与 API | ✅ 通过 | 无 |
| 四、安全 | ⚠️ 条件通过 | G-2 存量密码轮换未执行(git 历史 P2-1) |
| 五、功能与数据 | ⚠️ 条件通过 | G-3 数据库初始数据(管理员/角色)无自动化来源 |
| 六、性能与部署 | ⚠️ 条件通过 | G-4 nginx 未启用 gzip；G-5 未做压测 |
| 七、监控与运维 | ✅ 通过(本轮补齐) | 无 |

**结论：完成 6 项条件整改并履行既有待办后，具备上线条件。**

---

## 一、代码与质量审查

### 1.1 代码合并与构建 ✅
- 前端生产构建实证通过：`vite build --mode production` → `✓ built in 35.55s`，产物 121 个 JS chunk + index.html 完整。
- 本会话曾发现并修复**构建完全失败**的阻断缺陷：DR-5 拆分生成的 `<style src="X.scss">` 缺 `./` 前缀（commit `6ce9f72`）。教训：vitest/vue-tsc 无法覆盖 style src 错误，构建必须纳入发布前必检项。
- 后端全量测试 **864 用例全绿**（最近一次全量运行）。

### 1.2 依赖锁定与安全审计 ✅
- 前端：`package-lock.json` 锁定版本；`npm audit --audit-level=high` → **0 vulnerabilities**。
- 后端：requirements 分层锁定精确版本（如 `daphne==4.2.2`、`sentry-sdk[django]>=2.0,<3.0`）。
- ⏳ 待办：后端 `pip-audit` 未安装未执行，建议纳入 CI（此前 SC-7 已规划，标记 PENDING）。

### 1.3 调试代码清理 ⚠️
- 前端残留 **22 处 console.log/debug**（已注释的除外），典型：HardDiskSNCard.vue:113、BasicAssetDetails.vue:266 等。均为信息性输出非密钥泄露，但违反整洁发布标准。
- 建议：统一接入日志封装或在构建期用 esbuild drop 配置自动剥离。
- 后端：无 print 调试残留（抽查）；日志经 LOGGING 统一管理。

### 1.4 日志轮转 ✅
- 后端 file handler：`RotatingFileHandler maxBytes=10MB backupCount=5` ✓
- 容器层：prod compose `json-file max-size=10m max-file=3` ✓

### 1.5 类型与规范 ✅
- `vue-tsc --build` 零错误（本会话多次验证）。
- 后端 mypy strict 门禁覆盖 employee_view + asset model（commit `1ea96bd`），渐进收紧机制就位。
- 冗余代码：本会话 DR-1 治理净减约 2500 行（前端），B-5 全部收敛。

---

## 二、环境与配置审查

### 2.1 环境隔离 ✅
- settings 分层：base.py + development/test/production 三模块，`DJANGO_SETTINGS_MODULE` 切换。
- production 独有加固：DEBUG=False、SECURE_SSL_REDIRECT=True、Cookie Secure、Redis channel 强制、日志 JSON 化。

### 2.2 敏感信息 ✅（附历史遗留）
- 入库文件扫描：`.env.production` 内容为纯配置无密钥 ✓；`VITE_TOKEN_CRYPTO_KEY` 注释留空勿提交 ✓
- 后端硬编码扫描：`(password|secret|token)=["']...{8,}` 模式 **0 命中**
- ❌ 历史遗留（P2-1）：git 历史中存在含真实 DB 密码的 .env（第五轮已登记）。**上线前必须轮换该密码**，轮换后旧值作废即消除风险。

### 2.3 配置启动校验 ✅（本会话大幅补强）
production.py 启动即 fail-fast 清单：
| 校验项 | 方式 |
|:--|:--|
| SECRET_KEY 缺失/弱密钥(<20字符) | ImproperlyConfigured |
| ALLOWED_HOSTS 缺失 | ImproperlyConfigured |
| REDIS_URL 缺失 | ImproperlyConfigured |
| InMemoryChannelLayer | ImproperlyConfigured |
| DB_PASSWORD 缺失 | ImproperlyConfigured（本会话新增方案已确认）|
| cors_origos NameError | ✅ 已修复(commit `3aca7e7`)——修复前生产 settings 导入即崩 |

### 2.4 部署路径 ✅
- vite `base: env.VITE_BASE_URL || '/'` 可注入；nginx `try_files $uri $uri/ /index.html` 支持 History 刷新 ✓
- ⚠️ 条件：部署于子路径时须同时设置 VITE_BASE_URL 与 nginx 的对应 location，当前默认 `/` 与 nginx 根路径部署匹配。

---

## 三、前后端协同与 API 审查

### 3.1 API 契约 ✅
- 后端 drf-spectacular 自动生成 OpenAPI schema ✓
- 响应根结构 `{code, data, message}` 由 ResponseWrapperMixin 统一 ✓（跨端契约 §3）

### 3.2 接口地址 ✅
- 主 API：前端 `VITE_API_BASE_URL=/api/v1` 相对路径 → Nginx `/api/` 反代 → daphne:8000，链路一致 ✓
- 诊断工具直连地址（network.ts SERVER_URL）已在生产移除回退值 ✓

### 3.3 错误响应 ✅
- 统一 `{code(HTTP语义), message, data}`；业务错误码体系 VALIDATION_ERROR/INTERNAL_ERROR/NOT_FOUND 等
- B-5/B-8 治理后批量接口错误码全项目一致（CREATE_FAILED 单码制已消灭）

### 3.4 CORS ✅
- base.py L378：「移除 CORS_ALLOW_ALL_ORIGINS，仅使用白名单」✓
- ⚠️ 条件 G-1：生产部署 env 必须显式设置 `CORS_ALLOWED_ORIGINS=https://<域名>`（缺失时白名单为空，前端跨域场景会被拒）

---

## 四、安全审查

### 4.1 HTTPS 强制 ✅
- Nginx 模板 L21：HTTP server `return 301 https://...` ✓
- Django SECURE_SSL_REDIRECT=True 双保险 ✓

### 4.2 身份鉴权（后端强制）✅
- 权限三层实现：Permission 类（IsSystemAdmin/IsAssetAdminOrAbove 等）+ QuerySet 过滤（get_queryset_for_user）+ 序列化器校验，非仅前端隐藏
- 回归证据：test_admin_permissions.py / test_asset_view_rbac.py / test_hard_disk_sn_view_rbac.py 等 RBAC 测试套件全绿
- JWT Cookie Secure + 认证登录锁定（LoginLockoutThrottle）✓

### 4.3 输入校验 ✅
- 全部数据访问走 Django ORM，`raw()/cursor.execute` **0 处**（SQL 注入面消除）
- DRF Serializer 层全量输入校验；XSS：前端 v-html/dangerouslyUseHTMLString **0 处** + CSP 头

### 4.4 安全响应头 ⚠️（HSTS 治理待落地）
- Nginx 模板：HSTS(2年+preload+always) / nosniff / X-Frame-Options DENY / Referrer-Policy / Permissions-Policy，server 级 + static/media 显式重复 ✓
- CSP：Django 中间件下发，script-src 含 unsafe-eval（exceljs vm-shim 依赖，构建产物实证）✓
- ⏳ 待办（本会话审计确认）：① HSTS 在 Django 与 Nginx 双下发（max-age 1年/2年冲突），治理方案已审未执行；② 兄弟头（nosniff/x-frame/referrer-policy）因 Django 默认值仍在重复下发，Referrer-Policy 存在值冲突

---

## 五、功能与数据审查

### 5.1 核心功能 ✅
- 登录/CRUD/状态机流转由 864 个后端测试覆盖；前端 1420 用例（Store/API/组件）
- History 刷新：nginx try_files fallback 已验证 ✓

### 5.2 权限系统 ✅
- RBAC 三层（角色-菜单-按钮-接口）由 Permission + QuerySet 双过滤实现；第一轮 C-2 修复后持续回归

### 5.3 数据初始化 ❌（G-3，阻断级缺口）
- management commands 仅 check_data_consistency/export_schema；**无 init_admin/init_rbac 种子命令或 fixture**
- 生产库建表后将缺少管理员账号、角色、菜单数据，系统无法首次登录
- **必须补齐**：新建 `init_production_data` 管理命令（创建超管、默认角色、菜单树），幂等可重复执行

### 5.4 数据备份 ⚠️
- backup.sh（pg_dump custom 格式 + 保留期清理）✓；restore_test.sh/backup_verify.sh 已存在（本地未提交部分见"待办"）
- ⏳ 容器化 cron 调度方案已设计但 docker/backup/ 等文件**尚未提交**；Redis RDB/WAL 归档仍缺实现；季度演练为手动 SOP

---

## 六、性能与部署审查

### 6.1 前端资源 ⚠️
- 路由懒加载 ✓（121 个按需 chunk）；构建产物完整 ✓
- ❌ G-4：Nginx 模板**未配置 gzip**——JS/CSS 未压缩传输，首屏性能受损。需在 http/server 级添加 gzip on + types（text/css application/javascript 等）
- sourcemap 生产关闭 ✓

### 6.2 后端性能 ⚠️
- DB 连接池 CONN_MAX_AGE=600 + CONN_HEALTH_CHECKS ✓
- Prometheus 指标埋点（QPS/P95/P99/活跃请求）✓
- ⏳ G-5：压测未执行——上线前应对登录/资产列表/批量导入三条关键链路压测并记录基线

### 6.3 服务托管 ✅
- Compose 托管 + restart:unless-stopped/always + stop_grace_period:30s（优雅停机窗口）✓
- daphne 对 SIGTERM 的处理满足优雅停机基本要求；健康检查 /health /ready 双端点 ✓

### 6.4 发布与回滚 ⚠️
- 版本标识：APP_VERSION 环境变量 + git tag 可用 ✓
- 回滚方案依赖镜像 tag 保留策略——建议 CI 固定保留最近 5 个镜像版本并在 DEPLOYMENT.md 写明回滚步骤（登记待办）

---

## 七、监控与运维审查 ✅（本会话补齐主要缺口）

| 组件 | 状态 |
|:--|:--|
| 结构化日志(JSON+trace_id, console/file 双 handler) | ✅ commit 系列 |
| Sentry 错误追踪（条件启用, profiling 开启） | ✅ commit `373095d`/`dff069b` |
| Prometheus 指标 + 5 条告警规则 | ✅ 既有 |
| Alertmanager 告警路由(matchers 语法) | ✅ commit `5ab4b14` |
| Grafana provisioning(数据源+仪表盘) | 🔧 方案已审待执行（注意 asset-network external 命名与 status_code label 两处修正） |
| 通知渠道(钉钉/飞书适配器) | ⏳ 需运维部署 webhook 适配器后填写地址 |

---

## 阻断与待办清单（按优先级）

| # | 级别 | 事项 | 关联 |
|:--|:--|:--|:--|
| G-1 | 上线前必须 | 生产 env 设置 CORS_ALLOWED_ORIGINS | §3.4 |
| G-2 | 上线前必须 | 轮换 git 历史泄露的数据库密码 | §2.2/P2-1 |
| G-3 | 上线前必须 | 补生产数据初始化命令（管理员/角色/菜单） | §5.3 |
| G-4 | 上线前强烈建议 | Nginx 启用 gzip | §6.1 |
| G-5 | 上线前建议 | 关键链路压测并记录基线 | §6.2 |
| T-1 | 迭代号 | 前端清理 22 处 console.log | §1.3 |
| T-2 | 迭代号 | HSTS/安全头归属统一治理（方案已审） | §4.4 |
| T-3 | 迭代号 | Redis/WAL 备份实现 + 备份容器化 cron 提交 | §5.4 |
| T-4 | 迭代号 | pip-audit 进 CI；压测常态化；镜像保留策略 | §1.2/§6 |
| T-5 | 运维 | 后端 15 个本地提交 force push（网络恢复后） | — |
| T-6 | 迭代号 | recycle_asset_service 覆盖率 74%→90% | CT-2 |
| T-7 | 迭代号 | unregisteredasset CREATE_FAILED 变体后续（str(e) 直出已在本次修复范围外评估） | 账本 |

---

## 审计票

```
[审计票 - 必填项]
- 读取规范：已读 [后端/前端] AGENTS & Rules + 根级 §1~§9
- CT-1[√] CT-3[√] CT-5[√] — 864 后端 + 1420 前端用例全绿
- DR-1[√] DR-5[√] — 触及文件合规
- SC-1[√] SC-3[√] SC-5[√] SC-6[√] SC-7[~] — pip-audit 待进 CI
- OC-1[√] OC-2[√] OC-6[√] — 本会话补齐
- 跨端契约：未破坏（error_code 变更持前端零消费证据）
- 红线触发：无
- 建议提交：是
[审计票 - 自检项]
- Fact-1[√] 所有问题判定均附实证（grep/构建/测试输出）
- Style-1~3[√]
- 覆盖率：后端整体达标，recycle_asset_service 74%（T-6 登记）
```
